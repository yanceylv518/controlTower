# 验收记录：调权中心工作台改进（2026-08-02）

范围：ddff904（前端单文件，rc21 后、rc22 候选）。验收环境：Node 22（vue-tsc）。

## 结论：通过，1 个 P2 风险已由验收方加护栏修复（7094368）

### 通过项

- 模型区改为主从工作台（搜索 + 单选 + 每模型异常数角标），大模型量下可用性明显更好；空值防御（`?? []`）与 activeModel 兜底完备；
- **帮助抽屉内容逐条对照引擎行为核验准确**：倍率公式与 1.5 封顶、TTFT 三元组 50/30/20、组内 ≥2 合格渠道、数据不足保持 1.000、普通降权最低 1/熔断为 0、恢复倍率=成功率×探测速度（与实现一致）、人工修改暂停——文档质量高；
- **顺手修正两处旧问题**："最大加权倍数"误导性文案改为"输出加权上限"并注明不是最终上限；otps_cap 表单上限 5→3 对齐 server 校验（旧表单填 4 会被 400）；
- 参数表单移除"低流量回看"——核实该参数在连续引擎中零消费（死参数），移除诚实；schema 清理记档；
- 基础权重下限 1→0（配合引擎语义：基础 0 = 人工停用分流）；typecheck 通过。

### 验收修正（P2 风险，已修）

**"初始化/刷新基础值"变为一键自动落库**（旧流程为回填标黄+人工保存）。首次初始化无碍，但 auto 调度运行后再点击，会把线上当前权重（= 基础×倍率的计算结果）覆盖为新基础值——**锚点被污染为计算值，倍率此后在污染值上自乘**。修复：非首次刷新弹确认框，明示"覆盖并保存"的后果。

### 记档

未立项交付、无自查清单（惯例）；`continuous.sparse_lookback_minutes` 为死参数，随下次 schema 清理移除。

## 部署

本笔 + 护栏修复一起进 rc22（用户计划中）。
