# 验收记录：调度可观测性与只读访问改进（2026-08-02）

范围：c7df5ca（未立项交付）。验收环境：Linux，Go 1.24.5 + Node 22（typecheck + build）。

## 结论：通过，1 处迁移撞号已由验收方修正（0c1cb66）

### 通过项

- **评估可解释性**：状态表新增 metric_ready/baseline_ready，页面逐渠道给出"为何未参与计算"的人话解释（样本不足 n/m、缺 TTFT、合格渠道不足 2、模型关闭、混布暂停）——正是 observe 阶段运维最需要的排障信息；样本进度口径改为当前完整窗口（展示语义，K_error 折算游标不受影响，已核）；
- **observe 证据留痕**：新事件 `weight_observed` 仅在拟议权重发生实际变化时记录（有噪音控制，非每分钟刷屏）；页面事件人话化命名、30 秒运行态轮询；
- 直连页面/实例页多处打磨；scope 强制路径复核未松动；全量测试 + typecheck + **build**（新纳入验收流程）全绿。

### 验收修正

**迁移撞号**：新文件用了 022（与 v3.1-B1 的 022_scoped_users 重复）。重放容错的迁移器两个都会执行、功能无碍，但编号是 schema 历史的审计线——未发布期直接改名为 024，规划中的账单迁移顺移 025（B3 批次文件与设计文档同步修订）。

### 记档

无自查清单（未立项惯例）；ContinuousTuningView 引入 30s 轮询——多人同时挂着调权中心时对 server 是小量常态请求，量级可忽略，记录在案。
