# 验收记录：auto 权威化——每变必写+覆盖外部修改+界面精简（2026-08-11）

范围：461a419（引擎语义）+ ff41544（移除异常徽标）+ 175f24d（帮助抽屉精简）。codex 交付。验收环境：Linux,vet+全量三树测试、直连真库集成、vue-tsc、desktop build 全绿。

## 结论：通过;**两项语义反转经用户确认为本人指示（2026-08-11 拍板）**;两条 P3 记档

## 语义反转（用户确认）

1. **撤销写入迟滞（推翻 2cc6f4c,当日拍板即当日撤销）**：auto 写入不再受死区与最小间隔约束——每分钟重算,整数计算权重相对上次已写值有任何变化即写入 newapi;仅保留精确值去重防陈旧快照重发。写入量回到"漂移渠道≈每分钟一次 PUT+一条 weight_write 流水"。
2. **废除人工接管暂停,auto 权威化**：原"检测到外部修改→manual_override 暂停+事件"改为"确认的外部修改（快照晚于我方写入 2 分钟且值不符）→ 下一轮直接写回当前计算值";旧 manual_override 暂停升级后首轮被引擎清除,不再产生 manual_takeover 事件。**人工逃生口=把模型切出 auto**（off/observe 下引擎不写）,帮助抽屉已明文记载新哲学。

## 核验

- needsWrite = proposed≠writeReference(上次已写,未写过则当前值) ∨ 确认外部修改——陈旧快照期间不重发（confirmedExternalChange 要求快照晚于写入+2min）,写回后自身写入刷新 LastWriteAt 天然抑制连发;
- write_failed 暂停/慢速重试/蒸发清除、熔断置零、探针恢复、observe 事件锚点死区全部原样保留（测试在）;
- 三个语义测试正确改写钉新行为：每整数变化必写/不等最小间隔/确认外部修改后重新断言计算值;
- UI：异常徽标与"关注 N"移除,评估状态列改为直接显示暂停/熔断/恢复原因（净改善）;帮助抽屉精简为六节,与新语义一致。

## 记档（P3,建议下批清理）

- **min_write_interval_minutes 成彻底死参数**（引擎零引用,模型/校验/类型/defaults 仍在,设置页输入已删）——应从策略 schema 连同校验一起退役;
- **write_deadband_percent 成暗参数**：仅剩 observe 事件锚点粒度在用,设置页输入已删——已存策略里的非默认值会继续生效但无处可改;要么恢复输入框（标注仅影响观察事件粒度）要么固定常量退役参数;
- 行为记档：auto 模型的变更流水回到高频（每漂移分钟一条 weight_write）,300 条客户端上限下熔断/恢复事件可见性依赖事件类型筛选器。

## 部署

无迁移。rc45 不含本三笔,含本批需 rc46;生产语义提示：**升级后 auto 模型下人工在 newapi 改权重会在 1-2 分钟内被写回,操作渠道权重前先把模型切出 auto**。

## 跟进（bddd76b,同日,验收通过零缺陷）

**修正本记录上文的一处不准确**:我写"旧 manual_override 升级后首轮被引擎清除"——对 461a419 而言仅对 normal 相位渠道成立,清理代码位于混布/零权重/熔断/探针诸 early-return 之后,非 normal 相位渠道的残留暂停清不掉,且会经 writeAttemptAllowed 阻塞熔断写入等执行路径。bddd76b 将清理挪至渠道循环最顶端（任何相位/模式必经）,新增"circuit 提前返回也必须清"的测试;前端加 effectivePause 过滤器,server 未升级或状态未刷新期间也不显示已废弃的"人工修改后已暂停";帮助文案改为"被人工或其他系统修改,不会暂停调权"更准确。全量+真库集成+build 绿。
