# 验收记录：v3.0-B1 渠道基础值（2026-08-01）

范围：c8acb0e（B1 主体）、8d7d70e（搭车交付：移除降级标准界面）。验收环境：Linux，Go 1.24.5 + Node 22。

## 结论：B1 主体通过零缺陷；搭车改动已回退（99bd5ed）

### B1 主体（c8acb0e，通过）

- `go vet` + 全量测试 + `pnpm typecheck` 全绿；自查清单已贴。
- 018 纯增量 CREATE TABLE（联合主键+按模型索引）+ 反向断言测试；base 表 CRUD 与批量 PUT（校验、去重、事务、operation_audits `tuning.base_update` 含 before/after）；sync 接口只回填不落库（有测试）；`dispatch_modes` 缺省 off、旧 JSON 兼容、值域校验均有测试；**引擎零改动**（engine.go 不在 diff 内），回归成立；api-contracts 已更新；页面基础值区块按模型分组、同步回填标未保存态、保存才生效——逐项符合批次文件。

### 降级标准界面移除（8d7d70e）——验收误判更正

验收方最初以"v2.9 引擎仍在用这些阈值、界面退役应随 B2"为由 revert（99bd5ed）；**用户澄清该移除是其明确指示**（值班模型在 v3.0 方向下已判死刑），已重新应用（5ee64a5）。过渡态记档：B2 切换引擎前，v2.9 按库存/默认阈值继续判定但无界面可调——用户知情接受。criteria schema 的数据层清理仍归 B2。

## 部署提示

018 迁移随下次 Server 发布自动执行；基础值区块与开关立即可用（开关在 B2 前无引擎消费，仅存储）。
