# 上游账单页面整理

- 标识：2026-09-18-upstream-billing-layout
- 更新时间：2026-09-18 08:40 +08:00
- 状态：待页面验收
- 目标与验收条件：整理上游账单信息层级、筛选与宽表，改善窄窗口和详情弹窗高度；保留账单计算及下载行为。
- 负责会话、分支和修改范围：当前会话，main；实际路由 ChannelBillingV4View 使用的 BillingRecordsView 及 billingGroups 回归。未修改旧版 ChannelBillingView。
- 实际结果与关键决策：按站点+上游 ID 归组，组内账期倒序、同账期保留所有生成版本；每页 20 组。增加名称/ID/账单编号、重叠账期（包含结束日）、待复核/可使用筛选，数量随筛选更新。上游及站点移至组标题，内部任务 ID 在悬停和详情保留；订单数、异常与差异合并展示，生成时间随账期呈现。保留原最新 200 条已完成任务限制及提示。
- 详情与响应式：上游详情最大 92dvh，正文滚动、头尾固定；宽屏指标一行，窄屏三列/两列，计费说明折叠，复核警告保持可见。监听实际容器，低于 900px 取消操作列固定以避免遮挡，横向滚动条常显；历史价格弹层宽度不超视口。用户账单的分组/日期行为保留，共享表格滚动条和价格弹层响应式同步改善。
- 验证：node --test webapp/packages/desktop/tests/billingGroups.test.mjs，6 项通过（3 项用户账单原回归、3 项上游归组/筛选/分页新增回归）；最终 pnpm typecheck、pnpm build 通过，构建有既有大包提示；git diff --check 通过。Vite 日志显示组件热更新。
- 未验证与阻塞：浏览器连接此前持续失败，本轮未执行真实页面视觉验收；未改 Go 和导出函数，未重复 Go 测试或生成真实账单文件；无迁移、未打包发布部署。
- 下一步：本地验收上游折叠列表、复核筛选和详情在宽/窄窗口的显示。仅前端更新即可生效。
- 相关设计：[上游账单页面](../upstream-billing-page.md)。
