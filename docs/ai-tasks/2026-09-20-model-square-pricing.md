# 模型广场币种与表达式价格

## 卡片布局续修

- 提交发布准备：用户明确要求提交、推送并打包。252项全量前端回归通过，本版typecheck/build已通过；将同时包含已提交的日志精确时间范围修复。仅纳入本任务业务和交接文件，其余未跟踪设计/历史文件保留。正式发布结果另行回填。

- new-api参考图续修：卡片统一名称/厂商/复制、描述、计费标签、双列输入输出、分组/端点、详情结构；桌面三列、超宽四列、手机一列。动态计费只展示第一档主要价格并标明档位及总档数，详情保留全部档位。价格加站点币种符号；不虚构状态/延迟/吞吐数据。Chrome独立本地5192手机页面截图验证实际CNY价格和单列卡片，无横向截断；此前桌面验证不代表本版桌面已验收。

- 用户截图暴露表达式竖向价格列表撑高整行，普通卡片auto边距形成巨大空白。改网格align-items:start、普通价格自然排列；表达式前两档各用一行输入/输出/缓存摘要，每档最多三个价格项，完整档位字段保留详情。
- 隐藏空说明占位，顶部介绍、卡片内边距及分组标签收紧；小屏筛选两列、搜索独占一行，卡片单列。
- 本轮typecheck/build通过（原有chunk提示）；Chrome本地5192真实数据桌面截图确认币种/档位价格可见，普通卡片约230px、双档位约280px，首屏可见两整行。手机未实测，纯模板样式未重复价格单元测试；未提交推送或打包。

- 标识：2026-09-20-model-square-pricing
- 更新时间：2026-09-20，Asia/Shanghai。
- 状态：实现完成，待页面验收。
- 目标与验收条件：模型价格遵循选中new-api站点的显示币种；表达式模型展示可确定的具体档位单价。
- 负责会话、分支和范围：当前会话，main；ModelSquareView.vue、utils/modelSquare.ts及modelSquare.test.mjs。没有Go或计费执行逻辑变更。
- 实际结果与关键决策：复用已有passthrough/currency接口，获取type/symbol/price_multiplier，支持USD、CNY、CUSTOM及TOKENS；无配置时显示缺失提示，不默认美元。货币请求与模型请求共用序号隔离旧响应，切换站点立即清空配置。普通模型和按次价格、表达式系数统一进行分组与币种换算。
- 表达式：只读取tier里的完整线性价格项或fixed常量，不执行表达式；支持输入/输出/缓存/图像/音频单价，保留零价和科学记数。卡片展示前两档，详情展示所有档位；明确标注基础单价，命中条件及额外请求倍率由完整规则说明。遇动态计算、重复变量、不完整项和非法数字不猜单价。规则原文保留原始单位。
- 验证：node --test packages/desktop/tests/modelSquare.test.mjs，7项通过；webapp中pnpm typecheck、pnpm build成功，保留已有大chunk提示；git diff --check通过。首次沙箱Node子进程EPERM，提升执行权限后通过。
- 未验证与阻塞：未进行本轮浏览器/真机、真实站点数据及断网切站人工验收；任意任务表达式、非线性规则不能展示固定价，保留原文。未提交、推送、部署或打包。
- 下一步：用户在本地联调页核对本站币种和表达式模型；按用户安排决定提交及发布。
- 来源：仓库现有site_currency.go与modelSquare实现；2026-09-20对照new-api官方表达式文档 https://github.com/QuantumNous/new-api/blob/main/pkg/billingexpr/expr.md （系数为USD/1M，fixed为USD/次，分组及请求倍率另外应用）。
