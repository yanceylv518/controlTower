# 归档累计字节超限修复

- 标识：2026-09-25-archive-byte-pagination
- 更新时间：2026-09-25，北京时间
- 状态：实现和本地验证完成，未提交、推送、发布或部署。
- 目标与验收条件：历史归档累计超过8 MiB不再整批反复失败；从现有检查点继续，无漏行、重复统计或提前封存。
- 负责会话、分支和范围：当前归档会话；codex/archive-byte-pagination，基于最新origin/main d610eded；独立worktree local/archive-history-20260924。主目录已有修改保持原样。
- 现场证据：用户归档库2026-07-13 verify_archive，游标1783943822/4710112；后1000条10882162字节，前500条10616911字节，最大单条170885字节。根因为累计字节超限而非单条超过8 MiB。
- 实际结果：readPage返回完整记录和byteLimited标记；达到预算时保存前缀、下批重新读取未接纳行；历史三个阶段仅在未截断且行数不足时推进。点查保留readRows封装，禁止静默截断。单条超限提供ID、字节数、上限，不泄露内容且不跳过；有效前缀可先提交。原SQL排序、事务、摘要算法和调度间隔保留。
- 验证：go vet ./...、go test ./...通过（未配置数据库的其他模块集成测试仍按原逻辑跳过）；本轮隔离MySQL8测试容器127.0.0.1:33385，新引擎全包8.456s通过。新增专项4.102s通过：3条各3 MiB跨预算分页，采集和所有历史阶段接续、模拟重启、日统计触发器故障回滚再重试、封存行数3/额度21、单条超限重复执行不推进。git diff --check通过。Web未改，未运行Web检查。
- 未验证与阻塞：未生产负载验收、实际升级或浏览器验收；字节预算限制字段总量而非全部堆内存，驱动读缓冲及完整单条仍有额外开销；不涉及原统计进度显示问题。
- 下一步：按后续指令提交发布并升级Agent；无需数据库迁移、清库、重置游标或重做已封存日期。
- 相关文件：agent/internal/archivejob/raw.go、history.go、summary.go、byte_page_integration_test.go；docs/archive-new-engine.md。


## 后续：交替批次可配置

- 用户追加要求采集与历史处理的交替批次数量可配置；本轮与字节分页修复保存在同一分支，未提交/发布/部署。
- 设置新增两个1–100整数，默认采集4/历史1；协议和Server通用配置校验接入，现有JSON保存/下发，无迁移。Agent持久化轮次、旧状态保留已有采集次数，比例变化开始新周期、单任务连续执行。
- 验证：新增4:1/2:3/1:1轮次与JSON重启恢复、比例变化、旧检查点及单任务测试，配置范围/序列化测试通过；7项页面行为测试通过，含旧配置默认填充、自定义提交和任务启停保留比例。最终go vet ./...、go test ./...、Web typecheck/build通过（既有chunk提示）；调整调度后新引擎全包隔离MySQL8.570s通过。
- 未验证：浏览器视觉、生产升级与跨版本实机验收。旧Agent不支持新比例，须配套Server/Web/Agent更新；本功能按执行机会轮换，沿用错误批次原有重试行为。
