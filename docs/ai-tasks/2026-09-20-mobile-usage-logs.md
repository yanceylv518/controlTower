# 使用日志手机端适配

- 标识：2026-09-20-mobile-usage-logs
- 更新时间：2026-09-20，Asia/Shanghai
- 状态：待真机验收
- 目标与验收条件：按用户确认的三屏设计实现日志列表、更多筛选和详情；保留查看账号用户名查询及原有权限边界，桌面布局继续可用。
- 负责会话、分支和修改范围：当前 Codex 会话，main 未提交工作区；AppShell、AccountMenu、CompactDateTimeRangePicker、MobileLogFilters、ReadonlyLogsView 及相关测试。保留原有用户名筛选修复及其他会话文件。

## 实际结果与设计决策

- 900px 以下 viewer 隐藏侧栏，使用客户监控、用户管理、使用日志三项底部导航；缩窄顶栏与账号入口。此轮完整适配使用日志，未重做另外两页的内容。
- 用户名保留首屏搜索；日期使用紧凑入口，其他条件放入底部抽屉。抽屉编辑草稿，点击应用后统一查询，关闭不应用；重置草稿与重置全部筛选分开。渠道条件仅管理员可见。
- 日志卡片呈现时间、真实日志类型、用户/模型、令牌、费用、输入输出和耗时；保留管理员渠道信息及 fallback 展开入口。移动端提供上一页/下一页。
- 详情全屏、内容独立滚动、底部复制请求 ID；计费与完整信息可展开。保留敏感字段隐藏，关闭恢复触发按钮焦点，支持 Escape 与 Tab 焦点约束。
- 桌面继续使用原表格和筛选栏；后端 API、授权范围、计费计算未改。

## 验证

- 当前工作区 `pnpm typecheck`、`pnpm build` 通过；构建仍有大于 500kB 的 chunk 提示。
- `node --test --experimental-test-isolation=none --test-reporter=dot tests/mobileLogFilters.test.mjs tests/readonlyLogsColumns.test.mjs tests/fallbackRequestChain.test.mjs tests/readonlyLogsBillingSummary.test.mjs`：51 项通过。
- 独立本地示例数据服务 5193（不访问远程）浏览器检查：375/390/430px 无横向溢出；用户名 bob 查询从 4 条变 1 条；模型筛选从 4 条变 2 条；错误详情展示、内容滚动和关闭焦点通过；375×812 下固定详情页底栏 bottom=812，统计卡片高度与内容均为 44px。
- 1440px 回归：桌面表格显示、移动导航隐藏、无页面横向溢出。浏览器临时尺寸覆盖已恢复。
- `git diff --check` 通过。

## 未验证与下一步

- 真实手机软键盘、浏览器安全区域、真实账号远程日志及管理员手机视图尚未人工验收。无后端修改，本轮未重跑 Go 测试。
- 用户本地远程联调预览：`http://127.0.0.1:5192/readonly-logs`；保持既有远程代理，仅使用本地前端代码。5193 仅为示例数据验证，不应当作生产日志。
- 下一步：在 5192 登录查看账号验收，真机复核筛选和详情；本轮未提交、推送或部署。
- 相关代码：`webapp/packages/desktop/src/components/MobileLogFilters.vue`、`webapp/packages/desktop/src/views/ReadonlyLogsView.vue`；用户名问题见同目录 `2026-09-20-viewer-log-username-filter.md`。

## 用户管理与客户监控补充

- 更新时间：2026-09-20，Asia/Shanghai；用户明确要求将用户管理、客户监控也适配手机，状态为待真机验收。
- 用户管理：900px 以下展示用户身份/状态、剩余额度与进度、已用及总额度卡片，创建/最后登录时间折叠，保留原权限控制的余额告警开关。筛选纵排、44px 控件、上一页/下一页；复用原查询和分页接口。
- 客户监控：时间范围、搜索与刷新适应窄屏；图表单列，排名明细卡片化，保留加入趋势图和详情入口。Top 10 小屏缩减左右留白和柱外标签，随容器尺寸重绘。流量大图小屏全屏，图例和维度按钮加大点击区。
- 客户详情共用组件补充窄屏指标两列、趋势一列、长标识换行与表格容器约束；其他维度详情也受这些响应式样式影响，未改查询、阈值和权限。
- 修改：ReadonlyUsersView、CustomerMonitorView、DimensionDetailView、CustomerTrafficCard、CustomerTrafficPanel、CustomerTokenChart。桌面保留表格；此前未提交改动保留。
- 验证：最终 `pnpm typecheck` / `pnpm build` 通过（仍有原有大 chunk 提示）；customerTraffic、customerTrafficRefresh、customerTooltip、modelCustomerNames 共 31 项测试通过；`git diff --check` 通过。
- 浏览器证据：独立本地示例服务 5193，375px 两页 document.scrollWidth=360；用户 13 条由第一页 10 条翻至第二页 3 条，搜索无匹配显示空态及 1/1；客户排名卡片、Top 10 图和长名称视觉检查；430px 客户 scrollWidth=415；1440px 桌面表格显示且手机卡片隐藏。尺寸覆盖已重置。
- 未验收：真机键盘/安全区域、真实数据、管理员余额告警操作、客户详情及有实际拆分数据的大图。示例流量图无拆分数据，仅验证布局和空态。无 Go 修改，本轮未运行 Go 检查。
- 预览：`http://127.0.0.1:5192/readonly-users` 和 `http://127.0.0.1:5192/customers`，继续连接已配置远程服务。未提交、推送或部署。

## 手机滚动加载补充

- 更新时间：2026-09-20，Asia/Shanghai；用户确认手机改为滚动分页。此节替代上文手机上一页/下一页方案，状态待真机验收。
- 使用日志/用户列表复用 `useAppendPages`，保存首批查询参数快照，触底请求下一批并保留已有记录及滚动位置；按 ID 去重，offset 按原响应条数推进；空页停止，失败保留旧记录且允许同 offset 重试。首批查询刷新时阻断追加并使旧请求失效，新响应清空旧追加结果，组件销毁时取消。
- 共用 `ScrollLoadMore` 通过 IntersectionObserver 提前 180px 加载，显示加载中/重试/已全部加载；附带手动加载入口，错误不自动重试。桌面隐藏该入口并保留原分页和页大小选择。
- 客户排名在已取得指标数据中每批展示 20 条，搜索/时间/站点切换重置数量；客户流量图沿用现有可见区域按需加载拆分数据的机制。
- 验证：最终 `pnpm typecheck`、`pnpm build` 通过；appendPages 新增 5 项行为测试，与日志/客户回归合计 87 项通过；`git diff --check` 通过。构建仍有已有大 chunk 提示。
- 390px 本地样例浏览器：用户触底从 10→13 条；筛选 user13 后仅一条，追加的其他用户被清除；日志从 100→105 条且原首条保留、scrollY 保持在列表底部附近；客户排名从 20→25 条；各列表终点显示已全部加载。1440px 客户桌面表格及分页显示、手机卡片隐藏。浏览器覆盖尺寸已恢复。
- 尚未验证：真机手势/键盘、真实远程大数据量、动态增删数据导致的 offset 分页漂移；后端协议保持不变，此轮没有引入游标快照。用户列表接口不支持 AbortSignal，使用请求代次忽略晚到响应，日志请求支持中止。
- 当前仅本地修改，5192 继续远程联调，未提交推送或部署；无 Go 改动，未运行 Go 测试。

## 管理员全站手机适配补充

- 更新时间：2026-09-20，Asia/Shanghai；用户要求按已确认设计逐页适配、补足调权优先级与分组，同时保留电脑布局。状态：本地实现与浏览器检查完成，待真实手机及真实管理员数据验收。
- 范围：当前会话、main 未提交工作区。新增 `useMobileViewport`、`MobileTuningEditor`、`mobileCards` 指令、`mobile.css`；修改 AppShell、SiteSelect、Overview、ContinuousTuning、各管理表格及相关测试。先前日志、用户、客户适配和其他未提交文件均保留。

### 当前设计与行为

- 视口宽度 ≤900px 自动使用管理员底部“总览 / 监控 / 调权 / 日志 / 更多”；导航按已有权限过滤，告警进入更多菜单。监控提供客户、渠道、模型、系统状态分类。更多菜单与站点选择采用可搜索底部抽屉；总览增加调权快捷入口。
- 手机调权按模型选择，渠道卡片常显线上优先级、分组（前三个与剩余数量）、基础/计算参考/线上权重、容量及状态；支持搜索、状态筛选、逐批展示。优先级与基础值使用全屏编辑草稿，确认页显示基础值、容量、优先级、模型模式及规则逐项前后差异。手机未保存栏固定在导航上方。
- 优先级沿用现有线上同步语义及熔断/探测锁定；基础权重沿用现有计算行为。分组复用原组合库、多选、增删预览和确认流程，独立提交。编辑基础值的表单仅展示当前分组，避免跳入另一编辑器丢失未预览草稿。
- 管理表格小屏显示字段名与内容的纵向卡片，保留原行操作、选择和排序入口；没有隐藏数据列。数据属性避免 Element Plus 内部刷新覆盖布局标记。表格与嵌套详情、弹窗、日期控件、分页、长文本、设置表单统一约束宽度。修复登录、容器日志、模型摘要、告警卡片、调权性能参数标签等具体窄屏问题。
- 桌面继续使用原侧栏、表格、筛选和编辑逻辑；新增布局样式仅在小屏生效。原日志/用户/客户滚动加载保留，其他管理列表继续使用其原查询与分页协议。未修改 API、鉴权、计费或后端调权规则。

### 本轮验证证据

- `pnpm typecheck`、最终 `pnpm build` 通过；构建仍提示已有的 >500kB chunk。全套 `node --test packages/desktop/tests/*.test.mjs`：181 项通过，包含新增手机调权刷新后编辑、撤销、锁定与规则差异测试。测试初次因沙箱禁止 spawn 失败，在正常本地执行后通过；已更新既有测试的 viewport composable 模拟。
- 独立模拟服务 5194（不连接远程）逐页检查 26 个主页面：总览、客户/渠道/模型监控、系统状态、用量、用户、使用日志、容器日志、用户/上游账单、任务、折扣、上游管理、模型管理、账单核对、调权、告警、通知、实例、归档、账号、设置、审计、样本、延时。375px 页面级无横向溢出。另检查客户/渠道/模型详情与登录页；无权限/404 共用壳结构经代码检查，完整管理员访问时会按现有路由守卫重定向，未伪造其浏览器验收。
- 上述 26 个主页面在 1440px 回查：侧栏宽 188px，手机导航不存在，表格行保持 table-row，无页面级横向溢出。调权桌面截图确认原模型侧栏与权重表格仍显示。430/900px 手机布局、901px 桌面布局切换通过。
- 有数据的实例、模型管理、渠道排名、告警、三类详情和调权进行布局检查；渠道排名最终卡片宽度与表格容器均为 349px（390px 视口含滚动条）。其余部分管理页使用空数据/未配置态，不能视为全部业务数据组合已验收。
- 交互检查：更多菜单搜索“调权”与跳转；站点搜索空结果；调权基础值/优先级编辑预览与取消；分组 VIP 组合替换时明确展示 premium/backup 移除且未提交；性能系数标签可见；模型价格、实例创建、通知编辑、账单任务、管理员创建/权限选择表单打开与取消。所有远程写操作均未执行。
- 5194 初期不完整模拟响应曾导致实例 agents、核对 job、账号 permissions 报错，补齐模拟数据后相关界面重新验证；不是修改真实 API 响应或绕过权限。
- 查看账号 5193 回归：390px 无溢出、三项导航正常且无管理员导航；宽屏表格保留。浏览器尺寸覆盖已重置。最终 `git diff --check` 通过。

### 交付边界与下一步

- 本地远程联调仍使用 `http://127.0.0.1:5192/`；5193/5194 仅为独立模拟验收，不能当作真实数据。未提交、推送、部署，也未向 AI Workspace 上传。
- 需用户用真实管理员账号和手机复核软键盘、安全区域、长列表及真实分组/优先级保存。模拟环境没有验证远程写入成功、生产数据规模和所有账单明细组合；无 Go 修改，本轮未运行 Go 检查。

### 调权首屏精修（2026-09-20）

- 时间范围交付续记：用户要求提交推送，随后明确暂不打包。全量249项前端测试通过，本修复此前typecheck/build通过；本次仅提交推送修复及相关测试记录，不创建版本标签，不发布安装包或镜像。真机/浏览器验收仍待完成，rc124不含本修复。

- 日志时间区间续修：用户反馈仅有日期，定位为compact触发器隐藏时间且原生datetime-local手机时间入口不明显。compact弹窗每端显示独立date/time（step=1），草稿仍统一保存本地日期时间并确认后emit；触发器独立一行显示两端月日与时分秒。解析支持可选秒，拒绝非法日期/时间及结束不晚于开始；桌面保留双datetime-local并支持秒，原分钟触发摘要不变。新增解析与编辑行为测试，更新旧禁止拆分控件的静态断言；36项相关测试、typecheck/build、diff check通过，已有chunk提示。未进行浏览器或真机验收，未提交推送打包发布，不包含在rc124中。

- 正式发布续记：用户明确确认GitHub Release及GHCR/latest发布后，推送v2.0.0-rc124到3a7f57bb。release运行35476816881全部成功；Release非草稿，Server amd64、Agent amd64/arm64及SHA256SUMS均uploaded，API提供四附件digest。此前CI 35476402140成功。正式附件在Linux CI独立重建，大小/哈希不等于Windows本地候选包；使用Release对应SHA256SUMS。gh未登录，改用已连接GitHub只读接口检查发布结果。未部署服务器或修改业务配置。

- 本地打包交付：用户要求打包，核对远端最新tag为rc123，基于3a7f57bb生成v2.0.0-rc124本地候选包。按deploy/package.sh内容生成到独立dist/releases/v2.0.0-rc124（旧dist/release不动，复用已安装锁定依赖、重新构建前端）。Server amd64 6,432,452字节；Agent amd64 6,154,248字节、arm64 5,561,310字节。Windows打包初始执行位缺失，已统一包内目录/二进制/安装脚本755、其余文件644并重算SHA256SUMS。核验三包哈希、ELF机器架构、脚本LF、Agent辅助log-reader及配置示例、Server 083迁移与最新前端入口通过。未创建远端tag/Release、未推送镜像或部署，未Linux实机执行；本地stage和打包脚本保留用于溯源。

- 提交交付（2026-09-20）：按用户明确授权，将本轮49个文件提交为278f0251并成功推送origin/main。推送前衔接远端新增日志渲染优化2a38432d，重新执行247项前端回归、typecheck/build、提交diff检查全部通过（已有chunk提示）。缓存、构建产物和无关历史文件未纳入；本次未发布部署，未上传AI Workspace。此前各轮“未提交推送”描述为对应阶段历史状态。

- 设置页签文字偏上续修：共享手机规则把small radio按钮最小高度提升至36px，但原文字仍按小尺寸padding排列；使用inline-flex、align-items/justify-content:center保持实际高度及文字居中。Chrome真实5192设置三个页签均36px，DOM文字范围与按钮垂直中心差0px。typecheck/build、diff check通过（已有chunk提示），仅≤900px样式，无业务逻辑变更未重复单元测试；未提交推送部署。

- 最新主线同步：用户发现设置少“菜单显示”，确认本地e06bc785落后远端10个提交。按授权fetch、保存跟踪文件stash、快进至4b71092b后apply恢复；stash保留，未跟踪文件留在原地。解决11个文件冲突，旧价格页遵循主线删除并由模型广场替代；保留新版上游/通知、主题、日志查询快照与游标，手机追加分页使用已提交查询，导航接菜单可见性；调权基础优先级遵循新版可编辑语义并更新回归，保留手机保存预览。修复模板闭合及桌面flex-basis把手机模型头撑高的问题。
- 同步验证：247项前端回归全部通过；typecheck及最终build通过（已有chunk提示）；go vet ./...与go test ./...通过，未单独配置实库集成环境；diff check通过且无未合并文件。Chrome真实5192设置三页签可见，“菜单显示”可打开并读取现有配置，未保存；调权400px模型头约70px、首卡top约320px，无横向溢出。此次不是新版全站视觉验收；另一个用户标签已自行转到调权，未把它作为日志回归证据，该标签记录一次远端http500，未修改远端服务。未提交推送部署；保留本地stash mobile-before-main-update-20260920以便回溯。

- 概览卡片占比续修：仅≤900px将调权入口改横排、padding10×12px，指标隐藏独立图标行、padding10×12px、数值20px、网格gap8px；保留两列及完整数值，趋势区顶部间距10px。Chrome当前5192真实概览400px实测入口42.8px、指标70.8px、趋势top460.8px，截图确认首屏完整TPM曲线且无横向溢出。typecheck/build、diff check通过（已有chunk提示）；纯样式未重跑单元测试，本轮桌面只检查变更限定媒体查询。未提交推送部署。

- 客户监控控件偏大续修：局部 scoped 样式仍保留44px，覆盖此前共享尺寸；将时间/视图/指标/流量图层切换收至约32px，搜索/刷新/排序与图表头部操作36px，分类导航padding降至8px，工具栏间距6px。同步用户管理筛选、摘要、告警控制及维度详情选择框36px。仅修改≤900px规则。Chrome真实5192客户页400px实测切换32/32.8px、输入与下拉36px、分类36.8px，无横向溢出，截图确认首图完整可见。typecheck/build、diff check通过（已有chunk提示）；纯样式未新增或重跑单元测试，桌面仅代码范围检查，未再进行宽屏浏览器回归。未提交推送部署。

- 手机错误码补充：从日志扩展字段及正文/摘要的明确代码字段提取错误码（支持结构化 error.code 与明确 status code 文本），在卡片状态旁常显，如“错误 · 429”；缺失不推断，成功日志不显示错误码，桌面模板不变。2 项解析回归通过，typecheck/build、diff check 通过（已有 chunk 提示）。当前 Chrome 5192 页面读取到 16 张错误卡片，前五张均已显示“错误 · 400”；400px 视口无横向溢出。未提交推送部署。

- 日志左侧空白续修：用户明确问题为左侧空白，实际 Chrome 5192 联调页测得 workspace x=56、宽344（400px 视口）。根因为旧日志手机样式强制保留折叠侧栏占位，优先级覆盖共享布局；移除该媒体查询内侧栏、workspace、顶栏及 content 旧外壳覆盖，共享布局接管宽度和底部导航安全留白。HMR 后实测 workspace x=0、宽400，内容左右12px，页面 scrollWidth=400，截图确认空白消失。本轮 typecheck/build、diff check 通过（已有 chunk 提示）；只删除 ≤900px 样式，桌面规则未修改，本轮未另跑桌面浏览器和单元测试。未提交推送部署。

- 留白反馈续修：调权优先级上间距 6px、分组上间距 4px、权重上下间距 8px（原 16px），卡片 padding 12px，卡片间距 8px。日志统计区域去掉额外上边框和 padding，筛选控件/日期/抽屉统一 36px；身份按钮 32px，令牌与指标间距 6px，详情入口 36px，长用户名/模型/渠道保留完整换行。仅修改小屏样式及仅手机挂载的筛选组件。
- 本轮 typecheck/build、diff check 通过（已有 chunk 提示）；400px 模拟调权间距测量通过，日志无横向溢出、普通示例卡片约 183px、输入和抽屉控件 36px，筛选抽屉打开关闭正常；1440px 桌面日志表格显示、手机筛选隐藏且无横向溢出。临时视口已恢复；纯样式未重跑单元测试，未执行远程写入、未提交推送部署，真实手机仍待反馈。

- 后续密度反馈：管理员手机普通输入/下拉/按钮由 44px 收至 36px，调权模式切换 32px，页签 36px；输入文字 14px，其余控件文字 13px，模型区域内边距和间距进一步缩小。保持底部导航和关闭按钮原触控尺寸。400px 浏览器实测选择框/搜索框 36px、模式 32px、页签 36px；1440px 原表格正常、手机导航不显示且无横向溢出。该轮 typecheck/build 与 diff check 通过，纯 CSS 未重跑单元测试；真机键盘缩放仍待验收，未推送部署。

- 根据用户实拍反馈，收拢四个次要操作为手机“操作”菜单；移除重复模型标题，将模型选择、执行模式、评估时间整合为同一块。搜索与状态选择同行，保留全部/需关注/待调整；默认文案明确为“全部渠道”。
- 去除手机工作区套层边框，统一卡片间距；渠道名称与状态分行，避免长名称与样本不足提示互相挤压。优先级、分组及三项权重继续常显，桌面原操作按钮和模型标题保留。
- 本轮 typecheck/build、diff check 通过（已有大 chunk 提示）；400×824 本地模拟检查无横向溢出，首卡 top≈380px，首屏可见三项权重。操作菜单可打开使用说明并返回；1440px 原四按钮和桌面表格保留。本轮未重复执行无逻辑变更的单元测试，上一轮 181 项结果仅为前轮记录。未提交推送部署，继续待真机反馈。
