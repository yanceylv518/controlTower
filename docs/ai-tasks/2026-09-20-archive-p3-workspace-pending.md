# Control Tower：P3 日期补齐与恢复实现

- 建议项目：Control Tower；分类：项目资料。
- 来源日期：2026-09-20，Asia/Shanghai；基线 main / 23468bae，工作区代码尚未提交或部署。
- 本次已确认推送待确认稿；Workspace 项目查询连续两次连接失败，尚未上传，保留本文件供恢复后提交。没有改写已入库资料。

## 完成内容

在 P0–P2 基础上增加 CT 087 与 Agent 009–010：日期补齐任务、覆盖策略确认、完整月份观察接口、行数/原始字节/耗时预算，以及近期轮转与指定日期扫描。

目标 `(task_id,attempt)` 和 `(created_at,id)` 游标独立于主增量。原始行、统计、日期修订、游标、回执及扫描完成在目标同事务提交；新 epoch 接管、暂停和临时重试保留权威进度。近期新轮次从日初重扫，以捕获迟到的小 ID。超大行不跳过，缺索引、来源已清理、历史未知分别记录。

覆盖起点及源保留边界必须显式登记证据；扫描成功只产生待核验或零行候选。P4–P6 核验封存、历史金额配置、归档账单尚未完成；P7 页面操作入口仍待接入。

## 验证与边界

- 全仓 `go vet ./...`、`go test ./...` 通过。
- 本机 MySQL 9.7 隔离库新增 13 个目标场景、7 组 CT P3 测试及 P1/P2/只读目录回归通过；随机测试库和账号已清理。
- 独立审查修正临时失败不能重试、授权失败绕过退避、长退避占用队列和目录状态不兼容等问题。
- 未执行生产迁移/启停、部署、实际账单生成；生产版本、规模、物理断电和网络故障仍待验收。本轮未修改 Web 或重跑浏览器。

## 仓库来源

- `docs/archive-p3-operations.md`：升级、策略、接口与恢复说明。
- `docs/ai-tasks/2026-09-20-log-archive-billing-design.md`：P3 交接与验证证据。
- `docs/implementation-log-archive-billing.md`：P0–P9 分期计划。

本稿不包含连接串、密码、Token、日志原文或完整对话。
