# 使用日志查询优化（2026-09-23）

基线：`df2d4f7c1`。本轮保留页面、接口、权限范围、时间边界、排序分页、默认消费统计口径、最近一分钟速率及已有缓存时长。不修改 NewAPI 表结构或索引，不强制指定未经生产执行计划验证的索引。

## 改动

- 错误码：保留六种原匹配模式分别执行，先用 `CASE WHEN LOCATE(目标错误码, 字段)=0 THEN 0 ELSE ... END` 排除不含目标码的字段。分别处理 `content` 和 `other`，保留 NULL 语义、不增加或删除历史格式。初版合并正则在长空白正文上触发资源上限，已撤回（见下文 502 修复）。
- 空输出：使用 `completion_tokens = 0 OR completion_tokens IS NULL`，保留 NULL 视为空输出的原语义。
- 最终记录：改写为顶层 `NOT EXISTS`；继续按同一用户、同一请求的 `(created_at,id)` 判断，空/NULL 请求 ID 保留，比较不受外层时间、渠道、类型条件限制。
- 链路展示：两次补查都限定当前页的请求/用户组合；同一记录的渠道序列只解析一次。viewer 不传输和解析最终不会返回的 `other` 链路元数据，但仍完成关联查询并保留 `fallback_checked` 的失败状态。链路不截断、不懒加载、不新增结果缓存。
- 原表统计：错误码、空输出或最终记录筛选下，同时到达的 count/stat 共享一条 COUNT/SUM 查询。COUNT 仍包含全部匹配类型，默认 SUM 只包含消费，显式类型按该类型统计。RPM/TPM 独立；共享查询失败回退各接口原读取路径。
- 共享只发生在查询进行期间：完成后立即移除。站点、角色、连接池、时间及全部有效条件隔离；最后一个等待者取消时停止查询。原有 5 秒接口缓存继续独立工作，不叠加新的过期时间。
- 分页游标继续使用优化前的筛选指纹，执行查询使用优化后的 SQL；已打开页面持有的旧游标仍能使用，站点、角色、用户、时间和筛选条件校验不变。

## 验证

- 原生 MySQL 8.0.46 独立测试实例，新旧 SQL 在三种排序规则下各比较 384 组筛选/权限组合，共 1,152 组全部一致：`utf8mb4_0900_ai_ci`、`utf8mb4_unicode_ci`、`utf8mb4_bin`。
- 对比首屏、OFFSET、两个方向的时间/ID 游标、COUNT、费用、固定时间窗口的 RPM/TPM；包含 NULL/零/负输出、空请求 ID、跨窗口最终记录、同时间不同 ID、时间与 ID 乱序、同请求不同用户、退款/登录类型、长文本和不同错误码格式。
- 新旧链路补查结果一致；并发测试验证一次扫描共享、单个/全部等待者取消、站点/权限/连接池隔离、统计错误互不连带、完成结果不额外缓存、链路详情失败降级。
- dashboard/auth/httpapi/mysqlstore 四个 Go 包测试及 vet 通过；使用日志相关前端测试 70 项通过。
- 本机 `CGO_ENABLED=0` 且未提供 C 编译器，未运行 Go race 检测。

## 二次准确性复查

发现并修复一个兼容性回归：旧游标的作用域哈希包含 SQL 文本及参数，三种筛选的等价 SQL 改写使旧游标返回 `400 invalid_cursor`。已先通过接口测试复现，再保留原有游标指纹格式；旧 SQL 仅用于生成指纹，不用于执行查询。上下页旧游标均通过，改变站点、角色、用户、时间、错误码或其它筛选后仍拒绝旧游标。

新增并通过以下检查，测试库为独立的本地 MySQL 8.0.46：

- 重新运行三种排序规则、1,152 组新旧查询对照，并增加每组游标指纹一致性检查。
- 遍历 100–599 全部有效错误码，每个错误码使用 13 组正文/other 样本，在三种排序规则下完成 19,500 组匹配对照。覆盖 NULL、大小写、数字边界、Unicode 空白、历史支持格式和跨列文本片段。
- 请求 ID 含大小写差异、重音字符、尾随空格、空字符串及 NUL，单条页面和混合页面共 318 组管理员/viewer 链路对照，结果与原实现一致。
- 实际调用列表接口遍历所有页并返回上一页，验证原查询的完整 ID 序列、用户范围及 COUNT，没有漏条、额外行或重复行。
- 真实 SUM 溢出时 COUNT 仍正确返回；修正测试数据后失败统计可以重新查询，未缓存失败结果。
- dashboard/auth/httpapi/mysqlstore 四个 Go 包测试和 vet 通过。

该次复查未发现上述样本上的结果差异，但没有覆盖下文的长空白正文资源上限场景。以上为本地测试数据结论，尚未在生产数据与源库版本上验证。

## 错误码查询 502 修复

用户截图显示同一时间窗口的普通查询成功，加上 `status_code=500` 后列表约 1 秒、统计/总数约 2–3 秒返回 502。截图没有给出数据库原始错误，不能仅据此确认生产根因。

本地 MySQL 8.0.46 默认 `regexp_time_limit=32` 下复现了初版优化引入的回归：100,000 个空格加 `unrelated-500`，合并正则报 `Error 3699: Timeout exceeded in regular expression match`；原六种分开匹配正常返回不匹配。该限制是正则引擎的匹配步数上限，独立于接口的整体超时。[MySQL 官方说明](https://dev.mysql.com/doc/refman/8.0/en/regexp.html#regexp-resource-control)

- 恢复六种分开匹配，并加入目标错误码的必要条件预检查。使用 CASE 固定求值顺序，避免依赖 AND 的优化器顺序。
- 不截断正文、不跳过匹配记录、不扩大接口超时，不调整源库参数。旧游标指纹保持一致。
- 新增长文本接口回归：百万空格、大量其它错误码、长空白中混入普通数字 500、正文/other 中真正的 500、跨列片段和 NULL；三种排序规则下列表、总数和统计均返回 200，记录及费用符合预期。
- 补充列表/统计/总数的错误阶段、MySQL 错误编号及 SQLSTATE 日志；不记录 DSN、查询参数或正文。生产若仍有 502，可用该日志区分正则限制、数据库其它错误和连接/查询超时。
- 修复后重新通过 19,500 组错误码匹配、1,152 组筛选组合、旧游标、完整翻页、统计共享/溢出/取消测试，以及 dashboard/auth/httpapi/mysqlstore 四个包测试和 vet。

同一 20,000 条样本重新测量，每项 3 次：错误码原两次统计扫描约 1,388 ms，修复后共享扫描约 198 ms；错误码＋空输出＋最终记录由约 2,741 ms 降至约 1,245 ms。仅为本机 SQL 对照，不包含网络或生产并发；不同轮次的绝对耗时不可直接对比。

本修复需要更新服务端后生效，尚未部署或在生产源库复验。

## 初版本地性能对照（历史数据）

> 后续 rc132 线上复验仍出现 3699；上节的“修复”仅覆盖了当时本地样本，不能视为该问题已完全解决。见文末二次修复记录。

20,000 条合成日志，每请求四次尝试，含多用户、错误及空输出，错误正文约 420 字符。以下为统计 SQL 的原来两次扫描与优化后一次扫描对照，每项 3 次；不包含页面首屏、网络、RPM/TPM 或生产并发。

| 条件 | 原统计 SQL | 优化统计 SQL |
|---|---:|---:|
| 错误码 | 1,399 ms | 476 ms |
| 空输出 | 18.3 ms | 9.6 ms |
| 最终记录 | 310 ms | 172 ms |
| 错误码＋空输出＋最终记录 | 1,590 ms | 523 ms |

上表对应已撤回的合并正则版本，不代表 502 修复后的错误码查询性能。性能数字仅适用于此本地样本；生产版本、索引统计信息、数据分布与文本长度仍需部署前后 EXPLAIN/耗时对照。共享扫描要求两个接口的实际计算时间重叠，不承诺每次请求都合并。

## 复现

在独立 MySQL 测试实例创建一个名称以 `ct_readonly_query_test_` 开头的空数据库，设置 `CT_READONLY_QUERY_TEST_DSN`。测试自动创建唯一的同前缀测试数据库，结束时只删除自己创建的数据库；勿使用源库连接。

```powershell
go test ./server/internal/dashboard -run 'TestReadonly(OptimizedQueries|ScopedFallback|RawSummary)' -count=1 -v
go test ./server/internal/dashboard -run 'TestReadonly(LegacyCursor|CursorStill|FallbackCollation|RawSummaryOverflow|AllStatusCodes|HandlerPagination)' -count=1 -v
go test ./server/internal/dashboard -run 'TestReadonly(StatusCodeLargeText|QueryFailureLogs)' -count=1 -v
go test ./server/internal/dashboard -run '^$' -bench '^BenchmarkReadonlyQueryStatsMySQL$' -benchtime=3x -count=1
```

本轮验证在本地完成，未打包或部署，未修改生产源库。

## rc132 线上 3699 二次修复（2026-09-23，本地完成，未发布）

- 用户确认运行 rc132，并提供 Server 日志：`logs/rows`、`count_stat/shared_query`、`stat/quota`、`count/query` 均出现 `category=mysql mysql_errno=3699 sqlstate=HY000`。rc132 标签确实包含 6b6a53828；此前根据“未部署”记录推断未安装修复不成立。截图中的 rc133 镜像拉取返回 not found，不能视为升级成功。
- 在隔离 MySQL 8.0.46、默认正则预算下，以一百万空格加 `unrelated-500` 复现 **rc132 分开匹配表达式**仍报 3699。此前十万空格及不含 500 的百万空格样本不足以覆盖此类输入。尚未取得具体触发线上错误的原始正文，本地样本用于复现同类失败。
- 正常 SQL 路径保留；仅明确收到 MySQL 3699 且查询带 status_code 时回退。权限、时间、其它筛选保留在 SQL 中，以包含目标数字作为必要条件筛出候选，在原只读事务内每批最多 64 条，按 created_at/id 游标推进；应用层使用线性时间 RE2 匹配完整字段，不截断、不设置候选数量上限、不跳过异常记录。失败前的部分列表丢弃，OFFSET、limit、has_more 和分页游标均按精确匹配结果计算。
- 列表、共享 count/stat、独立 count/quota 和最近一分钟 RPM/TPM 均接入回退。统计只读取自身需要的数值字段；大整数累加后检查 int64 溢出，总数不受费用溢出影响。沿用原请求期限和取消语义，未完成扫描不返回部分成功。无需修改源库表、索引或 regexp_time_limit。
- 按字段实际 collation 保留大小写规则；显式映射 ICU 的 Unicode 字母、数字和空白类，包含 long s 与 st 连字等匹配差异。编译模式缓存最多 32 个错误码，不新增查询结果缓存。与本地 MySQL 8.0.46 三种排序规则进行匹配对照；其它源库/Unicode 版本仍需线上复验。
- 已通过：rc132 真正失败数据的列表/总数/费用/速率均返回 200 且结果正确；跨 64 条批次的 OFFSET、上下方向游标；三种排序规则下 6,048 组大小写/Unicode 边界匹配；新旧 SQL 的 1,152 组权限/筛选对照，并对其中带错误码的组合额外验证回退列表、统计与固定窗口速率；取消、非 3699 不回退、费用溢出及独立总数；dashboard/auth/httpapi/mysqlstore 测试和 vet。
- 取舍：回退需要传输候选正文，宽窗口统计可能比正常 SQL 慢，仍可能遇到整体查询超时；它解决的是 MySQL 正则资源限制，不能保证所有查询恒定耗时。未部署、未在生产数据上复验、未发布版本标签；工作区已有前端主题/日志页面改动，本轮未修改它们。
- 收尾验证：100–599 全部有效错误码在三种排序规则下共 19,500 组样本，原 SQL、rc132 SQL 与应用层回退匹配结果一致；最终代码的 3699 接口回归、Unicode 对照、取消/溢出测试和四包 vet 再次通过。未运行 race 检测。
