# 运行监控与日志既有改动提交交付

## 电话设置纠偏提交（2026-09-15，已推送）

- 用户授权提交并推送本任务相关修改，工作目录 .tmp/runtime-log-delivery，分支 codex/runtime-log-delivery；范围含客户自动目录/号码多选、设置页统一样式、万单位、默认 20% 和比例判断开关及回归文档。主工作区其他改动保留。
- 本轮全量 Go 与 76 项前端回归通过；沿用同一代码此前通过的类型检查/构建，未重跑实库或真实浏览器。无生产部署、无新标签。代码提交 0d2ec80aa 已从 d8c9e28c7 快进推送 origin/main；下一步按用户安排发布新版本并验收真实目录/拨号。

## 比例判断可选（2026-09-15，代码完成、未发布）

- 用户要求：默认 20%，增加是否参与比例判断的配置；沿用隔离目录与本任务。范围为电话规则前后端、回归和说明；不改 Agent、冷却频控、不写远端。
- 验收：开关默认开启，关闭只判断绝对差值；旧配置缺开关仍开启且保留已有百分比；保存与加载保留显式关闭。已实现 use_percent 持久配置、表单勾选与输入禁用、动态条件说明，差值单位保持万。
- 验证：voicealert/mysqlstore/Server 主程序三个 Go 包、7 项前端表单回归、类型检查和生产构建通过；覆盖 20% 边界、绝对差值过滤、开关切换、旧配置兼容、API 保存 false。构建仅既有包体积提示，实库集成本轮未配置，真实浏览器与拨号未验收。
- 状态：本地 5192 使用此源码；未提交/发布/部署、未修改远程配置，无新迁移或 Agent 改动。下一步本地验收及后续统一发布。

## TPM 阈值使用万单位（2026-09-15，完成、本地预览）

- 用户要求“TPM 差值”改为万单位；仅修改前端输入/显示。10000000 Token 显示 1000 万，可写 computed 保存时乘以 10000 并取整数，原阈值与 API 单位不变；步长 100 万，上下界等价保留，清空/非法值不提交。
- 6 项电话设置回归与 vue-tsc 通过，覆盖回显/原值保存、小数换算、上下界往返和清空校验。5192 模块 HTTP 200、已热更新；本轮未运行生产构建/后端测试，未提交发布或远程写入。下一步用户本地验收，后续统一打包发布。

## 设置页视觉一致性与旧 Server 提示（2026-09-15，代码完成）

- 用户指出设置页脱离全站设计。核对 design.css/通知设置/日志归档后，移除自定义渐变卡头、状态圆点、输入框和表格覆盖，改用 panel/sub-panel、StatusTag、Element Plus 空态及 --ct-* token；分类回到 AppShell 顶部工具栏。保留紧凑双栏和对齐参数行，不改全站 CSS。
- 用户反馈客户多选为空：本地新版前端代理的远程 Server 尚未部署新 customers 字段。原先缺字段静默显示空集，现明确提示更新远程 Server，并阻止不兼容的电话配置保存；正常空集/目录故障另行处理。未读取认证远端响应、未将截图解释为站点无客户。
- 验证：5 项电话设置表单回归、类型检查/构建通过；独立 Edge + 模拟接口核验两种桌面宽度/页签/多选保存，无页面错误或横向溢出。无后端改动，未重跑后端测试、未远程写入；5192 热更新，未提交发布。下一步远程 Server 更新后验收真实目录。

## 设置页紧凑布局接续（2026-09-15，代码完成、本地预览）

- 用户反馈宽松堆叠、缺少质感。本轮范围仅 SettingsView、VoiceAlertsSettings 模板和样式，沿用本任务与 5192 远程代理预览；不改后端及保存规则。
- 电话预警改为规则/号码左右双栏，顶部保存与小型凭据状态，模板/阈值分组、说明按需展开、号码行和频控摘要紧凑显示。系统设置按独立双列排列，每个参数的标签/输入/默认来源同一行对齐；统一细边框、小圆角、轻阴影与字体层次。
- 验收：4 项电话设置表单回归、类型检查/生产构建通过；独立无头 Edge + 模拟 API 验证 1440px/1100px 两页签无横向溢出、无页面错误，客户多选和保存请求正确。本轮无后端改动，未重跑后端测试；未远程写入/拨号。
- 本地 5192 已热更新，截图位于主工作区 .tmp/voice-settings-selected.png、.tmp/system-settings.png。未提交/发布/部署，下一步用户登录预览进行样式验收。

## 本地前端连接远程（2026-09-15）

- 用户指定 API http://124.220.201.99:8080/；从本检出 webapp/packages/desktop 启动 Vite http://127.0.0.1:5192/settings，后台 PID 9196。CT_DEV_API_TARGET 仅进程环境，未改持久配置。
- 验证：本地页面 HTTP 200，代理 /api/auth/me 返回 401 unauthorized，需用户重新登录。日志在主工作区 .tmp/voice-preview-5192.log 与 .tmp/voice-preview-5192-error.log。
- 仅运行前端；新客户目录/配置保存依赖远程后端升级，本轮未部署 Server、未改远程配置、未拨号。

## 电话客户范围纠偏与设置页样式（2026-09-15，代码完成、未提交发布）

- 用户验收纠偏：删除手工添加客户；添加接听号码时从系统客户列表多选，默认全部，自动播报用户名。追加设置页面样式优化。
- 工作目录 E:/projects/controlTower/.tmp/runtime-log-delivery，分支 codex/runtime-log-delivery；任务 ID 01a0a28d-1aa5-7610-8038-91fa87da40df。原主工作区混合改动保留。本次沿用电话发布验收流程。
- 范围：voicealert、Server 客户目录接线、VoiceAlertsSettings、SettingsView、相关回归及文档。范围外：Agent、阈值逻辑、覆盖判断、冷却和频控、真实拨号。
- 验收与实现：按启用站点自动获取 NewAPI 客户身份目录，缓存 1 分钟；全部范围自动包含新增客户，指定范围按站点/用户隔离。兼容旧手工名单但不再以其限制检测；目录失败保留指定范围、故障站点暂停检测。
- 页面：电话预警/系统与监控两个页签；规则、号码、检测/拨号记录分区。指定范围为空时禁止保存；刷新记录保留未保存编辑；表单宽度、间距、按钮位置统一。
- 本轮验证：go test ./...、三个相关包 go vet、73 项前端回归、类型检查/生产构建通过；回环测试库 TestUserVoiceIntegration 实跑通过。新增目录缓存/发现/故障/隔离、后台订阅、无手工名单配置、接口与表单状态测试。
- 浏览器验收：独立无头 Edge + 本地模拟接口完成添加号码、默认全部、真实多选和保存请求校验、页签切换；1440px/1100px 无横向溢出、无页面错误。截图在主工作区 .tmp/voice-settings-selected.png 和 .tmp/system-settings.png；既有桌面最小宽度 1100px 保留。
- 状态：本轮未提交/推送/发布/部署，真实源站目录与真实拨号未验收；更新 Server/前端即可，无需再次升级 rc111 Agent、无新增迁移。下一步用户安排提交发布与生产验收。

## 生产部署上下文（用户截图，2026-09-15）

- CT 根目录 /opt/controlTower，Compose /opt/controlTower/deploy/compose；用户跳过数据库备份，保留配置备份。两台 linux-amd64 Agent 使用 /usr/local/bin/control-tower-agent、/etc/control-tower/agent.config 和独立 control-tower-log-reader 服务，保留已有配置和进度。
- 47.110.135.144 / 172.16.145.75 已由截图证实 rc111 预检、安装及两项服务 active；源日志/归档预检通过。ct-agent 加入 adm 后 Nginx timing 日志读取测试成功，10:12:39 新 rc111 进程启动。
- 172.16.50.56 已见 rc111 下载、预检通过，CT_LOG_COLLECT_ENABLED=false；未取得安装后状态证据。Server 实际镜像版本、后续日志持续正常及电话外呼仍未核实；未通过工具连接生产。

## 原交付记录

- 目标：按用户指定范围提交并推送四项既有改动：运行监控历史、运行数据默认保留期、日志归档默认月份、Nginx 错误级别过滤。
- 状态：代码 a4a66ff3 已提交并推送 origin/main；更新时间：2026-09-15。未部署。
- 工作目录：E:\projects\controlTower；隔离目录：.tmp/runtime-log-delivery；分支：codex/runtime-log-delivery；任务 ID：未取得。
- 范围：四项代码及相关测试、本记录、PROJECT_PROGRESS.md。共享 API 仅添加运行指标 offset 参数。
- 范围外：账单列表、电话预警、其他本地改动、发布打包及部署。
- 验收：基于最新 origin/main 整理准确差异，相关 Go/前端回归和类型检查构建通过，推送远端 main，保留原工作区改动。
- 初始化：已读取当前上下文和活跃任务，原工作区 main=87b788ff，存在大量混合改动；最新 origin/main=8d7e6506。Nginx 既有记录标注尚未提交，本次仅承担指定变更的交付。
- 实现：运行历史按实例及固定起止时间逐页读取，解决 6h/24h 仅返回最新 200 条；运行指标和 Docker 状态默认保留 1 天，数据库/环境显式设置仍优先；归档默认选择站点最新有记录月份，无记录回退当月，手动选择保留；Nginx 默认 error/crit/alert/emerg，显式条件避免命中旧低级别结果。
- 验证：隔离检出 9 个 Go 包测试通过（Agent containerlogs、internal containerlog/archivecontrol、Server 主程序/config/settings/dashboard/mysqlstore/httpapi）；14 项前端脚本回归通过；pnpm build 含类型检查通过，仅既有包体积提示；git diff --check 通过。
- 限制：未配置 CT_MYSQL_TEST_DSN，实库集成测试跳过；真实站点/浏览器未验收，未部署。无新增迁移；上线需同步 Server/前端及 Agent 或独立日志读取服务，已删除历史无法恢复。
- 交付：a4a66ff3 从 8d7e6506 快进推送成功；原工作区分支及其他未提交改动保留。
- 下一步：生产升级与真实环境验收由后续发布安排。

## rc110 发布接续（2026-09-15）

- 用户要求打包发布新版本，范围扩展为发布已验证主线，仍不部署生产。
- 目标标签 v2.0.0-rc110，目标提交 8c555abcda46715150673fde4bed0179d77bde87；相对 rc109 包含客户监控、渠道分组和本任务四项修复，电话预警未合并，不在本次包中。
- GitHub CI 34916307701 已 completed/success，包含 Go 测试/构建和前端类型检查/构建；本地相关回归证据见上文。
- 当前步骤：推送标签触发 release 工作流，再核验安装包、校验文件和镜像构建推送结果。实库及真实站点仍未验收。

## 电话预警纳入发布（2026-09-15）

- 用户追加授权合并电话预警到本次发布。rc110 标签已推送，release 34916465021 构建中且不含电话预警；不改写标签，整合后使用 rc111。
- 目标：将 b37040f8 电话预警提交整合到最新主线，解决 066 迁移编号、权限与设置接口兼容，完整验证并提交推送、发布新包。
- 范围扩展：电话预警前后端、Agent 秒桶、迁移、回归及发布记录；不接管功能分支其他账单提交，不部署生产或实际拨号。
- 当前步骤：隔离检出整合，优先检查主线权限和已退役通知开关。

- 整合完成：合并 Agent 客户渠道保护、归档控制接口和设置页三处冲突；电话接口映射 settings.manage；移除已退役通知总开关依赖，保留电话独立启用；迁移改为 077_user_tpm_voice.sql。
- 性能保护补修：客户秒桶加入后可能超过 10000 行；先舍弃可选渠道拆分，仍超限时舍弃电话秒桶并持久化无效覆盖标记，原指标保留、电话判断等待完整窗口。新增 1000/2000/3000 客户规模回归及实库无效覆盖拒绝验证。
- 本轮验证：go test ./...、go vet ./... 通过；69 项前端回归及 pnpm build（含类型检查）通过。找到本地专用测试库配置并仅连接回环地址测试库，TestUserVoiceIntegration 实际通过，覆盖 077 迁移、秒桶去重、并发冷却/频控、无效覆盖与缺口拒绝；真实阿里云拨号和浏览器人工验收未运行。
- rc110 release 34916465021 已成功完成；其内容不含电话预警。下一步提交电话整合并发布 rc111，取代 rc110 作为本次最终交付版本。
- 2358c9e7 已快进推送 origin/main；v2.0.0-rc111 标签指向该提交。CI 34916930241 和 release 34916965488 构建中，下一步确认工作流成功及三份安装包、SHA256SUMS 和镜像。

## 最终发布结果（2026-09-15）

- 状态：电话预警已整合提交推送，rc111 已发布，未部署。
- v2.0.0-rc111 = 2358c9e744ecd7a51c6e23be9aaad167c258ea7a；GitHub CI 34916930241 和 release 34916965488 均 completed/success。
- 发布页：https://github.com/yanceylv518/controlTower/releases/tag/v2.0.0-rc111 ，非 draft；Server linux-amd64、Agent linux-amd64/arm64 三个 tar.gz 和 SHA256SUMS 均 uploaded。
- 镜像：ghcr.io/yanceylv518/controltower-server:v2.0.0-rc111；工作流镜像构建推送成功。rc110 已成功发布但不含电话预警，本次最终版本使用 rc111。
- 升级：先更新 Server/前端并执行 077，再更新日志采集 Agent/独立日志读取服务。电话默认关闭，需阿里云凭据、包含 customer/direction 的审核模板及值班号码；本次未拨打真实电话。
- 原主工作区的混合改动保留；整合代码仅位于隔离交付检出和远端。下一步为实际部署、页面验收和用户安排的真实电话联调。
