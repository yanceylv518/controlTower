# 测试跟进指定模型

2026-09-24，main；本地实现，未提交、发布或部署。

## 提交复核（2026-09-24）

- 用户要求再次检查，无问题则提交推送。本轮检查模型精确匹配、首次/静默状态、暂停及新轮次、历史JSON兼容、派发前复核和前端保存路径，未发现阻塞提交的问题。
- Go全量vet/test通过；419项前端回归、类型检查和生产构建通过（既有大chunk提示）。
- 新建隔离MySQL8.0.46（仅127.0.0.1:33439，ct_trial_model_test）执行完整迁移及TestTrialFollowupIntegration通过。覆盖非指定模型零事件/零通知、账户/Key重叠与并发去重、模型修改规范化并开启新轮次、旧轮次待发拦截、新模型首次触发及重放、旧JSON缺model时同轮次待发也跳过；电话/消息均测试替身，无真实外呼或群发送。最初mysql客户端参数被PowerShell解析错误，改为完整带引号参数后成功建库，未计失败尝试为通过。
- 前次“实库跳过”是历史阶段，本轮已补验证。未做最终浏览器布局实测或生产验收。
- fetch后HEAD与origin/main均为71ab6834b，无分叉；按本任务范围暂存，排除原有Agent故障复核文档及对应两条进度。提交推送结果以Git记录为准，不创建发布标签或部署；Server/Web需配套升级，旧对象需填写模型。

## 行为

- 每个测试对象新增单个 `model`，填写日志中的完整模型名称，去除配置首尾空白、区分大小写精确匹配，最多200字。启用检测必须填写，暂停/草稿允许为空。
- 账户（全部 Key）或指定 Key 的身份条件与模型条件同时满足才观察；其他模型不会更新该对象的 LastID、LastAt 或 Fired，不消耗首次提醒、不推迟静默后提醒。成功/失败规则沿用现有配置。
- 修改模型自动开启新一轮并重设观察边界；保留历史事件，已排队的旧轮次通知跳过。派发前同时复核当前模型，未配置模型的旧对象不再发送提醒。
- 名单、配置表单和通知预览展示模型。旧对象显示待配置及补充提示，需填写模型并保存开启检测。
- 复用 trial_watches.config_json 和已有源日志 model_name，无新增迁移，无需更新 Agent；需配套更新 Server/Web。电话模板变量保持原样，群消息及事件详情已有实际模型。

## 验证与限制

- voicealert/mysqlstore/dashboard/httpapi 四包 Go test 和 vet 通过。新增模型精确匹配、大小写/相似名/空模型、账户与Key、静默计时、旧配置与输入校验回归。
- 7项测试跟进前端回归、类型检查和生产构建通过；有既有大chunk提示。
- MySQL集成用例增加非匹配模型零事件/零发送、模型变更清空状态并开启新轮次；本轮未配置 CT_MYSQL_TEST_DSN，明确跳过，未记为实库通过。
- 未执行浏览器、生产或真实外呼验收。检测仍基于调用日志落库，不代表请求发起瞬间检测。
- 保留会话开始时 PROJECT_PROGRESS.md 和 Agent故障复核记录已有修改。启动核对发现早期 README/development-progress 的阶段描述落后于现行代码与rc136记录，本任务未重写历史看板。

## 本地前端连接远程后端

- 按用户要求启动 Vite：http://127.0.0.1:5200/，进程环境 CT_DEV_API_TARGET=http://124.220.201.99:8080，strictPort；会话95947。
- 首页200，代理 /api/auth/me 返回401，待用户登录。未写入远程配置或部署后端。
- 当前模型字段及过滤逻辑仅在本地代码；远程Server需升级本次后端后才能保存模型并按模型检测，本地前端启动不代表该后端功能已上线。

## 表单间距修正

- 用户截图反馈测试模型说明与客户名称之间留白过大。定位到通用sub-note的12px下边距叠加表单项间距，说明还继承表单内容行高。
- 仅在测试跟进编辑器内将字段说明改为上间距4px、下间距0、行高1.5，表单项下间距16px。说明占完整一行，保留自然换行。
- 首次Vite组件编译响应200；主模块不内嵌CSS，不能据此认定样式内容已回读。后续单独请求style模块验证。diff检查通过，未浏览器实测。

### 整体紧凑与同一行标签

- 用户继续明确整个区域过疏松、不要字段标签随意独占一行。最终采用左侧112px标签、右侧输入的单列紧凑表单，字段间距10px；窄屏标签100px，内容自然换行。替代过程中尝试的顶部标签/双列表单。
- 站点、Key、模型的说明放入字段标签悬停提示，取消输入框下方重复说明行；通知预览标题和段落间距同步收紧。
- 类型检查、7项前端回归、Vite组件及独立样式模块HTTP200、diff检查通过。本地热更新，未浏览器实测或部署。

### 平衡表单密度

- 用户截图指出左侧压缩过度。本次保留同一行标签和输入，将桌面字段间距从10px调为16px、面板内边距20px、输入/选择控件36px；标签宽136px，通知人员的“可多选”移到占位提示，避免标签挤成两行。左右面板比例调整为1.3:1。
- 手机保持14px面板内边距、12px字段间距。说明仍为悬停提示，不恢复逐字段的额外说明行。
- 类型检查、Vite样式模块200及16px间距/36px控件/20px内边距回读、diff检查通过。未浏览器实测，未部署。
