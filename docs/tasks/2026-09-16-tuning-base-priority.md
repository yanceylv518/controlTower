# 调权基础优先级异常排查

- 目标：定位基础优先级保存后多加 1，以及偶发归零但无熔断状态的原因。
- 状态：Server 只读来源接管和站点级清理修复已完成、本地验证通过；更新时间：2026-09-16。未提交发布部署，线上原始触发仍待证据；下方排查条目为实施前结论。
- 任务 ID：01a0a7f3-3a0c-7531-878e-4ec2d329702c。
- 工作目录：E:/projects/controlTower；分支：main。
- 范围：基础值编辑/保存、渠道快照同步和调权状态生命周期；本记录、PROJECT_PROGRESS.md、.tmp/priority-diagnosis 下的本地验证产物。暂不修改业务实现。
- 范围外：其他功能、生产配置写入、提交、发布和部署。
- 验收：给出可复现触发条件和代码证据，区分本地确认问题与线上尚未证实的触发源。
- 协作：工作区包含其他任务改动；沿用只读分析，保留全部现有代码。
- 已确认：常规自动调权只下发权重；熔断降线上优先级至 0，恢复写基础优先级。上一轮 directcontrol/tuning/dashboard 定向回归通过，未做生产验证。
- 已确认：当前 Element Plus 连续点击指令按下立即加 1，约 700ms 开始重复；定时器模拟可复现 11→12→13，尚无用户操作证据，不能认定根因。
- 已确认缺陷：server/internal/mysqlstore/store.go 的 SyncChannelSnapshotsAt 按单个 instance 的渠道列表删除 site 级基础值与状态，未考虑同站点其他实例仍有该渠道；空列表和非空缺渠道列表两条分支均受影响。当前 origin/main 同样包含该逻辑。
- 实库复现：本地独立 MySQL 8.0.46、独立测试库，Go overlay 注入 .tmp/priority-diagnosis/priority_diagnosis_test.go，调用实际 StoreInstanceChannels/SyncChannelSnapshotsAt，执行真实迁移及 SQL。两个 Agent 同属一站点，手工基础优先级设为 12、状态设为 circuit；B 仍持有渠道，A 上报缺少该渠道的完整列表，导致共享基础值/状态均被删除。B 再刷新后，基础值取其线上优先级，状态仍缺失。
- 验证结果：zero_empty、zero_nonempty 两场景重建基础优先级 0 且状态数 0；thirteen_empty 重建基础优先级 13 且状态数 0，三项诊断全部复现。不是完整业务回归，也不是生产验证；测试文件只在 .tmp 中，未新增业务测试/修改实现。临时 MySQL 已执行 shutdown。
- 另一条静态路径：模型名称变化会重置基础值并删除旧模型状态；零/多模型先删除基础值与状态，再变回单模型时从线上值重建。本轮未实库验证这些场景。
- 限制：尚未获取异常渠道 ID、发生时间、当时两个 Agent 的渠道列表和线上值，不能认定用户线上必定触发该缺陷。13 场景前提是重建时线上已经为 13，并非自动计算 +1。
- 下一步：修复快照清理的站点范围，避免单个采集器缺失删除其他采集器仍持有的共享基础值/状态；补充两类列表及最后采集器确实删除的回归。线上同步核查基础值 updated_by/updated_at、tuning.base_update 审计和各采集器 channel_current；现有被覆盖数值不可凭空恢复。

## Server 来源接管实施（2026-09-16）
- 用户明确 Agent 不改；仅 Server 根据只读配置选择渠道来源。
- 实施目录：E:/projects/controlTower/.tmp/readonly-channel-source；分支 codex/readonly-channel-source，基于 origin/main 1e5761b25。
- 范围：Server 只读渠道读取/后台同步、Agent 快照接收过滤、手动刷新与站点级清理、相关回归和文档。
- 验收：配置只读时拒收 Agent 渠道快照，其他上报正常；无配置沿用旧路径；读取失败不清理；基础值与熔断状态保留；多采集器缺失不误删；Agent/前端不改。
- 状态：代码完成并通过本地验证；未提交发布部署。
- 实现：directcontrol 新增只读 channels 查询、一分钟后台同步及手动刷新优先选择；mysqlstore 在事务内核对配置，过滤完整/单条 Agent 快照，并按站点保护共享基础值/状态。Server 完整列表在同一事务内统一实例缓存，保留采集开始后已确认的写入；保留各实例渠道名称/查询。ingest 仅携带渠道数据时检查来源，其他上报路径不变。
- 故障保护：只读配置存在即不回退 Agent；连接/查询失败、取消、超过 5000 渠道不发布新列表；查询期间配置变化拒绝旧结果。成功的真实空表仍按完整删除处理。模型变更/多模型资格继续沿用原规则；已有被覆盖的基础值无法自动恢复。
- 验证：隔离 MySQL 8.0.46 两个测试库，实际只读查询、配置选源/错误不回退、后台同步、5001 行超限/取消测试通过；双 Agent 空/非空缺渠道、基础优先级12与熔断状态保留、旧配置拒绝、完整及单条 Agent 忽略、新写入保护、各实例名称及最终真实删除均通过；既有渠道同步、HTTP 刷新/重试/写入专项实库回归通过。临时 MySQL 已关闭。
- 验证：全项目 go test ./...、go vet ./... 通过（无 DSN 的环境依赖集成按原约定跳过；专项实库已单独执行）；最后减少无渠道上报的配置查询后 ingest test/vet 再通过，diff --check 通过。
- 交付范围：仅 Server、三个新增回归文件及接口/任务/进度文档；Agent、前端和迁移均无改动。未运行浏览器/生产验收，未提交发布部署；只需更新 Server，无需调整 Agent 配置或版本。代码在上述隔离目录，主工作区业务改动未碰。
- 下一步：按用户安排提交发布；上线后确认只读账号具备 channels 表 SELECT 权限，核对异常渠道基础值并观察新同步日志。此前线上具体触发仍未取得证据，不承诺自动修复已丢失的历史值。

## TTFT 不足复用本轮 OTPS（2026-09-17）
- 用户授权实现：TTFT 或其基线不足时复用本轮平均输出速度系数；窗口样本不足不调整，熔断优先。用户已确认缓存保留原逻辑。
- 状态：代码完成、本地验证通过；更新时间 2026-09-17；目录 E:/projects/controlTower/.tmp/tuning-otps-fallback，分支 codex/tuning-otps-fallback；未提交发布部署。
- 依赖：从 .tmp/tuning-direct-ttft 复制已完成未提交的平均输出速度实现作为测试基线，不修改原任务目录；该依赖包含 Agent/081迁移，本次系数替代本身仅 Server/调权页。此前只读渠道来源仍独立保留。
- 文件范围：continuous_engine.go、调权页说明/状态、针对性回归和本任务文档；范围外：修改平均输出统计口径、其他功能、发布部署。
- 验收：使用本轮 OTPS 而非历史 TTFT，平方结果/有效 TTFT切回/窗口不足停调/熔断优先及页面来源提示可验证。

- 实现：每轮重新计算速度系数，不沿用历史 TTFT。TTFT 有效时 Ks×Ko×Kcache×Kerror；TTFT 样本或同模型基线不足、输出有效时 Ko²×Kcache×Kerror。缓存公式、各输出系数边界及最终组合/单次调幅保护保持原逻辑；替代值不再套 TTFT 边界。
- 执行保护：总窗口样本不足，或 TTFT 与当前输出均不可用时保持有效当前权重、不发正常调权；熔断/探测/恢复优先。修正只有输出基线有效时总乘数被强制归一、使输出系数无法实际下发的问题。全局无指标沿用既有空窗口保护，不进行新计算。
- 页面：明确显示“速度（输出替代）”、TTFT 样本/基线不足使用本轮输出、窗口不足本轮不调权；移除沿用历史速度的说明。
- 验证：新增三组回归在原依赖引擎上均失败、当前引擎通过，覆盖旧系数不复用、OTPS 1.2/0.8 对基础100实际下发144/64、TTFT恢复切回、缓存继续相乘、无输出保持权重及窗口不足时熔断仍执行。原重试/下发测试补足当前中性样本，保留原断言。
- 本轮验证：go test ./...、go vet ./...、调权包独立回归、91项前端测试（调权13项）、pnpm typecheck、Vite生产构建及 git diff --check 通过。构建有既有大包提示；本轮未配置 MySQL DSN，环境依赖集成按约定跳过，未运行浏览器/生产验收。
- 交付限制与下一步：改动在隔离目录，主工作区及其他任务业务代码未改。需与所复制的平均输出速度依赖一并交付（Server/前端及081迁移后升级Agent）；本次替代规则本身未新增Agent逻辑/迁移。只读渠道来源仍在另一隔离目录，未合并。等待用户安排提交/发布，不能视为线上已生效。

## 调权中心界面统一（2026-09-17）
- 用户授权：按已确认整套设计实施，保留系统外框、内容区模型列表及直接编辑；容量入表、计算依据浮层、记录/规则/弹窗统一。
- 状态：代码完成、本地验证通过；更新时间 2026-09-17；目录 .tmp/tuning-otps-fallback，分支 codex/tuning-otps-fallback；接续原调权功能，保留现有OTPS实现，未提交发布部署。
- 范围：ContinuousTuningView.vue及相关前端回归、任务记录；范围外：系统导航重设计、后端算法、提交发布部署。
- 验收：基础值和容量无需额外编辑入口、模型常驻、计算浮层不撑行、参数无丢失、事件信息不虚构、全部子页统一；类型检查/回归/构建及本地视觉核验。
- 已完成：运行概览七组列整合渠道分组、当前/拟执行权重、基础权重、线上/基础优先级、RPM/TPM当前及可编辑上限、状态；移除重复容量和原始指标列表。基础值/容量直接输入、Tab连续编辑，数值修改标记，统一保存栏跨页可用，切换模型不再静默丢弃草稿。
- 浮层：新增 TuningInfo.vue，悬停/聚焦显示、点击固定、允许移入选择复制、外部点击/焦点及Esc关闭；展示本轮因子、样本、容量约束和完整依据。编辑草稿时公式仍引用评估基础值并提示未保存，不假装旧评估已对应新值。
- 子页：变更记录合并模型渠道、展示权重/优先级前后值、真实status与详情浮层；新增本地日期筛选并明确仅针对最近加载最多300条。recorded显示已记录，不冒充执行成功。规则35个现有参数全部保留，按基础评估/性能矩阵/错误曲线/熔断恢复分组，值域和默认值保持；说明分组、分组编辑预览及反馈统一。系统AppShell/全局样式未改。
- 验证：95项前端回归通过；新增4项覆盖切模型草稿保留/公式基准、限升缺数据与0区分、状态优先级及渠道筛选、记录状态与日期边界；最终调权17项通过。vue-tsc和Vite生产构建通过，保留既有大包提示；所有原参数绑定静态比对无遗漏，git diff --check通过。本轮后端/SQL未改，未重跑Go/MySQL。
- 浏览器：本地纯模拟API预览，1264/1920宽运行概览及1440宽性能矩阵检查；直接键盘编辑40→43、Tab、模型切换保留及模拟保存成功、计算浮层固定/Esc、记录/规则/分组弹窗/说明核验完成，无生产写入。窄桌面保留渠道固定列与表格横向滚动；未作生产数据验收。临时预览源归档到 .tmp/verification/ui-preview，不进入产品构建。
- 交付：仅本轮前端页面、TuningInfo组件和前端回归/记录；同目录既有平均OTPS及fallback依赖保留，其他任务目录未改。未提交发布部署，线上页面尚未生效；后续按用户安排与既有依赖一并集成交付。

## 本地连接远程预览（2026-09-17）
- 用户授权启动本地前端连接远程；沿用已指定 http://124.220.201.99:8080/。从 .tmp/tuning-otps-fallback/webapp/packages/desktop 启动 Vite 5198，执行会话98639，CT_DEV_API_TARGET仅设进程环境，不改持久配置。
- 访问：http://127.0.0.1:5198/tuning；页面HTTP200，代理/api/auth/me返回401，说明服务可达但需在本地登录。已请求Codex打开页面，工具返回queued。端口PID查询被系统拒绝，未记录未经确认的PID。
- 本轮未改业务代码、未重跑构建测试；未提交发布部署、未主动写入远程配置。本地新页面使用远程现有后端，新OTPS算法及迁移仍需配套部署才能生效。下一步用户本地登录验收。

## 设计还原纠偏（2026-09-17）
- 用户指出真实页面偏离设计；状态实施中，沿用原目录与前端范围。重点修正字号/控件尺寸、容量列对齐与长数值、常规拟执行颜色和模型导航比例；不改全局外框。容量显示万/亿，聚焦编辑精确原值，保存仍为Token整数。

- 最新方向：用户明确采用“最初未重设计的原页面”为视觉基准，之前放大文字/加宽留白方案被否定。已撤除该轮覆盖样式，恢复原白色页签、300px模型区、描边标签和标准控件；保留容量入表、直接编辑、计算浮层、状态图标和子页功能。用户随后要求收小外边距，调权中心专用content padding改为8px，不改共享AppShell文件。
- 实际核验：模拟浏览器1920×920概览行高63px，8条渠道完整可见；规则/变更记录查看通过。DOM测量顶部52→60、左侧188→196，外间距均8px。容量2000万聚焦为20000000，未修改退出不失精度；2000.01万编辑解析为20000100，模拟API未生产写入。
- 验证：98项前端回归通过，vue-tsc/Vite构建通过（保留已有大包提示）。临时模拟入口重新归档移除；本地5198继续运行并热更新，未提交/部署，待用户继续视觉验收。本轮仅前端与记录，不改后端算法。

## 首列渠道信息调整（2026-09-17）
- 状态：实施中；沿用当前分支与目录，仅修改调权页面。按用户要求首列显示渠道名、ID、当前TPM/RPM；分组独立展示，上限保留直接编辑，不改计算或接口。验收：字段无重复、无丢失，原紧凑风格与8px外边距保持。

- 完成：首列两行显示渠道名及ID/当前TPM/RPM，分组独立成列；RPM/TPM上限继续直接编辑。按追加要求移除渠道搜索/状态筛选/负载时间整行，表格直接接模型标题；负载时间与统计区间移入标题信息浮层，表格直接使用当前模型全部渠道。
- 验证：本轮17项调权回归、vue-tsc/Vite构建、diff检查通过；本轮未重跑浏览器视觉验收。5198已热更新，未提交部署。

## 渠道容量内联浮层（2026-09-17）
- 实施中：用户确认首行渠道名与ID，次行当前TPM/RPM及可选上限，取消独立容量列；点击数值打开编辑浮层，支持万/亿和清除。沿用页面统一保存草稿，浮层说明生效方式；不改接口或计算。

- 已完成并本地热更新：首行按最新要求为“#ID · 渠道名”，12px同字号/20px同行高、baseline对齐；次行TPM/RPM，只有设置上限时出现斜杠和上限，未设置不显示不限。第二列所有分组完整换行，不再+N省略。删除独立上限两列。
- 新增TuningCapacityMetric点击浮层：显示精确整数、万/亿解析、确认/清除、Esc及外部关闭；沿用页面统一保存，浮层明确提示确认草稿后保存更改生效，避免无意提交其他未保存字段。
- 验证：20项容量/调权专项回归通过，vue-tsc/Vite构建通过；末次仅CSS对齐调整通过浏览器DOM核验（ID与名称top均159、高均20、字号均12px），未为该纯CSS调整重复构建。模拟浏览器验证2000.01万→20000100，清除后仅当前TPM、无斜杠；全分组wrap样式核实。未生产写入、未提交部署。临时模拟入口归档移除，5198继续运行。

## 模型导航收缩（2026-09-17）
- 实施中：模型导航300px缩至220px，提供收起/展开；状态改圆点及悬停说明，保留模型选择与当前草稿。范围仅调权前端，不改全局导航和接口。

- 最终完成：模型区220px，收起36px，可展开。用户否定状态圆点后改为名称下方同一行左侧渠道数、右侧“自动/观察/关闭”文字，无边框标签，不占名称宽度。
- 验证：17项调权回归及最终类型检查/构建、diff检查通过；模拟浏览器核验展开220px/收起36px及当前模型不变；最终文字布局静态核验，未重复浏览器。5198热更新，未提交部署，未生产写入。

- 箭头样式修复（2026-09-17）：根据用户截图将文字‹/›替换为Element Plus ArrowLeft/ArrowRight图标；按钮32px高与搜索框一致，padding0、12px图标居中，展开收起逻辑不变。类型检查/构建通过；本轮未重跑浏览器和回归，未提交部署。5198热更新。

- 首列宽度纠偏（2026-09-17，实施中）：用户指出分组距左过远；首列由最小360px且自动扩展改为固定320px，空余宽度留给分组，防止大屏首列产生大段空白。范围仅表格列宽。
- 已完成：固定320px首列，本地5198热更新；类型检查/构建及diff检查通过，本轮未重跑浏览器/回归，未提交部署。

- 分组列收紧（2026-09-17）：按用户要求由min-width240自动扩展改为固定220px，限制分组按钮不超出单元格，保留全部分组及自动换行。实施范围仅列宽/CSS。
- 完成并5198热更新；类型检查/Vite构建通过，本轮未运行浏览器及回归，未提交部署。

- 恢复评估系数列（2026-09-17，实施中）：分组之后展示速度/缓存/输出/错误四项系数，紧凑两行；使用现有评估状态，无状态显示等待评估，详细计算沿用浮层。
- 已完成：分组后新增210px评估系数列，速度/缓存、输出/错误两行显示三位小数，系数大于1绿色、小于1红色；无评估状态显示等待评估。计算详情仍在本轮计算浮层，未改算法。17项调权回归、类型检查/构建通过；本轮浏览器未复验。5198热更新，未提交部署。

## 隐藏基础优先级（2026-09-17）
- 实施中：按用户“先不显示”要求移除概览基础优先级输入，只读显示线上优先级；本轮不删除存储字段、不改熔断算法。用户提出后续熔断仅清零权重，需要先核实上游零权重选路语义后接续。

- 优先级已完成：概览只读显示current_priority，按用户追加要求表头简写“优先级”。基础优先级输入隐藏，字段及当前熔断仍保留，本轮未改后端。

## 权重三项分组（2026-09-17）
- 已完成：按用户追问落实双层表头“权重 / 基础、计算、线上”，基础直接编辑，计算旁信息浮层，线上适度加粗；移除之前当前/拟执行上下堆叠。
- 计算目标使用评估基础值与四系数相乘，按已保存规则综合倍率上下界限幅、取整；不采用已受限升修改的state.multiplier/proposed_weight。浮层明确前端推算口径，保留安全限制后拟执行与实际线上。样本不足、关闭、非normal/暂停、多模型、无有效系数时显示—，不把历史系数伪作本轮目标。限制：后端未提供评估时倍率边界快照，当前已保存规则与评估可能有短暂时差。
- 验证：18项调权回归、类型检查/Vite构建及diff检查通过；新增覆盖公式目标不等于受限执行、草稿基础不污染、倍率上界、缺样本及熔断。本轮浏览器未复验。5198热更新，未提交部署。

## 优先级直接编辑（2026-09-17）
- 实施中：单一优先级输入默认显示线上值，编辑保存复用base-values触发base_priority_sync；内部同步基础恢复值但不额外展示。当前后端跳过circuit/probing写入，对应渠道禁用编辑说明原因；后端熔断改造仍未实施。
- 完成：优先级列改输入框，默认current_priority，修改使用按channel_id共享草稿并同步内部base_priority，取消恢复线上展示；保存/重新加载清草稿。熔断/探测锁定，复用既有优先级同步接口，不伪改线上快照。19项调权回归及类型检查/构建通过，新增覆盖线上/基础差异、跨模型同渠道草稿、取消与熔断锁定。本轮浏览器/真实写入未运行，5198热更新，未提交部署。

## 样本不足速度展示纠错（2026-09-17）
- 实施中：恢复系数列时直接展示k_speed未检查本轮有效性，可能误呈历史值。新增有效性判断，窗口不足/未参与速度显示—；有效OTPS替代需新口径并明确标记。同步计算浮层速度展示，不改远程算法。
- 已完成：速度展示新增本轮样本/口径/阶段/参与条件检查，缺样本显示—而非历史k_speed；输出替代须OTPS新口径可用且速度系数与本轮输出一致，明确标注输出替代。计算目标及浮层同步采用有效速度。20项调权回归和类型检查/构建通过，新增覆盖历史值/缺样本/无效及有效替代；本轮浏览器未运行。5198已热更新，后端未改、未提交部署。

## 评估系数分组列（2026-09-17）
- 实施中：按用户要求把四系数拆成评估系数分组下四个子列，每格系数和状态同处；本轮只调整展示，不实施前述计算权重逻辑放宽。状态依据样本/基线/版本/参与信息区分有效、替代、回退及保留值。
- 已完成：评估系数采用双层表头，速度/缓存/输出/错误各86px，数值上方、状态下方，悬停显示原因；区分未参与、窗口不足保留值、TTFT/输出替代、缓存中性回退、旧输出口径和平滑错误率。21项调权回归、类型检查/构建和diff通过，本轮浏览器未运行。计算权重逻辑未改，5198热更新，未提交部署。

- 系数状态减噪（2026-09-17）：按用户要求缓存/输出/错误仅显示数值，状态保留悬停说明；速度继续显示状态。缺版本标记文案修正为口径待确认，避免断言旧算法。
- 已完成并5198热更新；类型检查/构建通过，本轮未重复回归/浏览器，未提交部署。

## 区分窗口与速度样本（2026-09-17）
- 实施中：总窗口不足在状态列显示请求数/门槛，速度仅显示—；达标后区分TTFT样本不足、基线不足与输出替代，保留熔断/写失败等优先级。
- 完成：窗口不足优先显示“窗口样本不足n/门槛”，速度仅—；窗口达标才显示TTFT样本不足或基线不足。按追加要求把总状态列移入评估系数分组作为第五子列（145px），保留限升/熔断等提示，删除最右独立状态列。22项调权回归及最终类型检查/构建、diff通过；本轮浏览器未运行。5198热更新，未提交部署。

## 整体状态共行（2026-09-17）
- 实施中：移除状态第五子列，保留四系数表头，表体系数下共行显示整体异常；仅速度专属问题留速度下。限升入口移至计算权重，用户要求去掉感叹号。
- 完成：四系数表头保持，表体合并四格以承载四等宽数值及共用异常行；速度专属说明不与整体状态重复。限升移至计算权重旁，移除熔断/写失败感叹号及页面警告图标。23项调权回归及类型检查/构建通过；模拟浏览器确认colspan4宽344px、表头数值对齐，整体状态为窗口样本不足/熔断纯文字。无生产写入，5198热更新，未提交部署。

- 窗口不足占位简化（2026-09-17）：按用户截图要求去掉窗口样本不足时速度位置的—，只保留共用状态文字；其他系数及有效速度显示不改。
- 已完成，类型检查/构建通过；仅展示条件修改，本轮未重复回归/浏览器。5198热更新，未提交部署。

- 状态最终简化（2026-09-17）：用户明确窗口不足时整组只显示一行“窗口样本不足”，四系数全部隐藏，数目移入悬停；正常速度隐藏TTFT有效字样，异常/替代说明保留。最终类型检查/构建通过，静态核对模板；本轮回归/浏览器未重跑。5198热更新，未提交部署。

- 评估空态居中（2026-09-17，实施中）：窗口不足居中；未参与/关闭/安全暂停隐藏系数并居中未参与调权，具体原因悬停保留；熔断/写入失败等专门状态不合并掩盖。
- 已完成：窗口样本不足、未参与调权均隐藏四系数并居中单行。追加修复TTFT自身样本/基线不足的速度格：有状态无系数时移除—，仅保留原因文字。24项调权回归通过，最终类型检查/构建通过；最后展示条件修改未重复回归，浏览器本轮未运行。5198热更新，未提交部署。

- 计算列提示图标移除（2026-09-17，实施中）：按用户截图去掉所有圆圈i入口，改由计算数值/—本身触发浮层，保留键盘与固定查看，不影响其他信息入口。
- 完成：TuningInfo支持纯文字触发，计算列移除圆圈提示，数值和—触发原浮层，使用说明同步。类型检查/构建通过，本轮未重跑浏览器/回归；5198热更新，未提交部署。

## 计算列参考值门槛（2026-09-17）
- 实施中：按用户最新明确要求只因窗口不足或基础权重0显示—；计算列基于最近返回系数作参考值，解除阶段/模式/TTFT有效性门槛，执行逻辑不改。浮层说明最近系数可能保留，与本轮执行资格区分。
- 已完成：计算列取消off/phase/paused/速度口径与ready门槛；窗口请求数不足（无状态按0）、当前或评估基础0仍为—。使用最近返回四系数及已保存倍率边界，浮层明确保留系数参考值不代表本轮执行。24项调权回归和类型检查/构建通过，覆盖关闭/熔断/暂停/TTFT不可用仍有参考值，窗口不足/基础0无值；本轮浏览器未运行。仅前端，后端执行未改，5198热更新，未提交部署。

- 表格对齐统一（2026-09-17，实施中）：按用户要求仅渠道列左对齐，其余表头、分组标签、数值控件和整体状态全部居中；只调整本页表格样式。
- 已完成：渠道列显式左对齐，其他列表头与内容居中，分组标签和计算浮层触发数字、整体状态同步居中；按追加要求线上权重改span及400普通字重。对齐变更类型检查/构建通过，最后字重/语义标签调整静态核对、未重复构建；本轮浏览器/回归未运行。5198热更新，未提交部署。

- 表格右侧留白（2026-09-17，实施中）：全部列fixed width导致总宽小于容器；仅系数、权重子列和优先级改min-width，由Element表格分配剩余空间，渠道320/分组220保持，避免重现首列过宽。
- 完成：数值列弹性填满可用表宽，窄屏仍保留最小列宽和横向滚动。类型检查/构建通过；本轮未重跑浏览器/回归。5198热更新，未提交部署。

- 列宽比例调整（2026-09-17，实施中）：评估系数四子列固定80px，分组最小宽度260px并参与剩余空间分配；保留自动换行和整表铺满。仅调整本页布局。
- 已完成：系数合计320px，分组由固定220px改为最小260px并弹性扩展；类型检查与构建通过。本轮未做浏览器验证，未提交部署。

- 自适应布局（2026-09-17，实施中）：按页面内容宽度切换紧凑列宽，窄屏模型列表收窄，表格约920px下限、内部滚动；仅调权页，不更改全局布局及业务计算。验收：宽屏保留现状，半屏工具栏换行且表格可访问所有列。
- 已完成：ResizeObserver观察页面内容宽度，低于1200px切换紧凑列宽（合计最小920px）、170px模型列表；手动收起仍保持36px。工具栏换行，输入框64px，表格保留内部横向滚动。类型检查/构建及24项调权回归通过；浏览器半屏视觉验收未运行，需用户本地确认。未提交部署。

- 自适应反馈修复（2026-09-17，实施中）：发现全局body最小1100px导致整页溢出；仅本页解除，内容区不足1200px自动折叠模型栏，并保留窄屏临时展开与宽屏原折叠偏好。
- 已完成：小于1200px自动折叠，窄屏可手动展开且同一区间内resize/刷新不会重复收起，宽屏恢复手动偏好；仅body含本页时解除1100px最小宽度，约束页面grid及导航min-width以免整页溢出。类型检查/构建与24项调权回归通过。尝试浏览器验证失败（CUA fetch连接失败），视觉待确认；未提交部署。

- 窄屏渠道列继续收窄（2026-09-17，实施中）：紧凑渠道列240改190px，TPM/RPM自动换行，宽屏320px保持。仅布局微调。
- 完成：紧凑渠道190px，负载指标按空间换行，表格紧凑最小宽度降至870px。类型检查/构建通过；本轮未运行浏览器及回归，未提交部署。

- 2026-09-17交付整合：已纳入local-changes-integration统一验证，提交推送进度见[整合记录](2026-09-17-local-changes-integration.md)。历史状态不代表本次推送结果。
