# 调权中心评估样本短暂归零

- 目标：排查并修复评估窗口短暂查不到样本时，所有渠道状态和权重展示反复归零的问题。
- 状态：再次验收补修 2a5d528df 已提交并推送 origin/main，隔离检出 Go 回归通过；本轮打包已由用户取消，未部署；更新时间：2026-09-14。首版为 f6a261bdb（本地对应 87b788fff）。
- 工作目录：`E:\projects\controlTower`；分支：`main`；任务 ID：未提供。
- 文件范围：`server/internal/tuning/continuous_engine.go`、`continuous_engine_test.go`、`empty_samples_test.go`；`webapp/packages/desktop/src/views/ContinuousTuningView.vue`、`webapp/packages/desktop/tests/tuningRefresh.test.mjs`；本文档、`PROJECT_PROGRESS.md`。
- 范围外：其他告警、账单、容器日志任务及线上部署。
- 验收：全站指标查询返回空集时，普通渠道保留上一轮可信评估、不发起常规权重写入；熔断探测及恢复继续运行；指标恢复后正常续评。页面请求失败或意外空列表时保留同站点上次结果并告警，首次无状态不显示 0 样本，真实返回的 0 正常显示；刷新乱序、慢请求、站点切换不回退或阻断更新。
- 已确认：评估引擎原先会把缺失指标映射成零值并逐渠道持久化，页面用 `last_observed_requests` 显示 0 样本。首版采用全站提前返回，验收后收窄为逐个普通渠道保护；不再阻止熔断、探测、软启动、零基础权重处理和活跃渠道初始化。工作区其他任务改动未触碰。
- 本次验证：`go test ./internal/tuning -count=1`、`node --test tests/tuningRefresh.test.mjs`（8 项）、`pnpm typecheck`、`pnpm exec vite build`、指定文件 `git diff --check` 均通过。构建只有大包体提示；浏览器/真实接口联调、实库、线上及其他模块未运行。
- 限制：尚未取得故障时的实际 `metric_1m` 数据和服务日志，无法确认空集产生于 Agent、写库还是查询，也不能认定用户看到的线上权重 0 由同一原因造成。全站持续无流量时会保留最后一次有样本的状态并显示评估时间过期。
- 用户补充（2026-09-14）：空样本与恢复间隔很短且反复发生。15 分钟是滚动查询窗口，正常已有桶不会在短时间全部消失再出现；因此“查询确实返回空集”本身尚未证实。页面初始 `load()` 的状态 API 失败分支会把 `states` 清空，呈现 0 样本；定时 `refreshRuntime()` 失败时会保留旧状态。需用故障时间的 `metrics_rows`、状态 API 响应和渠道变更流水区分后端空集、前端临时失败及真实写零。
- 前端修复：状态只在同站点复用；请求失败和意外空列表保留上次成功状态并显示错误，首次无状态显示等待，避免把缺数据伪装为 0 样本；加载与定时刷新使用代次防止旧响应回写。当前线上零权重是否实际写入仍未确认。
- 下一步：部署前检查故障时 `metrics_rows` 日志与变更流水，核实是否有 `circuit_opened`/`weight_write` 到 0；上线后观察空集日志和页面状态。

## 再次验收（2026-09-14）

- 范围：后端普通评估与熔断恢复、前端请求失败/空响应/刷新乱序/站点切换；沿用本任务。
- 已发现：全站空结果且任意历史状态有样本时整轮提前返回，会阻断熔断渠道发起探测和消费成功探测结果。历史关闭模型的状态也可能阻止活跃模型初始化。
- 已发现：前端按最后发起请求作废旧请求；当每次响应超过 30 秒轮询间隔时，完成的请求会不断被作废，页面无法更新。
- 修正：空指标只保留普通渠道状态，其他生命周期继续执行，并在周期日志记录保留数量；前端按已完成请求序号防止回退，完整重载仍使先前轮询失效。
- 复现证据：新增 Go 测试在首版上复现探测发起、成功恢复、探测超时、软启动停滞，以及非活跃历史状态阻断初始化、零基础权重未更新；修正后通过。页面实际脚本的慢请求测试在首版上失败（样本仍为 42，预期 60），修正后 8 项全部通过，涵盖初次失败恢复、同站点保留、真实零值、跨站点、乱序、重载、慢请求及迟到错误。
- 下一步：后续按用户安排打包/部署及真实浏览器/站点验收。原始线上故障触发源仍需要故障时的指标查询日志、状态 API 和调权流水定位，不能用本地回归代替线上证据。
- 提交方式：在远端 f6a261bdb 的隔离检出中仅纳入本任务 7 个文件；主工作区其他任务改动保留，本地 main 未强制同步远端。
- 交付范围更新：用户于本轮明确取消打包；仅提交推送，不生成发布包、不创建发布标签。
