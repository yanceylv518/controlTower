# 上游管理页面重设计

- 目标：实现用户确认的深色主从布局设计图，兼容其他主题。
- 状态：代码完成、验证通过；未提交部署。更新时间：2026-09-18。
- 授权：用户明确“就按这个来实现”，本会话转入此功能，前项按钮修复已完成。
- 工作目录：E:/projects/controlTower/.tmp/dual-theme；分支：codex/dual-theme。
- 任务 ID：01a0b317-381b-7bd3-bdca-04d98b2e6d0f。
- 文件范围：BillingUpstreamsView.vue、必要页面测试、本记录、PROJECT_PROGRESS.md。
- 范围外：后端接口、计费/密钥管理、导航重做、其他任务改动、提交部署。
- 验收：上游列表、详情渠道表、未分配入口、编辑抽屉；保留增删改及渠道独占规则，切换站点清空旧状态；主题可读且窄屏可用，类型检查与相关测试/构建通过。用户追加要求取消顶部统计，按渠道数降序排列，整体采用紧凑密度。
- 已核查：主题目录存在通知等并发改动，本页面未修改；仅在本页面及独立测试内实施，不覆盖共享接口/导航。
- 完成：左侧搜索/状态筛选和未分配入口，右侧渠道名称/ID/模型/状态表格；渠道按现有接口关联，编辑抽屉支持搜索、仅看已选、占用禁选。上游按关联数量降序、同数稳定排序，保留当前选择；统计卡、重复页面标题、重复详情字段和空备注占位已删除，刷新/新建移到顶栏。
- 数据保护：切站同步清空数据与编辑状态，旧请求不能覆盖新站点；保存捕获原站点及表单快照、防重复提交，删除取消及确认期间切站不写入；不可用的旧关联可显示/取消，未擅自丢弃。
- 验证：最终页面6项行为测试通过；本轮31项主题回归通过；最终类型检查和生产构建通过（既有大包提示），diff检查通过。首次TS7006已修复，通知会话记录中的当时阻塞已解除。最终按钮间距8px纯CSS补充由浏览器复验，不重复生产构建。
- 浏览器：5200真实数据加载45上游、212去重渠道；排序前列53/28/19/17/7，未分配17；抽屉600px，已选筛选53行。顶部统计DOM为0，工作区起始72px；表格行高46px。800px窗口改为单列且无页面横向溢出，1083px同样无页面溢出，表格窄时内部横滚。
- 限制：真实页面仅检验当前深色配色，未逐一截图八套主题；未对远程站点执行保存/删除，写路径以模拟接口测试验证。未提交部署。其他通知/调权改动保留。
- 下一步：用户在5200复验最终紧凑布局，按需提交交付。
