# 调权速度排除重试样本

## rc115 提交与打包（2026-09-15，已发布）

- 用户授权提交、推送并打包；功能提交 26a2e4b3，合并远端主线的提交 6685ce2d8b28e9042bdaf71766ef6b35082d3181 已推送 origin/main，v2.0.0-rc115 指向该合并提交。仅进度文档冲突，合并时保留双方记录；主工作区其他代码保留。
- 本轮合并后全量 go test ./... 与 go vet ./... 通过（可缓存包由 Go 缓存命中，环境依赖实库跳过）。GitHub CI 34941944921 成功，包含 Go 测试/构建及前端类型检查/构建。
- release 34942184936 成功；API 核验公开非草稿 Release，Server amd64、Agent amd64/arm64 三份非空安装包及 SHA256SUMS 均 uploaded；Server rc115/latest 镜像构建推送步骤成功。未下载复核安装包内容，未部署生产。
- 发布页：https://github.com/yanceylv518/controlTower/releases/tag/v2.0.0-rc115；镜像 ghcr.io/yanceylv518/controltower-server:v2.0.0-rc115。
- 本次新增运行时代码仅为 Server 的完整分钟查询边界，无新增迁移，已升级 rc114 的 Agent 可继续使用。频道 255 长期无有效 TTFT 的源数据仍未核实，窗口修复不承诺解决所有真实样本不足。
- 下一步：用户部署 Server 后验收一分钟评估的稳定性；未授权/执行生产部署。

## 分钟窗口修复（2026-09-15，代码完成，未提交）

- 用户授权修复；实现位于 E:/projects/controlTower/.tmp/tuning-direct-ttft，分支 codex/tuning-direct-ttft；主工作区其他代码保留。
- 变更：continuous_engine.go 性能评估查询以 now.Truncate(time.Minute) 为结束边界，再回退配置的 N 分钟。分钟内固定区间，每次仍重新查询以接收迟到补报；不再把上一完整分钟排除而只用当前未结束分钟。
- 范围：窗口内请求数及 TTFT/缓存/输出证据共用该对齐区间；评分公式、筛选条件、阈值不变。通用 SQL、实时 RPM/TPM、错误衰减时钟、快速熔断、监控、Agent 和 NewAPI 均未修改，无新增查询/迁移。
- 回归：新增 continuous_window_test.go，模拟 SQL 对分钟桶的过滤，修复前 1/5/15 分钟场景失败复现。修复后覆盖 UTC/CST/跨日、整分/小数秒/跨分钟、真实不足仍判不足、实际 now 用于限流与评估时间、查询次数不增加。
- 验证：本轮 tuning、mysqlstore、ingest、metricaggregator 四包完整 Go 回归通过，tuning vet 与 diff --check 通过；实库依赖测试跳过，未跑浏览器/生产验收，未重复前端构建（无前端变更）。
- 限制：结束的分钟仍可能有采集补报，未额外引入沉淀等待；不能保证真实有效请求少于 5 时不提示不足。渠道 255 长期缺少有效 TTFT 的源数据问题仍需独立核查。
- 交付：代码/回归/文档已完成，未提交推送或部署；本次边界修复仅需升级 Server。下一步按用户安排提交与上线验收。

## 分钟窗口边界定位（2026-09-15，代码问题已确认，未修复）

- 用户截图确认调权评估窗口为 1 分钟、min_samples=5，描述刚达标后又频繁不足；两台 Agent 已升级。此前只验证分批累加不足以排除查询窗口错误，不能沿用“未发现计数问题”作总体结论。
- 代码证据：Agent aggregator.go:46 按 CreatedAt.Truncate(time.Minute) 写桶；continuous_engine.go:89 用 now-N分钟到 now 查询，mysqlstore/tuning.go 按 bucket_time>=start 且 <end 过滤；rc114 标签源码确认相同。非整分执行时，1 分钟窗口只包含当前未完成分钟，上一完整分钟被排除。
- 边界算例（按现有 SQL 条件执行 PowerShell 时间比较，非实库测试）：15:09:20 只取 15:09 桶的 6 条，15:10:20 只取 15:10 桶的 1 条，即从达标变不足。窗口查询对象是分钟桶，不能实现精确滑动 60 秒；该问题在新速度过滤前已存在，过滤后的较少有效样本更容易触发。
- 修复方向：保持配置时长，以最近结束的完整分钟对齐窗口上下界；补充跨分钟/不同窗口长度测试，限制到调权评估，保持实时 RPM/TPM 和快速熔断路径独立。当前仅定位和方案，尚未修改生产实现或线上设置。
- 独立问题：渠道 255 最新说明显示窗口请求 350、直接/重试/未知（含专用覆盖缺口）全部 0，按查询关系没有公共有效流式 TTFT，不能用窗口修复解释其所有长期零值。NewAPI 初始化 frt 对应时间为 StartTime-1秒，未打点时生成 -1000；某些非流式或自定义扫描路径不打点，CT 会过滤。源站该渠道实际走哪条路径尚无证据，未修改 NewAPI。
- 本轮仅源码对照及时间条件算例，未运行新的 Go/实库/浏览器测试；前轮新增 8 场景回归仍为此前结果。未提交/部署。

## 线上样本不足复查（2026-09-15，待线上证据）

- 用户确认两台 Agent 均已升级；未将旧 Agent 作为已确认原因。截图渠道 255 请求数 530/185 而速度有效 0，渠道 141/146 也仅少量有效样本。
- 核查：原始日志全量进入渠道聚合，未走慢样本抽样；速度分类要求有效流式 TTFT 和可确认单次尝试，缺失/不匹配路由不能当作未重试。公共 TTFT = 速度有效 + 重试排除 + 未知 + 缺少专用统计；窗口请求数还包含没有有效流式 TTFT 的日志，不能直接与速度计数相减认定漏算。
- 新增回归：530 条原始日志拆成单条批次，经 ConvertRow、Aggregate、真实报告 JSON 编解码及统计合并，正常日志仍为 530 条有效；跨渠道/同渠道重试、缺路由、路由不匹配、零输出、非流式、缺 TTFT 共 8 场景通过。测试位于隔离目录 agent/internal/metricaggregator/speed_ttft_test.go。
- 验证：本轮 9 个相关 Go 包通过（另 speedstats 无独立测试），新增后 logcollector/metricaggregator 全包再通过，diff --check 通过；实库依赖测试跳过，未跑生产查询。
- 结论边界：未复现计数按批次漏算，不能据此认定线上统计正常。页面把多种缺证据情况统一显示“速度样本不足”，且未知/未覆盖计数只在悬浮中显示；样本不足时可能展示保留的上一轮系数。
- 阻碍：浏览器工具 nodeRepl.fetch request failed，未能读取实时状态或源日志；等待渠道 255 的无法确认计数，必要时核查同窗口成功日志的 is_stream、completion_tokens、frt、admin_info.use_channel（不需要敏感请求内容）。
- 本轮仅增加回归与记录，未改生产统计/前端口径，未提交/推送/部署；NewAPI 不变。下一步用线上分类与源日志确认具体缺口后再修复。

- 目标：监控 TTFT 保留现有全部样本；调权速度与基线只使用可确认未重试的有效流式 TTFT。
- 状态：代码 0edb9e4e2 已提交并快进推送 origin/main；更新时间：2026-09-15。未发布/部署；下方未提交描述为当时状态。
- 任务 ID：01a09ee4-7eb0-7b73-a512-fc4e9e1893ae。
- 工作目录：E:/projects/controlTower/.tmp/tuning-direct-ttft；分支：codex/tuning-direct-ttft；基于 origin/main eeea96788。
- 范围：Agent 日志解析/渠道聚合与上报协议、Server 验证/存储迁移/调权查询与样本保护、调权页样本说明、测试及接口文档。
- 范围外：NewAPI 修改、跨批次请求关联/持久化、监控 TTFT 过滤、OTPS/缓存/错误统计口径、部署/发布。
- 验收：同次上报新增渠道专用直方图与计数；普通/跨渠道重试/同渠道重试/未知元数据正确区分；公共监控数据不变；旧 Agent 不产生有效新样本；速度样本不足保留先前可信系数（首次中性）；上报去重及分钟合并正确。
- 协作：主工作区已有多项改动，未覆盖；相关调权空样本任务已提交；其他任务状态只读核实，无并发改动本隔离目录。
- 已完成：解析成功日志的 admin_info.use_channel，校验正整数渠道序列及末渠道归属；单渠道重复尝试也视为重试，缺失/异常元数据不算未重试。只在有有效 TTFT 的流式日志中额外解析，减少无关请求处理。
- 已完成：Agent 仅为 instance_channel 在同次上报中附带 speed_ttft（15 桶及 retry_count/unknown_count）；公共 TTFT、请求/Token、OTPS、缓存/错误数据保持原样。Server 校验可选统计，异常仅丢弃可选证据，不阻断监控。
- 已完成：079 迁移新增 1m/5m 统计列及调权状态字段；新旧数据按可用新证据合并，缺失覆盖单独计算，不从历史公共桶回填。沿用现有批次幂等事务；调权速度分位数/基线使用未重试样本，最低门槛按有效样本数判断。缓存/OTPS 的原有资格条件和数据不变。
- 已完成：不足时保留同模型已有新口径速度系数，首次/旧口径使用 1；调权页显示有效与重试排除计数，提示中展示排除比例及未知覆盖；事件 evidence 携带口径版本和计数。
- 验证：全项目 go test ./... 通过（47 包结果，环境依赖集成按约定跳过）；相关 7 包 vet 通过；最终解析/聚合微调后 4 包回归通过；10 项调权前端脚本测试、类型检查及生产构建通过（仅既有大包提示）；git diff --check 通过。
- 实库验证：独立本地 MySQL 8.0 的 TestSpeedTTFTMySQLIntegration 通过，覆盖迁移、公共/专用分钟查询、1m/5m 合并、重复批次去重、新旧混合和调权状态写入读取。测试服务已停止、临时数据库目录已清理；未访问生产数据库。
- 性能证据：示例每条渠道 payload 增加 91 字节未压缩 JSON；1000 条事件聚合基准约 0.817ms/op、514KB/op（整个聚合过程，不能视为新增开销）；没有增加 HTTP 次数或源数据库查询。本机基准不能代表生产性能。
- 限制：真实站点 use_channel 覆盖率、生产迁移耗时及浏览器人工交互未验收。需先升级 Server/前端并完成 079 迁移，再升级 Agent；旧 Agent 不能提供新口径样本。
- 下一步：按用户安排提交/发布及真实站点验收。实现只在上述隔离目录，主工作区代码和预览保持原状。详细测试输出保留于隔离目录 .tmp/verification/*.log。


## 上线前复查（2026-09-15）

- 结论：发现并修复边界联动，复查后本地检查通过；未提交/发布/部署。
- 已修复：恢复探针原先复用 TTFT 基线，替换为未重试速度基线会使缺少新证据时错误放行慢探针、或让正常探针错误失败。恢复探针现保留原公共 P95 平均基线，只有调权速度评分使用新基线；新增两类实际恢复状态机回归。
- 已修复：speed_ttft 的字段类型错误此前在 JSON 解码阶段拒绝整批上报。新增可选字段容错解码，非法统计交由 ingest 忽略，公共监控数据继续处理；补充字符串/数组/子字段错误类型回归。整份非法 JSON 仍正常拒绝。
- 已修复：升级后首次重新评估前，缓存的旧 metric_ready/baseline_ready 可能在页面显示成已完成新口径评估。前端增加 speed_stats_version 检查，旧版本显示等待新口径评估。
- 已修复：保留已有速度系数的规则收窄到正常可参与渠道；基础权重 0 或多模型安全排除渠道仍使用中性系数，避免改变已有排除语义。
- 验证：两项主要缺陷在修复前测试复现，见 .tmp/verification/review-repro.log；修复后全项目 Go 测试通过（47 包结果）、8 个相关包 vet 通过，11 项前端测试及类型检查/生产构建通过；最终排除渠道保护调整后 tuning 包全量回归通过，git diff --check 通过。
- 迁移/协作：已 fetch 确认 origin/main 仍为 eeea96788，隔离基线无落后，无 079 编号冲突。本轮未改 SQL/迁移，前轮本地 MySQL 专项验证仍适用于该未变部分；本轮未重复启动实库测试，未把此前结果计作本轮执行。
- 限制：仍未在真实站点验证渠道尝试字段覆盖率、生产迁移耗时或浏览器人工交互。部署顺序仍为 Server/前端和 079 迁移先行，再升级 Agent；NewAPI 与监控 TTFT 口径不变。


## 提交交付（2026-09-15）

- 用户授权提交并推送；从隔离目录提交 32 个本任务文件，代码提交 `0edb9e4e2`（fix(tuning): exclude retry TTFT from speed scoring），已从 eeea96788 快进推送 origin/main。
- 提交前 fetch 确认无主线变化，已审核暂存范围、git diff --cached --check；代码未变，本次提交操作未重复运行前轮已通过的测试。此前的代码验证与限制见上文。
- 主工作区其他代码及预览保留，未强制同步本地 main；本次未打包发布或部署，升级需 Server/前端与 079 迁移，再升级 Agent。
