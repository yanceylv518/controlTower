# 调权速度排除重试样本

- 目标：监控 TTFT 保留现有全部样本；调权速度与基线只使用可确认未重试的有效流式 TTFT。
- 状态：代码及验证完成，未提交/发布/部署；更新时间：2026-09-15。
- 任务 ID：01a09ee4-7eb0-7b73-a512-fc4e9e1893ae。
- 工作目录：E:/projects/controlTower/.tmp/tuning-direct-ttft；分支：codex/tuning-direct-ttft；基于 origin/main eeea96788。
- 范围：Agent 日志解析/渠道聚合与上报协议、Server 验证/存储迁移/调权查询与样本保护、调权页样本说明、测试及接口文档。
- 范围外：NewAPI 修改、跨批次请求关联/持久化、监控 TTFT 过滤、OTPS/缓存/错误统计口径、部署/发布。
- 验收：同次上报新增渠道专用直方图与计数；普通/跨渠道重试/同渠道重试/未知元数据正确区分；公共监控数据不变；旧 Agent 不产生有效新样本；速度样本不足保留先前可信系数（首次中性）；上报去重及分钟合并正确。
- 协作：主工作区已有多项改动，未覆盖；相关调权空样本任务已提交；其他任务状态只读核实，无并发改动本隔离目录。
- 已完成：解析成功日志的 admin_info.use_channel，校验正整数渠道序列及末渠道归属；单渠道重复尝试也视为重试，缺失/异常元数据不算未重试。只在有有效 TTFT 的流式日志中额外解析，减少无关请求处理。
- 已完成：Agent 仅为 instance_channel 在同次上报中附带 speed_ttft（15 桶及 retry_count/unknown_count）；公共 TTFT、请求/Token、OTPS、缓存/错误数据保持原样。Server 校验可选统计，异常仅丢弃可选证据，不阻断监控。
- 已完成：079 迁移新增 1m/5m 统计列及调权状态字段；新旧数据按可用新证据合并，缺失覆盖单独计算，不从历史公共桶回填。沿用现有批次幂等事务；调权速度分位数/基线使用未重试样本，最低门槛按有效样本数判断。缓存/OTPS 的原有资格条件和数据不变。
- 已完成：不足时保留同模型已有新口径速度系数，首次/旧口径使用 1；调权页显示有效与重试排除计数，提示中展示排除比例及未知覆盖；事件 evidence 携带口径版本和计数。
- 验证：全项目 go test ./... 通过（47 包结果，环境依赖集成按约定跳过）；相关 7 包 vet 通过；最终解析/聚合微调后 4 包回归通过；10 项调权前端脚本测试、类型检查及生产构建通过（仅既有大包提示）；git diff --check 通过。
- 实库验证：独立本地 MySQL 8.0 的 TestSpeedTTFTMySQLIntegration 通过，覆盖迁移、公共/专用分钟查询、1m/5m 合并、重复批次去重、新旧混合和调权状态写入读取。测试服务已停止、临时数据库目录已清理；未访问生产数据库。
- 性能证据：示例每条渠道 payload 增加 91 字节未压缩 JSON；1000 条事件聚合基准约 0.817ms/op、514KB/op（整个聚合过程，不能视为新增开销）；没有增加 HTTP 次数或源数据库查询。本机基准不能代表生产性能。
- 限制：真实站点 use_channel 覆盖率、生产迁移耗时及浏览器人工交互未验收。需先升级 Server/前端并完成 079 迁移，再升级 Agent；旧 Agent 不能提供新口径样本。
- 下一步：按用户安排提交/发布及真实站点验收。实现只在上述隔离目录，主工作区代码和预览保持原状。详细测试输出保留于隔离目录 .tmp/verification/*.log。


## 上线前复查（2026-09-15）

- 结论：发现并修复边界联动，复查后本地检查通过；未提交/发布/部署。
- 已修复：恢复探针原先复用 TTFT 基线，替换为未重试速度基线会使缺少新证据时错误放行慢探针、或让正常探针错误失败。恢复探针现保留原公共 P95 平均基线，只有调权速度评分使用新基线；新增两类实际恢复状态机回归。
- 已修复：speed_ttft 的字段类型错误此前在 JSON 解码阶段拒绝整批上报。新增可选字段容错解码，非法统计交由 ingest 忽略，公共监控数据继续处理；补充字符串/数组/子字段错误类型回归。整份非法 JSON 仍正常拒绝。
- 已修复：升级后首次重新评估前，缓存的旧 metric_ready/baseline_ready 可能在页面显示成已完成新口径评估。前端增加 speed_stats_version 检查，旧版本显示等待新口径评估。
- 已修复：保留已有速度系数的规则收窄到正常可参与渠道；基础权重 0 或多模型安全排除渠道仍使用中性系数，避免改变已有排除语义。
- 验证：两项主要缺陷在修复前测试复现，见 .tmp/verification/review-repro.log；修复后全项目 Go 测试通过（47 包结果）、8 个相关包 vet 通过，11 项前端测试及类型检查/生产构建通过；最终排除渠道保护调整后 tuning 包全量回归通过，git diff --check 通过。
- 迁移/协作：已 fetch 确认 origin/main 仍为 eeea96788，隔离基线无落后，无 079 编号冲突。本轮未改 SQL/迁移，前轮本地 MySQL 专项验证仍适用于该未变部分；本轮未重复启动实库测试，未把此前结果计作本轮执行。
- 限制：仍未在真实站点验证渠道尝试字段覆盖率、生产迁移耗时或浏览器人工交互。部署顺序仍为 Server/前端和 079 迁移先行，再升级 Agent；NewAPI 与监控 TTFT 口径不变。
