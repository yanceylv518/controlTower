# 持续超限主动降权方案

状态：用户随后要求“再看看此方案有没有潜在问题，没有的话，就按这个来实现”，已完成复核、实现和本地验证。尚未提交、推送、发布或部署；生产效果待升级后验收。

## 目标和边界

采用比例容量反馈、单轮降幅限制、完整反馈窗口及滞回恢复。自动模式执行；观察模式展示建议；关闭模式保持原行为。容量控制不修改基础权重、优先级、分组，不因容量问题禁用渠道或拒绝请求；错误熔断保留独立保护。

权重影响相对分流，不能保证绝对TPM上限。单渠道独占、最高优先级无替代、分组隔离、候选容量耗尽及请求Token规模变化时，必须如实说明能力边界。

## 实现默认值

| 参数 | 建议 |
| --- | --- |
| 进入阈值 | 实际负载达到配置上限100% |
| 首次降权确认 | 新的有效数据持续超限至少60秒 |
| 目标利用率 | 90% |
| 单次容量最大降幅 | 25%，整数权重允许最少下降1的明确例外 |
| 反馈等待 | 写入确认后，收到窗口起点不早于权重生效时间的完整60秒负载窗口 |
| 解除条件 | 所有已配置项严格低于85%，以新数据持续确认60秒 |
| 容量最低权重 | 1；不把已有熔断权重0提高到1 |

## 决策规则

1. TPM/RPM分别除以各自正上限，取最大利用率u；上限0的项忽略，两项不相乘。用Agent统计截止时间推进判断，重复窗口不累计确认时间；缺失、过期、不完整数据不能视为0。
2. 首次u>=1立即进入限升状态。有效超限证据持续60秒才主动降权，期间回到u<1取消本次降权确认，但不解除限升；解除仍须低于85%持续60秒，避免100%→95%就恢复上调造成循环。原有性能下降和错误保护仍工作。
3. 检查实际模型/分组/优先级的分流条件。每个分组均需有同优先级、正权重、未熔断/暂停、错误率未达轻度阈值的替代渠道；配置容量的替代渠道利用率须低于85%，且未处于容量保护/待执行状态。部分分组缺替代或分组未知时保持限升，提示分流空间不足；未配置上限的替代渠道允许逐步尝试，但标明余量未确认，不当作无限容量。多个超限渠道不会互相作为有余量的替代。
4. W使用最近确认的实际权重，不用基础权重或待执行目标：

   Wraw = max(1, floor(W * 0.90 / u))

   本轮允许下降量 = max(1, floor(W * 0.25))

   Wcapacity = max(Wraw, W - 本轮允许下降量, 1)

   仅在W>1且超限确认、反馈等待、分流条件满足时应用下降。W=1继续超限显示已达容量权重下限。
5. 普通最终目标取性能目标与容量约束的较小值。25%仅约束容量原因的下降，不阻挡性能惩罚或错误熔断；容量目标可低于性能倍率下限。性能样本不足时保持原性能目标，但有效实时负载仍可触发容量下降。
6. 直连必须收到写入成功确认；Agent队列必须收到执行回执，入队不代表生效。确认前同一渠道不堆叠普通调权/容量命令，页面展示正在执行的目标。熔断置零可以替代未领取的旧容量命令。失败沿用写失败保护，不计作降权成功；数据/配置变化造成的主动取消不计入写失败，也不触发整个站点的过期命令保护。
7. 确认后等待完整的新负载窗口，禁止性能上调抵消容量下降。窗口起点达到最近权重生效时刻后，才允许下一次容量下降。等待期间更强的性能下降/熔断仍可执行，新的权重变化重新建立反馈基准。
8. 反馈后u>=1按新负载继续计算；0.85<=u<1保持容量约束，不为退出而继续压低，也不立即加回；u<0.85持续60秒后解除。解除后沿用现有单次上调上限，不跳回基础权重。稳定在90%且长期限升属于预期行为。
9. 实时负载不可用时暂停新的比例容量下降、继续禁止有配置上限渠道的上调；有效性能下降和错误熔断保留。熔断恢复、软启动和待执行命令也应检查容量门禁，不能绕过保护扩大流量。

## 示例

W=100、TPM150万、上限100万，u=1.5，Wraw=60；单轮最多下降25，首轮Wcapacity=75。待新窗口覆盖该写入的实际效果后重算，不预设下轮一定降到60。若新负载在85%至100%，保持；持续低于85%才解除。

整数例外：W=2最小可执行下降为1，实际降幅50%，超过名义25%；说明中需明确。W=1保持可用，容量规则不写0。

## 实现与验收要求

- 容量判断独立于性能样本门槛及性能指标空窗，保持混合模型/基础权重0等既有排除规则。
- 持久化容量阶段、证据截止时间、超限/恢复确认起点、待执行命令、最后实际生效时间/权重；重启和重报不重置等待或重复降权。
- 页面显示超限观察、容量降权、等待反馈、保持限升、恢复观察、分流空间不足/最低权重及写入失败；记录TPM/RPM、上限、窗口、原权重、理论目标、限幅后目标和实际结果。
- 回归覆盖短时/持续超限、双上限、旧窗口重复、数据缺失/延迟、性能样本不足、整数边界、性能更强下降、85%/100%边界、90%保持、恢复上调、写入失败、队列回执、重启、熔断/软启动及无分流空间。
- 生产验收同时观察负载和权重执行记录；权重降低本身不等于流量控制成功。

## 实现与复核结果（2026-09-24）

- 新增容量状态机，使用现有滚动60秒负载快照的共同截止时间；超过60秒的证据推进间隔重置连续确认，重复/倒退/过期数据不累计时间。性能数据为空或未达最少样本时仍可按容量证据降权。
- 持久化采用096迁移的`capacity_control_json`，保存阶段、限额、利用率、证据时间、确认权重、保守生效时间及待执行命令。队列/直连命令记录与待执行标记在同一事务关联，避免重启后重复下发。
- 反馈起点使用不早于实际生效的确认时间：直连记录真实API成功时间，队列使用观察到回执的时刻。更强的性能下降、普通熔断、快速熔断共享确认跟踪；启用恢复、软启动与领取队列重新检查容量/数据/模式门禁。
- 领取时拒绝已过期证据、配置或模式变化、熔断取代的旧容量决定。取消原因单独标记，避免旧自动保护逻辑误停整个站点。真正的写入失败仍累计并按现有三次失败/十分钟重试保护执行。
- 页面新增各容量阶段、待执行/反馈提示、容量计算依据和`capacity_reduce`变更事件；帮助文字更新为主动控制策略。未改变基础权重、优先级、分组或Agent协议。
- 沿用单个Server调权循环；分流检查基于当前渠道快照和负载，不承诺未知分组流量分布、未配置上限的替代渠道容量或权重到流量的严格线性关系。已投递给Agent的命令无法撤回，后续命令由同一个Agent顺序执行。

## 验证

- `go test ./...`、`go vet ./...`通过；最终相关`tuning/mysqlstore/directcontrol`包专项与vet通过。新增11个容量测试函数，含整数边界、双限额、持续/瞬时超限、95%保持、90%保持、严格85%边界、稀疏样本、缺失/重复/倒退数据、队列回执、重启、写失败、性能下降、两种熔断恢复、同优先级/分组分流及观察模式。
- 独立本地MySQL8.0.46、127.0.0.1:33419、专用`capacity_control_test`库：096迁移、状态JSON往返、原子待执行标记、防重、回执、配置/模式/陈旧数据撤销、不误触发站点暂停、熔断替换、健康队列上调和直连终态时间通过；并复跑既有命令清理并发、熔断状态集成。
- 真实MySQL+模拟new-api直连集成通过：实际写入目标、成功时间及变更记录有效；模式变更在发出HTTP前阻断。顺带更正旧集成断言：自动调权记录在recommendations/commands，不进入仅人工操作的审计表；保留人工分组操作审计断言。
- 前端420项回归（调权34项）、TypeScript检查、生产构建通过；沿用现有大包体积提示。未做本轮真实浏览器/生产流量验收。
- 首次Go运行碰到共享全局缓存权限，改用工作区`.tmp/go-cache`后通过。复跑全局过期清理时，旧直连集成遗留3条待执行命令导致总数253而非250；补直连fixture定向清理，并在新建的`capacity_final_store_test`与`capacity_final_direct_test`分库复跑，容量/熔断/过期清理和直连全部通过。没有修改生产配置或连接生产数据库。

## 交付与生产验收

需发布Server+Web，096迁移由新版Server启动时自动执行，正常升级无需手动执行SQL；Agent无新增协议要求，但必须能提供完整实时负载。已实现/本地测试不等于已发布/部署/线上验收。升级后检查：超限观察约60秒后有实际降权回执；下一步下降只在完整反馈窗口后发生；90%保持、低于85%持续60秒后渐进恢复；没有同组同优先级替代时显示分流不足而不无效连降；同时观察真实TPM/RPM及错误率，不能仅凭权重下降判断控流成功。

工作区其他任务的监控图表改动及原Agent复核文档保留，未纳入本功能提交或生产操作。

## 096迁移与启动专项复核（2026-09-24）

- 096只有一条`ALTER TABLE tuning_continuous_states ADD COLUMN capacity_control_json MEDIUMTEXT NULL`，无数据回填、索引重建语句或分号注释问题。旧行新字段为NULL，读取侧`COALESCE(...,'{}')`兼容旧状态，不改权重或熔断字段。
- Server在HTTP监听前执行迁移，使用独立迁移连接、30分钟上下文和MySQL连接级迁移锁；按`schema_migrations`跳过已应用版本。字段已创建但版本记录缺失时，1060重复列由既有迁移器容忍，可补写记录。Dockerfile及Linux发布包均复制整个迁移目录。
- 新增`TestCapacityMigrationStartupCompatibility`，用临时独立schema重建001–095并预置普通/熔断旧状态。覆盖元数据锁等待失败且不记录成功、解除锁后的两个并发升级、旧数据/NULL读回、重复启动不重写应用时间、字段已存在但版本记录丢失的重试与容量证据保留；没有改迁移SQL或通用迁移逻辑。测试账号需有创建/删除隔离schema权限，避免改动现有测试表。
- 在独立MySQL8.0.46（127.0.0.1:33423）通过上述回归，两个并发升级约24ms完成。另编译实际Server，在仅本地测试库的API-only模式执行095基线启动→096升级→再次启动，三次`/healthz`均200；后两次从启动至健康约522ms/513ms。表ID保持1414，`TOTAL_ROW_VERSIONS`由0变1，该实验中增列使用即时DDL。以上是小规模隔离库结果，不推算生产耗时。
- 相关迁移/容量MySQL回归和`go vet ./server/internal/mysqlstore`通过。阻塞实验使用会话`lock_wait_timeout=1`，得到包含096文件名的1205错误；生产仍沿用原有超时配置。数据库权限不足、长事务元数据锁等待、连接异常或迁移超时，均会让现有Server启动流程报错退出，不能保证任意现场条件下一定成功。
- 未访问生产数据库，未提交发布部署。生产是否存在长事务或特殊DDL权限仍需现场验证；正常升级和中断重试未发现096特有的启动阻塞缺陷。

## 提交验收（2026-09-24）

用户授权本功能与使用日志 Enter 修复一并提交推送。独立交付快照 Go 全量 test/vet、426 项前端回归、类型检查和生产构建通过，隔离了监控/错误统计的未提交改动。实库专项沿用上文已通过记录，本次未重跑。同步发现远程新增日志详情和分组筛选，后续合并与最终验证结果见追加记录。提交推送以 Git 为准，未发布部署。
