# 本地业务改动整合推送

- 目标：按用户明确授权整合全部本地业务代码，保留最新主线功能，推送 origin/main。
- 状态：代码已提交并推送main，未发布部署；更新时间：2026-09-17。
- 工作目录：E:/projects/controlTower/.tmp/local-changes-integration；分支：codex/local-changes-integration。
- 文件范围：各工作区尚未交付业务代码及必要测试、迁移、接口/任务文档；按差异去重，不覆盖源工作区。
- 范围外：缓存/构建产物/临时预览提交，发布打包、部署、生产数据变更。
- 验收：以最新 origin/main 为基线整合，处理共享文件冲突；Go/前端检查通过；推送后核对远端提交。
- 初始化：主目录 main 落后且有重复交付残留；只读渠道、平均输出及TTFT替代/调权UI、账单站点选项在隔离工作区。已fetch远端到229c9036a（日志页面改动），以其为集成基线。保留各源目录，先比较三方差异。
- 下一步：建立隔离集成目录，排除已交付/无实质变化文件，整合新增业务差异。

## 整合与验证（2026-09-17）
- 以229c9036a为基线。主目录73个业务文件与主线一致；其余大部分变动在三方合并后也无新增差异。保留主线客户语音指标、命令渠道目录、设置布局、rc35只读日志以及新TTFT测试，未回退到旧工作区版本。
- 新增业务：用户账单按站点/用户分组、搜索/账期筛选/分页；保留主线主账单与按日下载、历史计费规则及计费来源说明；增加mismatch_rows类型及3项分组回归。
- 整合tuning-otps-fallback完整平均输出/TTFT替代和调权UI；tuning-direct-ttft无独有文件，差异仅后续调权引擎/页面/测试扩展，取后续版本避免重复覆盖。
- 整合readonly-channel-source与billing-task-site-options。共享ingest/service.go、mysqlstore/store.go、API契约三方合并；调权前端、只读渠道后台同时保留。
- runtime-log-delivery两个文件无内容差异，不制造提交。缓存、构建结果、临时预览、独立未定稿归档设计、AGENTS本地规则保留源目录未纳入业务交付。
- 验证：go test ./...、go vet ./...通过；139项现有前端测试+3项新增账单分组回归通过；vue-tsc/Vite build通过，只有既有大包提示。初次Go测试因两项依赖缓存权限失败，补齐锁定依赖后重跑通过。未配置MySQL DSN，依赖实库的集成按约定跳过；本轮未做浏览器/生产验收。
- 交付依赖：081_total_time_output_speed.sql随Server迁移；平均输出统计需Agent更新。只读渠道来源本身不要求Agent变化。推送代码不代表发布或部署。
- 状态：整合和验证完成，待提交推送；源目录未覆盖、未清理。

## 提交与推送审批
- 已提交b6461feb8：58个文件，2552新增/215删除。推送前再次fetch远端无新增提交，origin/main为229c9036a，可快进；集成代码工作区干净。
- 推送未执行成功：自动审批拒绝git push origin HEAD:main，理由是涉及大量源码/迁移向共享默认分支出站，要求用户明确授权具体GitHub目的地。已向用户确认 https://github.com/yanceylv518/controlTower.git 的main，等待回复，未绕过。
- 下一步：取得具体目的地确认后推送，核对远端提交，更新交付结果。未发布部署。源工作区保留，不因本次集成交付自动清理。

- 推送成功（2026-09-17）：用户明确确认上述GitHub仓库main后，普通快进推送成功，远端由229c9036a更新到89f238a3d，包含业务提交b6461feb8和交付记录。之前审批阻塞已解除。未打包发布部署；源目录残留改动仍保留，不代表还有未推送的同一批业务代码。
