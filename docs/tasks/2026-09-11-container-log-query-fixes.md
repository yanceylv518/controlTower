# 容器日志查询错误处理与关键词修复

- 状态：代码已提交并推送 origin/main（4f8c8255）；真实 MySQL/线上验收待执行
- 更新时间：2026-09-11
- 工作目录：E:\projects\controlTower
- 分支：main
- 任务 ID：01a07f5e-3c11-77f3-b2c0-fe8b45ef813a
- 范围：容器日志详情读取、页面刷新错误、关键词匹配及打包前回归验证。
- 追加：修复 HTTP 下复制结果，优先 Clipboard API，回退用户点击触发的文本选择复制；两种均失败提供手动复制框。保留多行正文与来源标签，增加复制兼容回归。
- 复制修复结果（2026-09-11）：copyText helper 与容器日志页手动复制弹窗完成；HTTP/HTTPS/权限拒绝/均不支持共 4 项模拟回归通过，多行文本保持、清理临时元素并恢复焦点；类型检查和前端构建通过。测试已加入打包前脚本。本次未运行真实浏览器剪贴板验收，未部署；旧浏览器不支持兼容复制时仍可手动复制。
- 2026-09-11 追加范围：应用/Nginx 共用读取层按查询扫描建立有界内存时间块索引，不新增源库查询、不预扫全盘、不对压缩文件随机定位。验收需验证重复查询减少读取且结果一致、追加/轮转不漏读、乱序时间正确处理。
- 最新用户约定：相同条件先复用 CT 完整结果，不重复下发。已增加同用户同来源查询元数据匹配与接口短路；不同条件才利用 Agent 时间索引。最近 1,000 条候选上限、24 小时保留，首次并发未去重；不完整结果不复用。
- 索引实现：正常扫描记录约 1 MiB 时间块最小/最大时间与边界；最多缓存 64 文件/8,192 块。追加从最后一块继续维护，轮转/截断及快照失效时回退；压缩文件顺序读取。仅内存，不新增后台全盘扫描。
- 最新验证：verify-container-logs.ps1 再次通过（Go 包、前端 7 项测试、类型检查），前端构建通过。新增接口测试验证缓存命中不创建 Agent 任务；新增应用/Nginx、乱序/文件替换测试及原追加/游标测试通过。样本读取字节：应用 2,957,081→1,051,433；Nginx 3,288,837→131,589。非线上性能承诺。
- MySQL 结果复用集成测试已添加但未运行（无 CT_MYSQL_TEST_DSN）；没有宣称 SQL 在真实库验证通过。未部署。强制重查开关/并发去重/持久化索引不在本次实现内。
- 范围外：归档功能、告警与其他现有未提交修改；不部署生产。
- 验收：仅不存在/不可见返回 404；内部错误可定位且不暴露给浏览器；详情读取不执行清理写操作；页面临时失败保留进度并重试；关键词忽略大小写而请求 ID 保持精确匹配。
- 已读取项目上下文及已有两份任务记录；容器日志原会话此前未加载。worker_test.go 存在其他修改，避免改动该文件。
- 已完成：详情 GET 移除清理写操作并保留 24 小时可见性限制；仅 sql.ErrNoRows 返回 404，内部异常返回 500 并记录任务/用户及错误；超时清理使用 JSON 布尔类型；页面部分失败保留结果继续重试，404 终止该任务轮询；应用和 Nginx 关键词忽略大小写，请求 ID 不变。
- 验证：2026-09-11 执行 deploy/verify-container-logs.ps1，Go 指定包通过、前端 7 项回归通过、类型检查通过；pnpm --dir webapp build 通过（已有包体积警告）。
- 未验证：未设置 CT_MYSQL_TEST_DSN，真实 MySQL 集成测试跳过；未复现历史线上瞬时故障，不能确认当时的锁/连接/解码原因；未进行浏览器真实 Agent 联调。未提交、未部署。
- 发布门禁：部署前配置独立测试库并运行脚本 -RequireMySQL，缺少测试库直接失败；通过后再构建。详见 docs/container-log-queries.md。
- 下一步：完成真实 MySQL 验证，升级 CT Server/前端及 Agent 日志读取服务，复测同时间同关键词查询；如仍有内部错误按任务 ID 查新增日志。日志跳过及 startTime 异常尚无真实样本复现，不声称已修复。

- 提交交付（2026-09-11）：代码提交 4f8c8255 已推送 origin/main。为保留主工作区其他任务未提交文件，在 .tmp/container-log-publish 基于远端 6ecfaf2f 隔离合入；该检出相关 Go 回归通过。主工作区保留本地原提交 daa584d0 及其他任务改动，未强制同步。提交前前端 11 项测试和类型检查通过；真实 MySQL 与线上验收仍未运行，未部署。
