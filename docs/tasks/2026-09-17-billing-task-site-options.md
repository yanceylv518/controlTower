# 创建账单任务站点与对象列表修复

- 目标：消除同站点多实例导致的重复选项和实例 ID 误传，用户账单不请求无关上游列表。
- 状态：代码完成，专项回归及前端构建通过；未提交发布部署；更新时间：2026-09-17。
- 任务 ID：01a0ad0b-cba3-7c83-92f7-0ef73f50b877。
- 工作目录：E:/projects/controlTower/.tmp/billing-task-site-options；分支：codex/billing-task-site-options，基于本地 origin/main 1e5761b25。
- 文件范围：BillingTasksView.vue、账单前端回归、本记录、PROJECT_PROGRESS.md。
- 范围外：计费规则、数据库数据清理、其他任务代码、提交发布部署。
- 验收：可用站点按 site_id 去重，接口使用站点 ID；按当前账单类型加载对象；切换站点/类型/搜索及关闭后旧请求不回填、不误报。
- 初始化：已读取当前上下文、任务状态及账单相关记录；主工作区 main 存在大量既有改动，采用独立工作树保护。现有最新本地主线仍存在两项缺陷，未获取生产接口证据。
- 实现：沿用共享 siteOf 规则，过滤启用且具备只读配置的实例后按站点 ID 去重，标签和值统一站点 ID。打开、切换站点/类型只加载当前对象列表；切换清空已选对象；请求序号与同步监听防止旧站点/类型/搜索结果、错误及 loading 状态污染当前弹窗，关闭/卸载失效未完成请求。
- 验证：实际页面脚本 11 项回归通过（7 项新增对象列表测试及 4 项计费来源回归），覆盖多实例去重、同名不同站点保留、旧实例无 site_id 回退、只读/启用过滤、按类型请求、跨站点乱序、切类型旧错误、搜索乱序及关闭错误抑制。pnpm build（含 vue-tsc）通过，只有既有大包提示；git diff --check 通过。
- 限制：未运行真实浏览器/生产接口验收、Go/实库测试（仅前端变更），未提交发布部署。无法据截图判定原上游接口失败的具体服务端响应；本次消除无关调用及实例 ID 误传。
- 下一步：按用户安排交付前端（现有发布方式随 Server 包发布）；部署后在真实多实例站点验收。无需迁移或 Agent 更新。

- 2026-09-17交付整合：已纳入local-changes-integration统一验证，提交推送进度见[整合记录](2026-09-17-local-changes-integration.md)。历史状态不代表本次推送结果。
