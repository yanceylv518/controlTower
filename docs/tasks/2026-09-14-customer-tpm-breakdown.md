# 客户监控 TPM 模型与渠道拆分

- 目标：按用户展示模型/渠道的真实 TPM 构成，落实本会话确认的紧凑三列卡片与半透明叠加图。
- 状态：代码 d113c7dd 已提交并推送 origin/main；更新时间：2026-09-14。未打包、未部署。下方过程条目的未提交/部署描述为各轮当时状态。
- 推送交付（2026-09-14）：普通快进推送 origin/main 成功（2dc98ac4 → d113c7dd），未强推。主工作区分支和运行预览保持原样，其他任务改动保留；未将主工作区混合变更整体同步到远端。
- 提交验收（2026-09-14）：用户授权提交并推送；基于 origin/main 2dc98ac4 在 .tmp/customer-monitor-delivery / codex/customer-monitor-delivery 隔离整理本会话改动，保留主工作区其他任务。隔离检出 11 个相关 Go 包、31 项前端回归及类型检查/生产构建通过（既有大包提示），未运行实库/真实站点和竞态检测。提交范围为客户 TPM 拆分及性能保护、卡片/弹窗/提示、三个监控工具栏、可靠用户名与模型详情名称；无缓存目录、临时预览或其他业务改动，未打包/部署。
- 模型详情客户名称接续（2026-09-14）完成：前端原先错误地从模型列表中寻找用户名称，后端 instance_model_user 也未返回客户展示名。现在后端提取所属实例及最后 user ID，接入既有可靠名称解析；前端直接使用交叉维度行 display_name，旁边小号显示 #ID，旧 Server/缺失名称回退用户 ID。只扩展指标名称分支与 DimensionDetailView.vue，无新增前端请求，沿用站点名称缓存；客户/渠道详情模型名称不变。
- 详情名称验证：dashboard/httpapi Go 回归通过（新增真实 history handler 明细/汇总输出、含冒号及 user 片段的模型名、无名称/非法 ID）；3 项前端实际计算脚本回归通过（名称/ID/排序、旧服务兜底、客户/渠道模型名称保持）。类型检查、生产构建（既有大包提示）、指定 diff 检查通过，本地 5187 模块 HTTP 200 已移除错误查询；未实测登录浏览器或真实源站，未提交/部署。需要同时更新 Server 和前端，单独刷新连接远程旧 Server 的前端仍只能显示兜底 ID。
- 监控工具栏统一接续（2026-09-14）完成：用户追加将客户紧凑工具栏应用到渠道/模型监控；范围扩展到两页共用的 DimensionView.vue，修改前该文件无未提交改动。两层 tab 合并为约 52px 白底工具栏，左侧视图/指标切换、右侧筛选数量；排名模式隐藏指标切换，窄屏换行。保留原 TTFT 默认值和 TTFT/TPM/OTPS 顺序、筛选及数据逻辑，无新增接口请求，不涉及图层重做。
- 工具栏统一验证：已核对 /channels 与 /models 共用组件及原条件绑定，类型检查、生产构建（既有大包提示）、指定 diff --check 通过；本地 5187 模块 HTTP 200，已包含紧凑工具栏并移除旧 tab。仅低影响布局变更，未新增测试/重跑数据回归；未实测登录浏览器布局与切换，未提交/部署。
- 客户名称可靠来源接续（2026-09-14）代码完成：dashboard 优先读取已有站点只读连接的 NewAPI users 表，仅取 id/username/display_name，优先用户名、空值回退显示名。按站点共享 5 分钟内存缓存（实例到站点映射缓存 1 分钟），各站点独立锁合并并发；查询超时 2 秒，失败保留成功名称并等待 1 分钟再试，缺失时沿用日志/用户 ID 兜底。成功快照替换旧映射，可反映改名和移除；未配置连接视作失败，不清空成功快照。无新增用户列表 API，不改变权限或指标数值，无 Agent/迁移/前端改动。
- 名称验证：dashboard、httpapi、auth、Server 主程序 Go 回归均通过；新增无日志用户 104/105、同 ID 跨站点及多实例共享、改名/删除刷新、失败保留/恢复/负缓存、20 并发合并与跨站点互不阻塞测试；通过 database/sql 测试驱动验证三字段只读 SQL、用户名优先/显示名兜底/空值过滤、超时及部分读取失败；真实认证链验证明细/汇总均不泄漏未授权用户或站点。指定 diff --check 通过。竞态检测因 CGO 未启用无法运行；真实 NewAPI/MySQL/浏览器联调未运行，本轮未改前端故未重复前端检查。
- 名称交付限制：需要部署新版 Server 后，当前连接远程 8080 的本地前端才会取得修复名称；未部署、未提交，无需更新 Agent/迁移。每活跃站点缓存到期首次查询读取一次用户表，首次/到期请求可能等待最多查询超时，非零开销；新用户/改名最多约 5 分钟后按需更新。成功名称仅缓存在进程内，重启遇源站故障仍依赖日志/ID 兜底。范围为新增 customer_names.go 及两份回归、names.go 接入、mux.go 原只读连接提前初始化；保留 mux.go 已有其他任务 currency 路由。
- 放大弹窗接续（2026-09-14）完成：放大按钮改为 Element Plus 弹窗，支持关闭按钮、Esc 和遮罩关闭；标题显示客户/ID，保留最新 TPM、模型/渠道切换、图例/提示联动及时间状态。抽出 CustomerTrafficPanel.vue 共用展示，复用父卡片缓存/请求，无新增定时器或打开弹窗的重复请求；弹窗打开时背景卡片图表释放并保留占位，关闭立即销毁大图。弹窗打开时即使背景卡片离屏仍随原刷新更新，关闭后恢复离屏节流。仅客户组件/回归及文档，无后端改动。
- 弹窗验证：28 项客户前端回归、类型检查及生产构建通过（既有大包提示）；新增缓存复用、弹窗离屏刷新及关闭后停止离屏请求回归。本地 5187 卡片与新增面板模块 HTTP 200，弹窗模块热更新已核验；未实测登录浏览器的弹窗尺寸、Esc/遮罩关闭与焦点交互，未提交/部署。
- TPM 数值与曲线时刻接续（2026-09-14）完成：模拟数据复现顶部最新分钟 34.2M、曲线上一分钟 75M 的差异；总量与拆分分开请求，未对齐桶不绘制。卡片现在在加载或最新同粒度分钟未对齐时标注“数据更新中”，请求失败/旧样本分别提示“拆分更新失败”/“数据滞后”；曲线底部明确最后实际绘制的有效区间。保留原数值、刷新频率和 24h 的 5m 曲线/1m 头部口径，不拿 5m 均值核对 1m 数值。工具复用已有遍历和对齐集合，无新增接口/定时器或 Agent/Server 改动。
- 本轮验证：27 项客户前端回归通过（新增未对齐与补齐恢复、失败/滞后、5m 独立口径、最新零流量与旧缺口不误报），类型检查及生产构建通过（既有大包提示）；diff --check 无差异错误。本地 5187 卡片模块 HTTP 200 且包含新状态与曲线时间。未实测登录浏览器布局、未取得截图时刻远程接口；对齐仅表示两个查询快照一致，不证明上报全部结束，不承诺消除真实采集延迟。未提交/部署。
- 顶部工具栏实施接续（2026-09-14）已完成：按用户确认的默认白底整合栏，将原三行合并为约 52px 高的工具栏，视图/指标在左、客户数/TPM 排序在右，窄屏换行；排名视图隐藏指标切换及 TPM 排序，顶部时间/搜索保留。移除两层空内容 tab 与独立排序行，复用原状态及内容条件，保留无分页卡片和懒加载。仅修改 CustomerMonitorView.vue 及文档。
- 工具栏验证：类型检查、生产构建通过（既有大包提示），指定页面 diff --check 通过；本地 5187 模块 HTTP 200，已核验新工具栏及旧两层 tab 移除。此轮仅布局变更，未新增测试、未重复数据回归，未实测登录浏览器布局与点击切换；未提交/部署。
- 顶部留白设计（2026-09-14）：用户指出双层 tab 与数量/排序行造成大面积留白。本轮提供局部设计预览，建议将视图切换、TPM/TTFT/OTPS、客户数和排序合并为约 52px 的单条工具栏，小屏换行；顶部时间/搜索保留。预览含白底整合栏/无底色栏及间距选项，使用示例数据，位于线程可视化目录 customer-toolbar-compact.html。仅新增设计预览及记录，未改业务页面，未运行浏览器验收或业务测试；实现待后续设计反馈。
- TPM 卡片取消分页接续（2026-09-14）已完成：移除六张卡片切片及 TPM 分页器，全部客户纵向浏览，保留搜索与排序。卡片接近视口上下 400px 时加载/刷新，离屏释放图表并保留同范围数据，重新进入后按当前刷新代次补齐；无 IntersectionObserver 环境正常加载全部。排名明细表分页保留。23 项前端回归（新增离屏不请求、进入加载、离屏保留数据及重入更新）、类型检查与生产构建通过（既有大包提示）；本地 5187 HTTP 200，热更新模块已无 trafficPage。未实测登录浏览器长列表滚动，未提交/部署。
- 模型区域与提示框联动接续（2026-09-14）已完成：开启 ECharts triggerLineEvent，以真实折线/面积图形的 seriesIndex 映射稳定模型/渠道 key。鼠标移入即将提示对应行加粗至 700、浅色底纹及模型同色左边线，其余行 opacity 0.45 弱化；移出恢复全部行正常。直接更新已显示提示 DOM，支持同一分钟内纵向跨层而不依赖 formatter 重新执行；长列表自动在提示框内部滚动到目标行，排序不改变。图例 hover/focus 同样联动，组件卸载销毁图表事件和提示引用。
- 联动验证：含新增同桶换层/弱化恢复/特殊模型名及渠道精确匹配回归共 22 项通过；用户追加“其余弱化”后，6 项提示工具专项再次通过，类型检查与生产构建通过（既有大包提示）。本地 5187 工具模块 HTTP 200 并含对比样式。尚未实测用户登录页面的真实鼠标联动，未提交/部署；仅改客户图、提示工具和测试，无采集或后端改动。
- 提示框交互接续（2026-09-14）已完成：关闭客户 TPM 提示的 transitionDuration/displayTransition/showDelay 及轴指示线动画（本地 ECharts 在 transitionDuration 非零时有 50ms 位置节流）。HTML 提示挂至 body，取消图内 confine，优先固定在当前图右侧/左侧，其次上方/下方，并避开视口边缘。屏幕完全没有外侧空间时退到鼠标对角位置，pointer-events:none 防止阻挡图内移动；外置提示仍可滚动查看全部条目，长名称可换行。仅改 CustomerTrafficChart.vue、新增 customerTooltip.ts 及专项测试。
- 提示框验证：4 项定位回归（移动稳定、右边缘/底部、放大图上下布局、窄视口穿透回退）及已有 16 项回归共 20 项通过；类型检查与生产构建通过（既有大包提示）。本地模块 HTTP 200，已核验 transitionDuration:0 与 appendTo:body 热更新。浏览器连接工具重试仍失败，未在用户登录页面实测鼠标手感；无外侧空间时无法保证完全不覆盖图形，其他客户图可能被外置提示临时覆盖。未改数据、Agent/Server、刷新频率或其他图表，未提交/部署。
- 延迟反馈接续（2026-09-14）已完成：用户截图 17:50 仍展示 17:47。已确认此前客户图与头部额外等待桶结束后 95 秒，页面每 30 秒刷新，时间标注只显示桶起点。现移除该页面额外沉淀等待，只排除尚未结束的 1m/5m 桶；头部显示分钟起止区间，提示数值随采集继续补齐，距桶结束超过 2 分钟提示滞后。保留调权等模块的独立沉淀规则、Agent 采集频率及每 30 秒前端刷新。
- 延迟修正验证：16 项前端计算/异步回归、类型检查与生产构建通过（既有大包提示）；新增分钟关闭前/后边界、最新分钟选择、后续补报更新与当前未结束分钟排除验证。Vite HTTP 200，已确认热更新模块移除 95 秒等待。未测远程采集/入库延迟或登录浏览器端到端时延；只能确认移除前端人为等待，不能承诺接口总能提供刚结束的一分钟。未改 Agent/Server、未提交/部署。
- 真实流量图层反馈接续（2026-09-14）代码完成：用户指出图层零碎、断层，与设计图不符。原代码无用户记录的分钟一律 null；现在额外读取每实例同粒度总量，只有其 Token 及请求数均与全用户同桶小计一致且有请求时才确认覆盖。已覆盖且无该用户记录的桶归零，未覆盖/拆分未齐仍断开，真实峰谷及固定图层顺序不改；1h/6h 头部可采用已确认的最新零流量分钟，24h 头部仍沿用独立 1m 样本。模型图例移除固定 86px 截断，长名称自动换行。
- 本轮验证：15 项前端计算/异步回归、类型检查、生产构建通过（既有大包提示）；新增测试覆盖孤立峰值两侧已确认无流量的补零、未知采集/受限数据/总量或请求数不匹配不补零、跨实例隔离、5m 换算及最新零流量头部。Vite 模块 HTTP 200 并包含新逻辑，本地 5187 保持运行。浏览器连接工具两次失败，未读取用户登录态真实接口、未重跑真实浏览器；截图是否全部由该原因产生仍待登录页面复核。未改 Agent/Server、未提交/部署。
- 覆盖查询限制：每实例每轮新增一条已有 CT 指标历史查询，所有卡片共用结果，不新增 Agent 计算或 NewAPI 源库查询。权限拒绝/接口失败降级为不推断零流量，不影响原数据展示；全站无记录时不能确认采集覆盖，仍保留未知断点。
- 任务 ID：01a09ee4-7eb0-7b73-a512-fc4e9e1893ae；工作目录：E:\projects\controlTower；分支：main。
- 文件范围：客户监控页、新增 TPM 卡片/图表/数据处理与回归；Agent/Server 指标聚合器；指标显示名称及客户监控权限映射与对应回归；本文及 PROJECT_PROGRESS.md。
- 范围外：其他监控页/导航整体重做、TTFT/OTPS 口径调整、账单/通知/日志/调权功能、历史源库回扫、提交发布与部署。
- 设计确认：三列（窄屏自适应）；客户名称与 TPM 紧凑同排；每个客户独立模型/渠道切换；16%—40% 半透明渐变面积；图层按固定模型身份顺序叠加、颜色跨客户稳定；图例独立按所选时段流量排序，更多浮层列全部，不合并其他，不重复展示下方分布条；支持放大与搜索/排序。
- 数据约定：TPM 使用桶内 tpm/桶分钟数，1m/5m 明确区分；最近一分钟来自 1m 总量历史且标注采样时刻，不把 5m 均值或旧桶伪装实时；按用户/实例严格隔离；缺失采集不伪装为零，渠道升级前历史不补造。
- 已核对：读取当前上下文、任务记录、客户指标验收记录、README 和 Git 状态；其他任务多项未提交改动保留。用户×模型维度已存在，用户×渠道尚缺；通用存储支持维度扩展，预计无需迁移。
- 验收条件：同用户图层总和与总 TPM 对齐；固定图层/颜色与流量排序独立；完整图例无截断合并；切换与刷新无跨用户/站点旧响应污染；未知历史明确标注；Agent 与 Server 聚合路径、权限与前端计算回归通过，类型检查及构建通过。
- 已实现：Agent 聚合上报及 Server 原始日志聚合均新增 instance_user_channel，继续复用通用存储与 5m rollup，无迁移；客户监控权限放行该维度，viewer 继续按站点及用户集合隔离；服务端复用渠道名称缓存。
- 前端：TPM 默认展示，所有客户按最近结束分钟/时段总 Token 排序，全部卡片同页纵向浏览；每客户独立切换，接近视口加载当前维度、同轮成功缓存；固定身份排序/颜色与流量图例排序独立；更多浮层完整列出，不合并其他；16%—40% 渐变、紧凑头部、单卡放大；TTFT/OTPS 及排名页保留。
- 数据完整性：图层仅绘制拆分和客户总 tpm 对齐的桶；未采集/未齐桶留空并提示，不补造渠道历史，不把未知样本当 0（已验证覆盖的无流量桶例外）。图例平均 TPM/占比按拆分对齐的覆盖分钟计算。1m 与 5m 仅排除尚未结束的桶，不再额外等待 95 秒；刚结束的桶允许补报更新。头部独立使用 1m，显示起止区间，距桶结束超过 2 分钟提示滞后。
- 验证进展：12 项前端计算/真实组件脚本异步回归通过；Agent 聚合/主程序、Server 聚合/权限/dashboard/ingest Go 测试通过；类型检查及生产构建通过（既有大包提示）。浏览器真实 Vue 页面配模拟 API 验证三/二/一列、6 卡片、更多全图例、单用户渠道切换、放大、搜索、24h 图及独立 1m 头部通过，无页面脚本错误。
- 环境说明：默认 Go 缓存写权限失败后改用项目内 .gocache；沙箱无头浏览器启动失败后通过工具审批运行隔离测试配置。测试页面仅使用模拟接口，不访问生产；截图位于 .tmp/customer-tpm-check/。小屏仍受既有 AppShell 最小宽度限制，本轮没有修改共享导航壳。
- 限制：未运行真实 NewAPI/Agent/CT MySQL 联调；需要升级 Server/前端与 Agent 才能得到新增渠道数据。升级前或含无渠道/模型归属的流量无法补齐时段拆分，页面明确提示；不扫描源库补历史。
- 最终复验：紧凑 TPM 保留一位小数、稳定配色微调后，12 项前端回归、类型检查和生产构建通过；隔离浏览器复跑上述交互检查通过，无脚本错误。截图为模拟数据的实际 Vue 页面，不代表线上采集验收。项目摘要已同步。
- 下一步：按用户后续安排提交与发布；部署时同步 Server/前端及 Agent，并验收真实站点新增用户×渠道数据、采集延迟与历史缺口。
- 本地预览（2026-09-14）：按用户要求启动 Vite，地址 http://127.0.0.1:5187/，进程环境 CT_DEV_API_TARGET 指向用户指定的远程 8080 服务；未改持久代理配置。页面 HTTP 200，代理 `/api/auth/me` 返回 401（接口已连通，需用户登录）。已请求在 Codex 浏览器打开；未登录验收真实客户数据，未更新远程 Server/Agent。当前本地前端包含工作区已有改动，远端尚未提供的新渠道维度可能无数据。
- 性能接续（2026-09-14）已完成：Agent/Server 新增用户×渠道维度改为独立 Token 累加，以分钟/用户/渠道（Server 另含实例）结构键维护 int64 小计，维度字符串每组合只生成一次；不分配该维度的完整 accumulator、延迟样本、直方图，也不计算分位数/成功率/缓存/TTFT 等非 TPM 指标。保留零 Token 的实际组合。原有八类维度保留原逻辑，逐字段等价回归通过。
- 上报保护：Agent 主程序在追加渠道秒级速率之后检查 10,000 行限制；超限时整批移除新增用户×渠道拆分并记录日志，保留全部原有指标及速率覆盖标记。该批拆分不再补传，页面按总量核对后将缺口留空。若原有指标本身已超限，既有超限问题仍存在，本轮未改变其批次协议。9999/10000/10001/11000 边界及真实 buildReport 的 2000/3000 用户场景回归通过。
- 文件范围追加：Agent 主程序仅接入新增维度超限保护；Agent/Server 各新增 customer_channel.go；Agent 主程序与 Server ingest 新增专项测试。共享上报结构/重试协议/存储 SQL/采集频率未修改，没有新增源库查询。
- 本轮验证：`go test` 覆盖 Agent metricaggregator/reporter/主程序，Server aggregator/ingest/agentgateway/dashboard/auth/mysqlstore，通过；最终四个有新增测试的包以 `-count=1` 再跑通过。验证 TPM-only JSON 接收、分批合并、重试不重复、1m/5m 保真且不生成非 TPM 统计。本轮未改前端，未重跑前端构建或浏览器；没有运行真实数据库压测和线上联调。

## 性能基准（2026-09-14）

命令：`go test ./agent/internal/metricaggregator -run '^$' -bench '^BenchmarkCustomerTraffic$' -benchmem -benchtime=500ms -count=3`。Windows amd64 / i7-8700；合成同一分钟请求，5 个模型、最多 8 个渠道；三次耗时中位数。原八维与初版九维参考路径复用未改变的完整累加/排序逻辑。计时仅聚合，不含 JSON/gzip、网络、入库与前端；分配量是每批累计堆分配，不是常驻内存或峰值。原始输出位于 `.tmp/customer-tpm-perf/benchmark.txt`。

| 场景 | 原八维耗时 | 初版九维耗时 | 轻量九维耗时 | 原八维/初版/轻量分配 MB |
| --- | ---: | ---: | ---: | ---: |
| 1000 请求 / 20 用户 | 4.15 ms | 5.35 ms | 3.96 ms | 2.370 / 3.341 / 2.474 |
| 5000 请求 / 100 用户 | 19.29 ms | 23.60 ms | 19.71 ms | 11.362 / 16.227 / 11.828 |
| 1000 请求 / 1000 用户 | 8.64 ms | 10.69 ms | 9.07 ms | 6.659 / 9.016 / 7.330 |

轻量版较初版聚合耗时减少约 15%—26%，累计分配减少约 19%—27%；相对原八维额外分配约 4%—10%，耗时变化约 -4% 至 +5%，小差异受本机噪声影响，不代表零成本。新增部分分配比初版降低约 72%—90%。

序列化未压缩指标数组（原八维/初版/轻量，字节）：142454/286773/225102；365726/1089954/779262；2695092/3592776/3212985。实际发送沿用 gzip，未测压缩后网络增量。新增行数没有减少：154→314、394→1194、3017→4017；说明数据库成本取决于活跃用户×渠道组合，不能用 Agent CPU 基准推断数据库无影响。继续复用原表，不改共享存储和迁移；部署后仍需按真实规模观察。
