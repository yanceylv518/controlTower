# 通知渠道按站点和告警类型分流

## 熔断通知接续（2026-09-14）

- 状态：代码及提交检出验证完成，准备提交推送，未部署。复用调权事件，接入实际熔断与恢复的站点通知；系统规则保持现状。
- 范围：通知桥接、事件去重、通知分类、数据库迁移和回归；不修改调权引擎及其他活跃任务文件。
- 验收：仅 auto 且执行命令成功的事件通知；观察/失败/待执行不通知；按站点及类别隔离；重启不重复；恢复消息独立重试，普通告警扫描不误关闭熔断。
- 初始化以迁移时间为起点，不批量补发历史事件；已发生而没有新事件的旧熔断不自动补发。未部署。
- 实现：076 事件映射与启用边界；后台将 auto 且 command succeeded 的事件事务导入告警。恢复使用独立 resolved/info 告警及投递身份，关闭此前同站点/渠道熔断；普通扫描不干预生命周期。最多每轮导入 500 条，按匹配渠道及到期条件查询待投递事件，防止成功事件挤占重试。
- 验证（本轮实际运行）：dashboard/mysqlstore/ingest/server cmd Go 回归通过；127.0.0.1 的 control_tower_test 实库执行 076 迁移及生命周期集成测试通过，覆盖历史/观察/失败/待执行过滤、成功确认、重扫去重、恢复、重试退避及再次熔断。HTTP 模拟投递验证跨站点及类别隔离、恢复去重；前端 3 项分类回归和 pnpm build（含类型检查）通过，现有大包提示。改动 diff --check 通过。
- 限制：未执行真实 NewAPI/机器人/线上浏览器联调；未生成发布包。需要升级 Server/前端并执行 076，Agent 无新增要求。事件映射持久保留；已发送到外部但本地记录失败仍受既有至少一次投递语义影响。若调权动作成功但原始事件记录丢失，本桥接无法补造该事件。
- 提交验证（2026-09-14）：基于 origin/main 823228629 在 .tmp/notification-routing-publish 隔离整理，相关 Go 回归、本地 MySQL 熔断生命周期实库测试、前端 3 项分类回归及生产构建再次通过；主工作区其他任务改动未带入。
- 下一步：按用户安排提交发布，部署后在各站点通知渠道选择系统告警/渠道熔断，验证一轮真实熔断及恢复。


- 状态：85352053 已推送；追加配置简化与站点币种发布验证完成，未部署
- 更新时间：2026-09-11
- 工作目录：E:\projects\controlTower
- 分支：main
- 任务 ID：当前会话未取得
- 范围：通知配置、投递路由、站点筛选、075 迁移、前端与相关回归。
- 范围外：Agent 独立企微推送、其他账单/日志任务、生产部署。
- 验收条件：单渠道只绑定一个站点；站点与类型同时匹配才投递；列表和记录随站点切换；旧渠道待分配；编辑保留密钥和 Webhook。

## 已完成及证据

- 2026-09-10 本会话完成站点和类型路由、旧渠道待分配、编辑入口、接口及迁移。当天 go test ./server/...、前端 typecheck/build 通过；MySQL 实库测试因未配置 CT_MYSQL_TEST_DSN 跳过。以上为上一轮结果，本轮尚未重跑。
- 2026-09-11 已读取项目当前上下文及全部三份任务记录；现有其他改动不纳入本任务成果。无通知路由既有任务记录，现补登记并沿用。

## 当前步骤

- 用户追加：渠道按余额/系统/请求三类选择，阈值也按同样三类分块；移除余额生成、通知总开关、仅余额推送三个全局开关，保留用户级余额预警与渠道启用。
- 本次范围增加 SettingsView、告警分类共享工具及三个旧开关的运行时退役；保留具体告警规则与阈值，旧渠道选择不自动扩大，编辑保存时按新类别选择。
- 用户追加显示简化：移除全局 Quota 换算率与货币符号配置，前端和余额通知读取所属 NewAPI 站点；复用只读 options 定价解析。账单历史快照及生成流程范围外，不修改。读取失败前端显示未获取金额，余额通知回退明确标识的原始 quota。

- 启动本地 API-only 后端及前端，检查通知页面及站点切换；禁用后台任务避免预览触发真实通知。
- 仅使用本地测试配置，不记录真实连接串或凭据。
- 下一步：核实可用端口、本地数据库和浏览器页面，回写实际结果及限制。

## 本轮本地启动结果（2026-09-11）

- 本地前端 http://127.0.0.1:5173/notifications 已在应用浏览器打开并保留；后端 http://127.0.0.1:18081/healthz 返回正常。
- 使用现有本地 127.0.0.1 MySQL 的 control_tower_test 测试库；API-only 启动日志确认 operational runners 关闭（billing worker 按现有实现仍启用），不运行告警投递。
- 通知渠道新接口真实读库成功，unassigned=true 返回 2 条旧渠道；未分配、未修改渠道。此次不等于完整 MySQL 集成测试通过。
- 浏览器需要登录；项目旧文档测试账号登录失败一次，未继续猜密码。已清空密码框，等待用户在本地页面登录后验收列表、编辑表单及站点切换。
- 启动日志位于 local/runtime/notification-preview/；本轮未重跑完整 Go 测试或构建，未部署生产。

## 提交交付（2026-09-11）

- 85352053 已推送 origin/main。基于 aeb08017 在 .tmp/notification-routing-publish 隔离检出，只带入通知路由相关代码及共享 API 的通知改动，未带入其他任务未提交内容。
- 本次隔离检出 go test ./server/... 与 pnpm --dir webapp build 通过（含类型检查，存在现有包体积提示）；未配置 CT_MYSQL_TEST_DSN，实库集成测试跳过。
- 主工作区保留原有分支及混合改动，未重置或强制同步。远端包含代码、测试、075 迁移、接口文档和任务记录。
- 下一步：部署时同步升级前后端并执行 075 迁移，分配旧渠道站点，完成浏览器及真实通知验收。本次未部署。

## 配置简化与站点金额显示（2026-09-11）

- 阈值按余额/系统/请求三块展示；通知渠道只选三类，仍存储底层规则列表。旧的部分规则范围不自动变更，编辑明确提示保存会覆盖为类别的全部规则。
- 移除三个全局开关及全局 Quota/币种输入；设置注册表不再返回这些键，旧数据库值和环境变量不参与运行时计算。
- 新增只读站点币种接口，复用 NewAPI options 与现有定价解析，支持 USD/CNY/CUSTOM/TOKENS 和站点显示汇率。前端切站点先清空金额并忽略旧响应，失败显示“—”；余额通知按所属站点格式化，无法读取则明确显示原始 quota。使用日志单价跟随站点显示单位，历史账单快照链路未改。
- 指定 dashboard/settings/auth/httpapi Go 测试通过；包含真实 HTTP 投递测试验证遗留全局开关不再阻止发送，币种解析和余额消息格式测试通过。前端四项分类/站点竞态回归及最新 build（含 typecheck）通过；现有大包提示仍存在。
- 未运行真实 NewAPI 站点配置联调、完整浏览器验收和本次实库集成测试；未自动重启此前本地后端。需同步前后端使新接口生效，无新增数据库迁移（原站点分流仍需 075）。追加改动未提交、未部署。

## 本次发布验证（2026-09-11）

- 用户要求提交推送；在 .tmp/notification-routing-publish 基于最新 origin/main 49d512e3 整理，只提取本次配置简化与币种变更，保留其他任务的账单字段和保留期调整。
- 该隔离检出 go test ./server/...、四项前端分类/币种回归、pnpm --dir webapp build（含类型检查）全部通过。真实 NewAPI 联调和本次实库集成未运行；已有包体积提示，不阻止构建。
- 已读取新增的上一版验收记录 docs/review-notification-routing-2026-09-11.md；该记录对应 85352053，不作为本次追加改动的验收证据。未部署。
