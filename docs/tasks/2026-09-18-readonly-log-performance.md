# 使用日志查询性能优化

- 目标：按列表优先、游标分页、统计减负、展示补查缓存及汇总调度顺序优化使用日志查询。
- 状态：四项补修及详情列收紧完成，已提交推送并快进合并origin/main；5200前端预览保留，未发布部署；更新时间：2026-09-18。
- 任务 ID：01a0b32d-a588-7670-80fb-22a4561b7ad3。
- 工作目录：E:/projects/controlTower/.tmp/readonly-log-performance；分支：codex/readonly-log-performance，基于 origin/main 7bd0d09e8（含 rc122）。
- 文件范围：日志前端/API、passthrough 查询及缓存、小时汇总 runner、专项回归、接口文档和本任务记录。
- 追加范围：用户反馈使用日志详情列过宽，仅收紧同一页面桌面列宽及摘要上限，保留移动卡片和完整详情入口；验收样式约束、既有页面回归及类型检查。
- 交付授权：用户要求“提交推送本地代码”，随后明确“合并到main”；提交本任务的代码、测试与文档，推送origin/codex/readonly-log-performance并合并origin/main。
- 范围外：NewAPI DDL、Agent、归档账单设计、生产部署及版本发布；不采用可能漏查的时间转 ID 窗口。
- 验收：权限/站点/筛选隔离不退化；顺序游标与跳页兼容；统计缓存有界并可取消、失败不缓存；列表优先；汇总站点并发有界；相关 Go/前端回归、类型检查和构建通过。
- 已确认：主目录与主题目录有既有改动；最新整合已发布 rc122，以新隔离工作区实施，不覆盖源目录。生产查询耗时、执行计划和索引未实测，不承诺提速倍数。
- 已完成：列表优先、取消旧统计；相邻页使用时间+ID游标，跳页/旧Server兼容；固定窗口统计5秒缓存与并发合并（256条），有效scope/连接配置隔离，全部等待者取消才终止共享查询，失败不缓存；渠道名1分钟缓存（2048条），补查各1秒预算；最多两个站点并行汇总、同站点不重叠。
- 保留：Viewer 去重、模糊/请求ID过滤继续查询原表；汇总为空的整段回源仍保留，以免将覆盖异常当零条。没有直接复用不等价的小时计数，也没有用时间推算ID范围。
- 验证：228项前端测试通过、vue-tsc及Vite生产构建通过（既有大包提示）；dashboard/httpapi/auth/mysqlstore/server cmd Go test与vet通过；diff检查通过。新增受控Promise前端查询顺序/游标回归、缓存并发/过期/取消/错误/范围隔离、SQL同秒边界、渠道缓存与汇总站点并发回归。渠道缓存测试首次因测试驱动缺少只读BeginTx失败，补全驱动后相关包重跑通过。
- 限制：未配置CT_MYSQL_TEST_DSN，实库集成未运行；未运行浏览器视觉验收、生产EXPLAIN/性能基准或竞态检测；不能承诺具体提速倍数。缓存会引入最多5秒统计、1分钟名称滞后；跨页不是数据库快照。未修改NewAPI DDL/Agent，无迁移。
- 交付位置：业务提交00c14b2c274d7f9130e5bde37a0be7c818fdc258已推送origin/main及origin/codex/readonly-log-performance；5200使用本目录前端，仍代理原远程后端。未发布或远程部署，没有调整5201；下方历史条目的未提交描述为当时状态。
- 下一步：用户在5200验收详情列；后续按需交付Server/前端，在真实MySQL上验证性能与补查场景；无需NewAPI DDL/Agent升级。

## 提交推送验收（2026-09-18）

- 合并前获取远程状态，origin/main为7bd0d09e8，与本任务基线一致；提交范围为本隔离目录的19项日志相关代码、测试与文档，保留其他工作区。
- 提交前再次执行pnpm --dir webapp build，最终详情列CSS版本的vue-tsc/Vite生产构建通过，仅有既有大包提示。此前232项前端回归、5个Go包test/vet及详情列28项回归已通过；本轮未重复这些测试，未运行实库/浏览器/生产性能验证。
- 业务提交00c14b2c274d7f9130e5bde37a0be7c818fdc258已推送功能分支；用户追加合并main后再次获取远程状态，确认main为当前HEAD祖先（0落后/1领先），以非强制快进推送将origin/main更新至该提交，无冲突。
- 主工作区main仍为旧提交且存在既有混合改动，未重置或切换；本隔离分支完成远程main交付。任务与项目摘要同步更新；未创建版本标签、发布安装包或部署，5200预览保留。

## 详情列收紧与5200恢复（2026-09-18）

- 详情列桌面宽度设为160px，摘要140px（原上限360px）；≤1320px时列128px/摘要108px。保持单行省略、悬停全文、点击原详情弹窗；移动卡片宽度不变。仅3处CSS规则修改。
- 本轮28项日志页面回归及vue-tsc通过，diff检查通过；未重复生产构建/全量回归/Go检查、未做真实浏览器布局测量。
- 用户反馈5200不可用；检查时端口无监听，未发现本会话停止该服务的操作，缺少退出日志，无法确认停止原因。未停止其他进程。
- 从本隔离目录重新运行Vite于127.0.0.1:5200，CT_DEV_API_TARGET沿用http://124.220.201.99:8080，执行会话45030。页面HTTP200，代理/api/auth/me为401（未带登录Cookie），新CSS响应200且含160/108px规则；未升级远程Server/写远程业务数据。
- 本地5200当前可查看新前端/收紧样式；服务端查询优化仍须交付远程Server后生效，不能把前端恢复当后端已部署。

## 复审问题修复结果（2026-09-18）

- 分页：成功响应保存其requestCursor；翻页/改页大小失败将游标、offset和limit一并恢复，错误提示改走reloadPage。新查询失败不拿旧查询快照覆盖；晚到的分页不回滚新条件。
- 重试：新增fallback_checked，成功查证才标明已确认，超时/部分读取失败保持未知，已有肯定重试证据保留。桌面/移动端对具备请求标识的未知记录显示“待确认”并支持独立关联查询；渠道ID缺失也可查看，Viewer权限保持。
- 优先级：前端翻页暂停并取消尚未结束的stat/count，列表返回后恢复被暂停的请求，过期响应不能恢复；Server按站点连接池限制统计并发为1，避免多个用户的统计占满两个只读连接，等待支持取消。该策略不承诺数据库锁/其他业务查询不存在竞争。
- 审计：移除共享计算内审计，由包装层按每个HTTP调用记录一次；成功/失败/取消按实际响应区分，缓存命中和合并等待者仍保留各自审计，不再重复计入冷查询。保留原操作类型与总数信息。
- 本轮验证：232项前端回归、vue-tsc/Vite构建通过（既有大包提示）；dashboard/httpapi/auth/mysqlstore/server cmd测试及vet通过；diff检查通过。新增分页失败直重试/深页与改大小恢复、统计暂停恢复/新查询竞态、未知状态独立加载；Go测试使用SQL驱动覆盖真实handler冷/热/失败审计、合并调用取消、超时/部分结果未知、统计占用时第二连接可用。
- 过程：全量前端测试首次有一项旧静态断言不接受新增未知状态，更新权限条件断言并保留行为测试后全量通过；待确认按钮改自适应宽度防文字挤压，最终构建包含此修正。
- 限制：本轮未运行真实MySQL集成、生产EXPLAIN/性能基准、浏览器视觉验收或race检查。源码/驱动/页面逻辑测试不冒充线上证据；未提交推送、打包或部署。

## 改动影响复审（2026-09-18）

- 用户已授权修复上述问题；本轮继续原任务/隔离分支，补修分页与重试状态、限制统计并发并恢复中断的统计、统一按实际响应审计；修改前已重读当前上下文与工作区状态，无新增重叠任务。

- 本轮只读审查业务代码，未修复业务实现。主/隔离工作区与原预览保持现状；将已确认问题回写本记录。
- P2已动态复现：相邻页查询失败后 reloadPage 恢复 offset/limit，却保留失败请求的 pageCursor；页面错误提示直接调用 state.reload，导致第2页游标搭配offset=0，返回第2页内容却标第1页。执行实际页面逻辑及useAsyncData的受控Promise复现，输出确认该组合与错误页码。
- P2静态确认：新增1秒重试补查超时仍沿用静默返回；对原日志没有显式fallback/多渠道信息的请求，查询失败后fallback仍为false，前端隐藏重试链路入口。原始日志不丢失，但未查明与确实没有重试被混淆；需未知状态/独立加载入口。
- 性能限制已动态核对：第1页返回后统计/总数同时运行，此时翻第2页未取消或降优先级，两者仍可能占满两个连接；首屏优先并非Server级或后续翻页优先。不同用户查询之间也无优先调度。
- 次要问题：缓存包装层不检查响应状态就调用audit，而audit固定记录succeeded；冷请求还会与原handler重复记录审计。不会改源日志，但新增审计结果可能失真并增加写入。
- 预期影响：固定窗口统计结果额外缓存5秒（并非数据绝对新鲜度上限），渠道名称缓存1分钟；相邻翻页按游标稳定边界，任意跳页仍按OFFSET，持续写入/删除下并非统一快照；双站点并行提高追赶机会，也会提高源库/CT库瞬时负载。
- 未发现此次改变原始日志写入、扣费字段或现有scope过滤。权限与缓存隔离仅基于代码及前轮回归判断，不替代实库/线上验证。
- 本轮验证：重跑7项现有查询测试全通过；另以不修改源码的Node脚本复现失败后直接重试错页及翻页时统计仍活动。未重跑完整228项、Go测试/构建，未做MySQL/生产性能验证；前轮结果不作为本轮新增场景通过的证据。
