# 账单计费来源与多媒体用量拆分

- 目标：新建用户/上游账单默认直接采用 NewAPI 日志扣费，可选重新计费；普通输入与图像/音频输入输出分开展示。
- 状态：代码完成、回归通过；未提交/发布/部署；更新时间：2026-09-15。
- 任务 ID：01a0a5ad-62f3-7580-846a-5dd7dea531d8。
- 工作目录：E:/projects/controlTower/.tmp/billing-source-mode。
- 分支：codex/billing-source-mode，基于 origin/main a1a46283a。
- 文件范围：billing、dashboard 账单接口、mysqlstore、迁移、账单前端、相关测试与文档。
- 范围外：NewAPI/Agent 修改、自动重生成历史账单、发布部署。
- 验收：默认源日志金额、不依赖重算参数；可选重算兼容原行为；模式持久化/查重/重启一致；普通与多媒体用量经明细及汇总正确保留，文件列与页面对齐；相关回归通过。
- 协作：主工作区已有多项修改，隔离检出保留；每日下载任务已发布 rc116，本次基线包含其修复。其他任务记录未更改。
- 实现：新建请求 recalculate 缺省 false，默认直接采用日志 quota / QuotaPerUnit（有理数、12 位金额），不重算单价/倍率/表达式；显式 true 保留旧重算与缺价兜底。来源/用量版本持久化且参与查重，Claim/Publish/读取均保留。旧任务迁移默认 recalculate/version 0，旧账单不重写；新版前端检测 Server 能力，避免旧接口静默忽略选项。
- 用量：普通输入/输出与图像/音频输入输出四列拆分，经磁盘暂存、compact 汇总、区间/每日/令牌预览和 XLSX/渠道 CSV 保留。单独定价音频读取兼容 audio_input_token_count。默认模式分项单价显示“未拆分”，不伪装零价；无需扫描明细解析单价。上游渠道折扣和原订单筛选规则保留，页面/文件标明计费来源。
- 迁移：080_billing_source_and_multimedia.sql；无全表历史回填，需要 Server/前端升级，Agent/NewAPI 不改。
- 验证：全量 go test ./... 通过；最终三个相关包 go test/go vet 通过；独立本地 MySQL 8.0 专项实测通过（迁移双重放、查重、列表/读取、Claim、Publish、模式持久化、跨页累加、Token/模型汇总及旧默认值）。新增运行器→gob 暂存→JSON 分流→XLSX 回归、缺价/无效表达式采用原金额、重算/旧模式差异保留、API 默认/选项查重、四个工作表数值及音频字段回归通过。
- 前端验证：87 项脚本回归（含 4 项本功能）通过，类型检查/生产构建通过；仅既有大包提示。新增任务默认、显式重算、不同模式/旧版本区分、旧 Server 拒绝不兼容保存均覆盖。
- 限制：未运行真实浏览器/Excel 应用及生产账单验收；未提交、发布或部署。多媒体无可信日志字段不估算，历史账单需新建任务才有拆分；源字段重叠时不能用各列简单相加反推日志原始总数。
- 设计与接口：隔离目录 docs/design-billing-source-mode.md。
- 下一步：按用户安排审阅、提交及发布；上线前核对迁移编号是否有并发新增。

## 提交交付（2026-09-16）

- 用户授权提交并推送；目标 origin/main（https://github.com/yanceylv518/controlTower.git）。fetch 后主线仍 a1a46283a，无新增提交或 080 迁移冲突。
- 本轮业务代码未变，沿用前轮已通过的 Go、MySQL、前端验证，本轮仅执行提交范围与 diff 检查，未重复测试。
- 准备提交本功能及文档，保留主工作区其他任务改动；未发布部署。
