# 验收记录：站点级只读容器日志查询（2026-09-08）

- 范围：`43f94ae feat(logs): add site-scoped read-only application log queries`
- 结论：**通过，验收修正一处 P2**（成功结果携带陈旧错误文案）。设计说明
  `docs/container-log-queries.md` 与实现一致，安全边界核对无误。

## 变更概要

- 新增高权限本机组件 `control-tower-log-reader`（root，仅监听本机 Unix socket，
  root:ct-agent 0660），只提供 `GET /containers` 与 `POST /query`；以固定参数执行
  `/usr/bin/docker ps -a` / `docker inspect --format`（投影不含 Config.Env），只识别启动
  程序为 new-api 的容器（或本机名单），按 `--log-dir` 解析并要求目录为 bind/volume 挂载；
  文件读取用 `os.OpenRoot` 限定在发现目录内、跳过符号链接、只读 `.log`/轮转/`.gz`，
  扫描/行数/字节/深度/超时上限齐全；回传前脱敏 Bearer、api key、password、cookie 等。
- Agent 配置 `CT_CONTAINER_LOG_SOCKET` 后独立 5 秒轮询 Server 领取任务、经 socket 执行、
  下轮回传；Server 网关只接受实例专属 Token（全局 Token 401）。
- Server：069 迁移两表（targets / tasks）；任务 24 小时清理、排队 2 分钟/领取 90 秒超时；
  结果只有查询人本人可读（全权限管理员也不能凭 ID 读别人的）；审计 `logs.query` 只含
  身份与条件不含正文。新权限 `logs.query`（与"使用日志"分开）。
- 前端"数据查询 → 容器日志"：跟随站点、默认最近 15 分钟、关键词/Request ID/错误码
  AND 匹配、历史抽屉。

## 缺陷与修正（P2）

**成功结果携带"本机日志读取服务不可用"**：worker 把 reader 的回复解码进一个预填了
失败状态与错误文案的 `Result`，reader 成功时省略 `error` 字段（omitempty），陈旧文案
原样留下并随结果上传——页面在"已完成"的结果上方同时显示红色错误条。修正：解码进
全新结构，请求失败或回复不完整才回填失败结果；回归 `TestExecuteQueryUsesFreshResult`。

## 实证

- `go vet`、`go test ./...` 全绿（含真库任务领取/隔离/回传/超时/审计）；
  `pnpm typecheck`/`build` 通过；069 首启套用（编号跳过 068）。
- **Server↔Agent 端到端**（本机无 docker：用真实 `Reader`/`ReadFiles`/`Redact` 代码、仅把
  docker 发现替换为指向临时目录的假清单）：签发 demo_1 实例 Token，真跑 agent →
  目标注册；灌 6 行 new-api/GIN 风格日志 + 1 个 gz 归档 + 1 个指向 /etc/passwd 的符号链接：
  无筛选返回 6 行、扫描 2 个文件（符号链接跳过、归档因时间范围排除）、Bearer/password/
  cookie 均 [REDACTED]；`request_id=req-abc123` 精确 2 行、`abc123` 部分匹配 0 行；
  `error_code=503` 命中 GIN 状态位与 `error_code=` 字段共 2 行、`900`（耗时）0 行；
  关键词与错误码 AND 正确；跨度 3 小时 400、过期 source id 409。
- 隔离与鉴权：另一个持 `logs.query` 的管理员读他人任务 404、自己列表为空；无
  `audits.read` 读审计 403；审计正文不含日志行；全局 Agent Token 轮询 401。
- 停止 reader：下一轮上报 discovery_error，目标不可用、提交 409；恢复后自愈。
- 页面 1366：来源计数、查询结果代码块、已扫描文件数渲染正常；修正前复现红色错误条。

## 记档（P3）

- `logs.query` 可查所有站点的允许容器，站点下拉只是目标选择，非授权边界（文档已写明）。
- 时间选择器按浏览器本地时区，结果行保留日志原文时间（reader 按 Asia/Shanghai 解释
  无时区时间）；浏览器不在东八区时两者会不一致。
- 每次查询至少两轮轮询（领取一轮、回传一轮），常态 5–10 秒。
- 本机无 docker，真容器发现（`docker inspect` 投影、挂载解析）只由单测覆盖，
  上线后按文档在 Linux Docker 上做一次已知日志检查。

## 部署要点

- 先 Server + 前端（069 自动套用），再 Agent；**本批动了 Agent**，Agent 包新增
  `control-tower-log-reader`、service 与安装脚本；仅更新程序不会启用，需在目标服务器
  `sudo bash ./install-log-reader.sh` 并给 agent.config 加 `CT_CONTAINER_LOG_SOCKET`。
- 该实例的 Agent 必须使用实例专属 Token。
- rc99 打在 000aff2 不含本批，上线需重打 rc100（server + 前端 + agent，069 迁移）。

## 追加：时区按日志来源解释（2026-09-08，用户令，P3 修复）

- 原页面时间选择器与历史时间用浏览器本地时区，结果行保留日志原文时间（reader 按
  Asia/Shanghai 解释无时区日志）；浏览器不在东八区时两者错位。
- 修正：页面以站点内日志来源自带的 `timezone`（默认 Asia/Shanghai）为准——选择器改为
  该时区的墙钟字符串（`value-format`），提交时按该时区换算成 UTC；结果说明与历史记录
  的时间同样按该时区显示；选择器旁标注 `Asia/Shanghai (UTC+08:00)`，站点内来源时区
  不一致时标黄提示按第一个来源解释。新增 `utils/zoned.ts`（初版使用 Intl 两次求偏移；DST 边界缺陷及修正见下文）。
- 实证（无头浏览器时区为 UTC）：选择器显示 14:35–14:50 并带时区标签，提交的任务
  from/to 为 06:35Z–06:50Z，返回的两条日志时间 14:44/14:46 落在区间内，结果说明
  按东八区显示。`pnpm typecheck`/`build` 通过。
- 只改前端，随本批进 rc100。

## 后续复核：时间区间保留与 DST 校验

- 来源离线或切换站点导致参考时区改变时，将原区间换算为新时区显示，保持起止 UTC
  时刻不变，不再重置为最近 15 分钟。无法无歧义转换时保留原区间及时区并明确提示。
- 墙钟时间转换通过候选偏移与反向转换核对，拒绝无效日期、春季跳时中不存在的时间、
  秋季回拨中重复的时间，不再静默猜测。重复时间暂不提供选择第一次/第二次的入口。
- 自动回归命令：`node --test webapp/packages/desktop/tests/zoned.test.mjs`；覆盖东八区、
  纽约跳时与回拨、Lord Howe 半小时切换、Apia 跳过日期及历史区间转换保留。

## 追加：2f718b5 区间保留与 DST 校验复验通过（2026-09-08，零缺陷）

- 修的是我 d920702 的两处不足：①来源时区变化（切站点、来源离线）时我把区间重置为
  最近 15 分钟，丢掉操作者已选范围——改为按瞬间换算到新时区保留，不能无歧义换算时
  保留原区间并提示；②我的两次求偏移在夏令时缺失/重复时段会静默猜一个——改为候选
  偏移 + 往返校验，无效日期、跳时缺失、回拨重复分别拒绝并给出中文提示。
- 复验：`node --test webapp/packages/desktop/tests/zoned.test.mjs` 6 条通过（东八区、
  纽约跳时与回拨、Lord Howe 半小时、Apia 跳日、区间换算保留）；`pnpm typecheck`/`build`
  通过；UTC 浏览器端到端重跑：选择器东八区墙钟、提交 UTC 正确、结果按东八区显示。
- P3：回拨重复时段暂无"选第一次/第二次"的入口，只能换一个不重复的时间。
- 只改前端，随本批进 rc100。

## 追加：a7ecab1 索引化分批自动扫描、范围限 72 小时（2026-09-08 复验通过，验收修正一处 P2）

### 变更
- reader 内建内存稀疏时间索引（只记区段起止偏移与时间极值，最多缓存 32 个文件，不上传
  CT）；查询先建文件大小快照，每批最多 64 MiB / 256 文件 / 约 20 秒后保存内部进度并返回
  游标，Agent 自动续查并上传**累计**结果，用户只提交一次；同一任务 ID 贯穿全部批次。
  追加型日志复用已索引前缀并校验文件身份/大小/mtime/首尾样本；gzip 保留解压状态逐批
  继续。进度保留 10 分钟、每 reader 最多 16 个活跃查询；新增 `POST /release`。
- 查询范围 7 天→3 天；排队上限 2 分钟→1 小时；整个任务 10 分钟超时；租约仍 90 秒。
  游标只能来自 Agent，浏览器提交带游标一律 400。systemd 加 Nice/CPUWeight/IOWeight/
  IOSchedulingClass=idle 压低首次建索引的资源占用。

### 缺陷与修正（P2）
**租约过期丢弃已上传的部分结果**：running 任务 90 秒未续期时，过期语句把 result_json
整体覆盖成空行数组，几分钟里逐批上传的部分行全部丢失，与"累计上传、不把不完整结果当
没有日志"的设计相悖。修正：pending 任务仍整体覆盖；running 任务改用 `JSON_SET` 只改
status/complete/truncated/error/note，保留 lines（JSON 异常时回退整体覆盖）。真库回归
`TestContainerLogsLeaseExpiryKeepsPartialLines`。

### 实证
- `go vet`、`go test ./...` 全绿（containerlogs 包 16 条含 140 MiB 中段命中、索引复用与
  追加、gzip 跨批、进度过期与轮转失效）；`pnpm typecheck`/`build` 通过。
- **端到端**（真实 Reader/索引代码 + 假 docker 发现，真跑 agent）：生成 142 MiB、时间
  跨 2.5 天、目标 3 行位于文件正中（74 MiB 处，头尾各 64 MiB 均覆盖不到）的日志：
  第一批 5 秒返回 running（已索引 64 MiB，0 行），第二批 10 秒 succeeded、3 行全部命中；
  同文件第二次查询命中索引缓存，只扫 4 MiB、一批 5 秒完成。页面显示分批进度与累计读取。

### 记档（P3）
- 首次建索引会读完整个文件（142 MiB 约 10 秒，含两批），生产大日志的 I/O 竞争需按文档
  在联调时测量；调度优先级只是降权不是限速。
- 返回顺序为扫描顺序，未按时间统一排序；多来源不合并。
- 进度只在 reader 内存中，reader 重启或 10 分钟过期后需重新查询（从头解压 gzip）。

### 部署
- server + 前端 + Agent + reader；**service 文件有改动，需重跑 `install-log-reader.sh`**
  重装 service 以套用低调度优先级；无新迁移。rc100 打在 c072f41 不含本笔，上线需重打 rc101。

## 追加：文件级时间剪枝（2026-09-08，用户指出，已实现）

- 用户质疑"不需要把所有文件都建一遍索引"。核实：原实现对目录内最多 256 个文件不看
  查询窗口一律全量建索引，只在建完后按区段时间跳过；轮转保留多天的目录首次查询会
  把所有历史文件读一遍，这是首次建索引 I/O 高的主因。
- 实现：`snapshot()` 建文件快照时按两条规则剪枝——修改时间早于窗口开始的文件
  （追加型日志不可能再含更晚的行）、首条可解析记录时间已晚于窗口结束的文件（读开头
  64 KiB，gzip 解压开头一小段），各留 1 小时容差吸收乱序与时钟漂移；被剪的文件不建
  索引、不计入扫描；完成说明标注"按文件时间跳过 N 个文件"。回归
  `TestSnapshotPrunesFilesOutsideWindowBeforeIndexing`（旧 gz、未来文件被剪，重叠文件
  仍命中）。
- 实证：142 MiB 当前日志 + 3 个 5 天前的 40 MiB 轮转文件，查最近 15 分钟：只索引当前
  文件（扫描 145 MiB、files 1），说明"跳过 3 个"，10 秒完成。
- P3：当前文件本身仍是首次全量建索引（时间跨度大的单个大文件无法按文件剪）；若要再
  降，可考虑对"窗口在文件尾部"的情况从尾部倒序建索引，属后续优化。
- 只改 reader（agent 包），随本批进 rc101。


## 追加：文件剪枝复审修正（2026-09-08）

- 撤销 `5f21e908` 使用 mtime/首条记录加一小时容差跳过整文件的规则。两者都不能证明
  全文件的事件时间范围；乱序记录以及文件时间与内容时间不一致均可导致有匹配却返回
  Complete=true、空结果。新增反例测试确认修复。
- 普通文件仅在完整索引通过文件身份/快照校验后，按所有区段的时间极值判断是否可跳过。
  首次索引仍分批建立；已有索引可以直接跳过不重叠文件，追加后重建末区段再判断。
- 删除快照阶段的 64 KiB 文件头探测和 gzip 预解压；正文索引、gzip 解压筛选均进入既有
  分批预算。元数据遍历及每文件最多 256 字节身份样本属于固定校验开销，不计入正文扫描量。
- 回归覆盖：乱序/修改时间不一致不漏查、缓存跳过与追加命中、gzip 在小批次预算内续查。
  保留 Claude 的租约过期部分结果修复。


## 追加：按用户确认改为最近两个文件流式筛选（2026-09-08）

- 替代前面的索引方案。每个来源按修改时间倒序选择最近两个文件（含 gzip，时间相同按
  文件名升序），选完才读取身份样本。旧文件不读正文，页面不新增文件范围提示。
- 删除内存时间索引与缓存，正文顺序读取一次即完成过滤；每批 64 MiB、约 20 秒后
  Agent 自动续读同一快照。新查询重新读取并包含新增内容，不进行后台建索引。
- 保留时间/Request ID/错误码/关键词 AND 筛选、多行上下文、脱敏、2,000 行/512 KiB
  返回限制、10 分钟总超时、72 小时及单次 1 小时区间限制。
- 回归覆盖 140 MiB 文件中间命中且正文读取量恰好等于文件大小、首批即返回匹配、
  游标幂等、gzip 跨批、多行跨批、新文件不改变在途快照、旧文件不参与查询。

## 追加：26812c5 撤销文件剪枝 + 3801bbb 改为最新两个文件流式筛选（2026-09-08 复验通过）

- 26812c5：撤销我 5f21e90 的 mtime/首条记录整文件剪枝，理由是二者都不能证明全文件的
  事件时间范围（乱序、文件时间与内容时间不一致会漏查且 Complete=true）。我接受这个
  取舍——它以多读文件换不漏查，风险方向正确。
- 3801bbb（按用户与 codex 确认）：删除内存时间索引与缓存，每个来源只取修改时间最新的
  2 个日志文件（含 gz，同时间按文件名升序），从头顺序读取边读边过滤；每批 64 MiB /
  约 20 秒自动续查同一快照；新查询重新读取并包含追加内容。其余筛选、脱敏、返回上限、
  超时、72 小时/1 小时区间限制保留。

### 实证
- `go vet`、`go test ./...` 全绿（containerlogs 21 条）；`pnpm typecheck`/`build` 通过。
- 端到端（真实 reader 代码 + 假 docker 发现，真跑 agent）：当前 142 MiB + 10 MiB 的
  .log.1 + 10 MiB 的 .log.2，三处各埋 3 行目标：
  - 目标在当前文件正中：两批 11 秒命中 3 行，读 154 MiB（两个文件全量）。
  - 目标在 .log.1（第二新）：命中 3 行。
  - 目标在 .log.2（第三新，被排除）：**0 行、Complete=true、无任何提示**，该时间点
    仍在允许的 72 小时范围内。
- 每次新查询都重新读取两个文件全文（本例 154 MiB 约 10 秒），无索引复用；这是本方案
  的既定代价，生产上按文档在联调时测量 I/O 影响。

### 记档（P2，建议用户拍板）
- **查询窗口允许 72 小时，但实际只搜最新 2 个文件，超出部分静默返回"完整、无匹配"**。
  按天轮转时第 3 天以前的时间点必然查不到却显示完整；按大小轮转、流量大时可能连
  昨天都查不到。`excluded` 已统计但未展示（文档写明"页面不增加排除数量提示"）。
  建议至少二选一：完成说明标注"仅扫描最新 2 个文件，另有 N 个更早文件未查询"，
  或把允许的查询范围收到与实际覆盖一致。改动很小，等用户确认后再做。

### 部署
- 只改 reader（agent 包）与前端文案；随本批进 rc101（server + 前端 + agent + reader，
  service 需重装以套用低调度优先级）。
