# 验收记录：通知渠道按站点与告警类型分流（2026-09-11）

- 范围：`8535205 feat: route alert notifications by site and rule type`
- 结论：**通过，零缺陷**。codex 自述真库集成测试未运行、浏览器未验收，本次已补齐。

## 变更
- 075 迁移：notification_channels 加 `site_id`（索引）与 `rule_keys`（JSON）。
- 每个渠道绑定且只绑定一个有效站点；站点与告警类型（rule_keys，空=该站点全部类型，
  11 个已知类型，未知类型 400）同时匹配才投递；已绑定渠道不能跨站点更新（409）。
- 渠道/投递记录/重发接口必填 `site_id`；旧渠道通过 `unassigned=true` 列出，"旧渠道待分配"
  中指定站点后恢复投递。编辑时 Webhook/密钥留空保留原值，响应只返回脱敏地址与是否有密钥。
- 路由映射由实例→站点构建（已删除实例排除）；余额告警等站点级告警使用站点内真实实例 ID，
  能正确映射。

## 实证
- `go vet`、`go test ./...` 全绿（含真库 `TestMySQLNotificationRoutingRoundTrip`，MariaDB 上
  075 的 JSON 列正常）；`pnpm typecheck`/`build` 通过。
- 接口实测：无 site 列表 400；无站点创建 400；带站点+类型创建 200 并返回脱敏地址；未知类型
  400；不存在站点 400；模拟旧渠道（site 空）出现在 unassigned 列表，分配到 demo 且 Webhook
  留空保留原值；跨站点改绑 409；投递记录无 site 400、按站点 200；重发他站/不存在记录 404。
- 页面 1366：站点渠道表（类型标签、脱敏地址、接收类型）、添加/编辑入口、投递记录渲染正常。

## 记档
- **部署后旧渠道全部暂停投递（P2 级运营影响，设计如此）**：075 后历史渠道 site_id 为空，
  在管理员进入"通知设置 → 旧渠道待分配"逐个指定站点并保存前，所有告警不会推送。
  上线后必须立即完成分配，否则告警静默丢失。
- 一个渠道只能属于一个站点；多站点共用同一机器人需要按站点各建一条渠道（Webhook 可相同）。
- Agent 侧独立企微直推不在本批范围，仍按原配置工作。

## 环境事故（与代码无关）
- 烟测 MariaDB 数据目录也在 /tmp，30 天清理删掉了 6 张表的表空间文件与服务端二进制（进程
  靠内存存活，重启即失），075 在旧库套用失败暴露此事。已把 Go/Node/MariaDB/烟测栈整体迁到
  `~/work/controlTower/.tooling`，全新库重放 74 个迁移并导入种子后继续验收。生产不受影响。

## 部署
- server + 前端，075 自动套用，未动 agent。rc107 打在 6ecfaf2 不含本批与前一批容器日志修正，
  上线需重打 rc108（server + 前端 + agent/reader）。
