# 验收记录：账单收尾修补四笔（2026-08-04）

范围：bb59d9c / 25fb757 / c3f26fd / 372215f（未立项修补）。验收环境：Linux，Go 1.24.5 + Node 22（typecheck+build）。

## 结论：全部通过；一项设计口径按实测定稿变更（文档已对齐）

- **372215f 回退计价改为 quota 换算（重要口径变更）**：未配 CT 价的模型金额从"ModelRatio 现算"改为 `quota÷站点 QuotaPerUnit`（快照取，缺省 500000）。理由成立且来自对账实践：newapi **内置默认价格**的模型不在 ModelRatio 配置里，现算路径对其无从取价；quota 已含全部倍率与内置默认，换算即准确。语义从"当前倍率"回到"当时实扣"。测试同步更新；设计文档 §4 已对齐（ee3c2d4）。此为该口径第三轮演进（quota→ratio→quota），本轮以生产对账实证收口；
- **25fb757**：日志页默认时间区间终点改为 now+1h——避免边界含义/时钟偏差吞掉刚写入的行，合理；
- **c3f26fd**：is_stream 对旧版数据三态化（true/false/未知），stream_status 存在作为流式的正向证据、缺失不误判为非流式——细致；
- **bb59d9c**：只读用户页头部样式简化，无碍。

全量测试 + typecheck + build 绿。

## 部署提示

**v2.0.0-rc24 打在 c3f26fd，不含 372215f 回退计价修复**——该修复直接影响账单对账（步骤 6），若 rc24 尚未部署建议弃用、在含全部修复的节点重打（rc25 或移动 rc24）；遗留分支 agent/billing-management 可删。
