# 专项验收：用户账单 / 使用日志 / 用户管理 / 账单计价 四页面（2026-08-04）

用户点名对四个菜单功能做深度验收（代码级全链路：路由/守卫→接口→数据流→渲染；typecheck+build 复验）。注：**"渠道账单"不存在**——按渠道拆分账单在 B3 批次明确不做（记档），如属新需求另行立项。

## 用户账单（/billing）——通过，2 处体验缺陷已修（761df61）

- 月份选择/搜索/翻页（无总数式翻页组件契约使用正确）/全站合计（缓存至日切）/数据截止说明/CSV 导出（BOM 已在）/viewer 可见且被闸钉到 scope 站点——全部达标；
- **修复①**：明细抽屉"查看使用日志"深链带 user_id/month 但日志页不读参数（跳转落默认视图，误导）——改传 username（日志页真实筛选参数）+ 日志页挂载读 query 初始化用户与整月区间（封顶 now+1h）；
- **修复②**：明细行缺档位列——多档模型的两行无法区分，补"档位"列（≥N / —），API 本就输出 tier_from。

## 使用日志（/readonly-logs）——通过

筛选组（用户/令牌/模型/请求ID/类型/时间区间）、区间必填含上限、分页、脱敏摘要、other JSON 解析带 WeakMap 缓存、is_stream 三态、quota→金额换算、未配 DSN 降级——全部达标；深链初始化随修复①补齐。

## 用户管理（/readonly-users）——通过

带总数的标准分页、余额/总额度进度条展示（quota 按站点单位换算）、状态筛选、viewer 自动加载、admin 用户过滤——达标；只读边界正确（无任何写操作入口）。

## 账单计价（/billing/pricing，admin-only）——通过

三重守卫（路由 adminOnly+viewer 重定向+服务端闸）；阶梯编辑客户端校验与服务端一致（首档 0、严格递增）；**生效日期语义在 UI 文案中明示**（"保存新增一套生效日期，不覆盖历史账单"）；new-api 价格建议导入（过滤已配模型、显示 QuotaPerUnit）、分组倍率 Tab——全部达标。

## 结论

四页面（另含账单计价）验收通过；两处体验缺陷已修。渠道账单待用户确认口径。
