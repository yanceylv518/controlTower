# 令牌账单部署说明

部署包含迁移 `046_billing_token_daily_versions.sql`，Server 启动时会自动创建令牌日账单表，不修改 new-api 数据库。

升级前生成的历史账单不包含令牌维度。升级后需对需要查看令牌账单的区间执行一次“强制重新生成”；重生成完成前，用户账单的“按令牌”页会显示明确提示。令牌请求明细下载实时读取 new-api 日志，不依赖历史账单重生成。
