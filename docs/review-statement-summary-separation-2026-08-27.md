# 验收记录：用户/上游账单汇总分离 + ZIP 下载反馈（2026-08-27）

- 范围：`d86bb8e fix(billing): separate user and upstream summaries`
- 结论：**通过**。

## 交付面清点（两个,均有当日 PROJECT_PROGRESS 拍板条目对应）

1. **用户账单维度纠正（服务端）**：用户账单区间统计只按模型汇总、日账单按
   日期+模型汇总——跨渠道合并,渠道列不再出现;固定原价,不读取渠道折扣
   （折扣列恒 1.000000,final=原价）;按令牌统计同样不吃折扣。渠道维度与
   渠道折扣自此仅属上游账单。
2. **ZIP 下载反馈（前端）**：下载期间持续提示条+按钮 loading+全局防重复
   点击;`URL.revokeObjectURL` 从同步改为 60s 延迟释放——同步释放会让部分
   浏览器在大文件尚未接管 blob 地址时取消下载。

## 核对要点

- `groupStatementRows` 统一了预览与工作簿两处的汇总逻辑（原先重复实现）：
  user 键=模型（daily 加日期前缀）,upstream 键=渠道|模型|折扣不变。
- 工作簿日表折扣单元格公式引用区间统计行：daily 行重算 key 时 user 行
  ChannelID 恒 0（合并时不回填）,与汇总 sheet 的 key 严格一致,引用不断链。
- upstream 路径行为不变（折扣查询、分组键、渠道列全保留）,两条新单测分别
  钉住 user 跨渠道合并无折扣、upstream 保留渠道折扣分组。
- user 任务不再查询折扣表（ListBillingStatementDiscounts 仅 upstream 调用）。
- **本笔为展示层改动**：聚合行仍按 日期+渠道+模型 落库,分组在预览/下载时
  计算——**已生成账单立即生效,无需重生成**。

## 测试

- server 树 vet + test 全绿（含 CT_MYSQL_TEST_DSN 真库）;
  webapp `pnpm -r typecheck` + `build` 通过。

## 记档

- P3 观感：上游日表排序改为分组键字典序,渠道 ID 按字符串比较
  （146 排在 76 前）;原为 SQL 行序。金额无影响。
- 曾生成且吃过折扣的用户账单,重新打开预览/下载即显示原价新口径,与其
  历史下载留档可能不一致——对外已交付过的用户账单留意口径解释。
