# 验收记录：v2.9-B2.8 低流量稀疏采样（2026-07-31）

范围：d563169。验收环境：Linux，Go 1.24.5 + Node 22。

## 结论：通过，零缺陷

- `go vet` + 全量测试 + `pnpm typecheck` 全绿；自查清单已贴。
- **稀疏统计**：QueryRecentChannelBuckets 用 `CONCAT(?,':channel:',?)` 正确构造 dimension key（未复现历史提码坑）；LIMIT=sparse_min_samples 桶数上界正确（每桶 request_count>0）；逐桶累计至 ≥10 条即止，凑不齐跳过。
- **新鲜度守卫**：最新桶必须落在本评估窗口内，否则整体跳过——同批旧请求不会反复驱动判定（有测试）。
- **隔离正确**：稀疏路径下 latencyWindows 强制清零、不做基线查询；动态配权仍要求窗口足量样本，不受稀疏影响（有测试）。
- **盲区闭合**：试岗中低流量渠道经稀疏统计可完成留任/再降判定（专项测试在）；无数据窗口同时重置 trialHealthyWindows，留任要求连续可评估的健康窗口——语义比旧版更严，合理。
- 参数校验（sparse_min_samples ≤ min_samples、回看介于窗口与 2880 之间）、默认值 10/360、旧 JSON 解码回退默认、页面两字段+说明卡+表单上限联动、evidence 带 sparse/样本数/跨越时段——逐项符合批次文件。

## 部署提示

纯策略与引擎逻辑，无迁移。**至此 v2.9 全系列完结**，建议打 rc20 全量部署（含 v2.8 多站点系列）；auto 正式启用前仍欠"人工优先 24h 静默"批次。
