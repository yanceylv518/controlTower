# 验收记录：v3.0-B2 连续配权引擎（2026-08-01）

范围：dbddbf8（直接按设计文档交付，无批次文件与自查清单——记档）。验收环境：Linux，Go 1.24.5 + Node 22。

## 结论：通过，4 个必修缺陷已由验收方直接修复（ab4f184）

### 交付质量（好的部分）

- 公式实现与设计 §1 一致：TTFT 三元组 0.5/0.3/0.2 加权、`^0.5λ`、OTPS 上限、总倍率 clamp 1.5、基线算术平均且组内可比渠道 <2 不调权、K_error 用归因口径；019 状态表结构完备（因子/写入对账/增量游标字段齐全）；每分钟节奏正确（不受 15 分钟窗口门控）；哨兵已扩展覆盖 dispatch_modes 的 auto；写入走事务化 rec+command；observe 档算而不写、页面可见拟写值；ContinuousTuningView 覆盖 §6 主要区块（基础值编辑/因子状态/事件流水/计数卡）。测试与 typecheck 绿。

### 验收修正（四处必修，已修，附回归测试）

1. **P1 重复写入洪水**：auto 去抖只对比快照当前值，而快照约 10 分钟一刷——一次调整后每分钟重发相同命令直至快照追上。修复：对账自身 LastWrittenWeight。
2. **P1 人工接管误报（auto 自毁）**：2 分钟墙钟宽限对抗 10 分钟快照延迟，任何自动写入 2 分钟后被自己误判为人工改动而自暂停。修复：基础值查询带出快照 captured_at，仅当快照晚于写入 2 分钟以上且仍不一致才判人工接管。
3. **P1 K_error 计数漏计/重计**：每 tick 折算"最近 1 分钟"，agent 上报延迟导致晚到桶永久漏计、tick 抖动可重计。修复：019 补 last_bucket_at 游标（未发布迁移就地修改），按 90 秒沉降滞后逐桶折算、每桶恰一次；表中预留的增量字段真正投入使用。
4. **P2 混布保险丝缺失 + 审计缺失**：设计 §4 明确保留的混布跳过未实现（多模型渠道会被单模型指标调权）；自动写权无 operation_audits。均已补（混布置 paused_reason=mixed_channel 并记事件一次）。

### 记档

- **值班引擎以类型断言短路方式整体退役**（生产路径不再执行 demote/trial/rebalance/信息建议），代码与其测试仍在（测试测的是死路径）——退役意图符合设计，但清理未做，**待立清理批次**（含 criteria/scheduling 遗留字段、duty 引擎代码、相关测试、旧 TuningView.vue 死文件——router 已改指 ContinuousTuningView，旧文件零引用，删除待用户示意）。
- 无批次文件、无自查清单（直接按设计文档交付）；本次验收按设计文档 §1-§6 逐项对照。
- B3 未做项照设计：熔断/探针/软启动/优先级联动（引擎注释已标注 deferred）。

## 加固补交验收（2026-08-01 追加，13f8151）

结论：**通过，零缺陷**。两处：①基础值查询过滤只留启用渠道——补上设计 §4"只碰启用渠道"护栏，状态值列表与 agent enabled 语义一致；②自动写权事件状态 adopted→auto_executed，与 v2.9 auto 语义统一。全量测试+typecheck 绿。记档：被禁用渠道的基础值行随之从页面消失（锚点数据仍在表中，重新启用即恢复显示）——可接受的连带行为。

## 部署提示

019 迁移随下次 Server 发布执行；建议全模型先 observe 观察因子行为几天再逐模型开 auto。
