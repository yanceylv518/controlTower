# 项目会话规则

## 每次新会话必须先完成全项目梳理

每次在本项目开启新会话时，必须先通览整个项目，详细了解项目整体进度，再回到新会话的任务要求进行理解、规划和执行。不得直接依据新任务描述跳到局部文件开始修改。

### 必须执行的启动顺序

1. **读取项目规则与项目入口**：读取本文件、适用的子目录 `AGENTS.md`、根目录 `README.md` 和 `docs/README.md`，明确项目目标、架构边界和文档组织。
2. **通览整个项目结构**：列出项目文件与主要目录，覆盖 `agent/`、`server/`、`internal/`、`webapp/`、`web/`、`deploy/`、`.github/` 和 `docs/`；查看各模块说明、程序入口、核心功能组织、依赖声明、数据库迁移、测试入口、构建与发布配置，建立模块职责和数据流的整体认识。全项目梳理不能仅限于新任务涉及的模块；依赖目录、缓存、构建产物、二进制和运行数据不要求逐文件阅读，不读取或展示凭据等敏感内容。
3. **详细核对整体进度**：读取 `PROJECT_PROGRESS.md` 的当前总览、`docs/development-progress.md`，并沿当前总览中的链接读取最新任务总结及交接记录，结合 `docs/tasks/` 和 `docs/ai-tasks/` 中的相关记录，明确各主要功能已完成、进行中、待完成、受阻及待验收的内容，以及已知问题和下一步。较长文档应分段读取，不能把截断内容当作已经读完。
4. **用当前仓库状态校验记录**：检查 `git status --short`、当前分支、近期提交及未提交改动概况，必要时核对相关实现。区分“已实现”“已测试”“已提交/推送”“已发布”“已部署”和“已验收”，不能相互替代。文档与代码或 Git 记录冲突时，明确记录差异和证据，不沿用过期结论，也不覆盖已有未提交工作。
5. **先给出项目理解，再处理新任务**：开始针对新任务的方案设计或修改前，向用户简要说明当前架构、主要功能整体进度、最近变更、未完成事项和验证缺口。完成上述梳理后，再重新阅读本次用户任务，确定任务在整体项目中的位置、依赖和影响范围，然后开始执行。

### 执行要求

- 以上步骤是每次新会话的默认必做前置流程，不需要用户重复提醒，也不能用上一会话的记忆代替当前仓库检查。
- 启动梳理采用只读检查，不要求为了解项目而安装依赖、运行全部测试、启动服务或执行发布部署。
- 无法读取的信息应明确标为未知，不能声称已经完整了解；能够独立完成的梳理继续进行。
- 会话结束前，如本次任务改变了项目进度、验证结果或待办事项，应同步更新 `PROJECT_PROGRESS.md`，必要时补充对应任务交接记录，方便后续新会话恢复上下文。
