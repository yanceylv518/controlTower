# Control Tower 项目进度

> 最后更新：2026-08-25  
> 当前基线提交：`cbe2e443 feat: redesign billing workflow and interval statements`

本文档用于在新会话、上下文压缩或人员交接后快速恢复项目背景。开始处理本项目任务前，应先阅读本文档以及 `README.md`；完成重要设计或功能变更后，应同步更新本文档。

## 1. 项目定位

Control Tower 是独立于 new-api 的多站点监控与轻运维系统。各站点彼此隔离，不存在跨站点的同一客户。Control Tower 通过站点配置的只读查询连接读取业务数据，不修改 new-api 数据库，也不进入用户请求链路。

当前工作的重点是重新设计账单系统，使账单可生成、可修复、可核对、可按区间查看和下载，并保持站点、用户和数据源边界清晰。

## 2. 已确认的账单设计

### 2.1 账单生成

- 账单不在项目启动时自动全量生成，也不由定时任务自动刷新。
- 账单生成的唯一入口是用户手动创建“账单任务”。
- 账单任务按站点和日期区间生成，每个账单日可追踪当前生效版本。
- 如果历史计算逻辑错误，可对指定日期区间重新生成；新任务完成后切换生效版本，查询只读取当前生效版本。
- 用户账单和渠道账单只负责查询，不在查询页面隐式触发生成。
- 查询区间缺少账单或账单不完整时，页面提示缺失日期，并引导用户前往账单任务生成对应区间。

### 2.2 日期规则

- 所有账单查询只选择日期，不提供时分秒。
- 查询的结束日期为包含关系。
- 当天账单不纳入可选范围；例如当前日期为 2026-08-25，账单最多计算并查询到 2026-08-24（包含 8 月 24 日）。
- 账单业务日期统一使用账单业务时区，当前为 `Asia/Shanghai`。

### 2.3 多站点与权限

- 不同站点的数据完全隔离，不合并用户、令牌、渠道或金额。
- 只有配置并验证过只读查询连接的站点，才允许出现在账单查询和数据查询的可选站点中。
- 服务端必须执行相同的只读连接检查和用户作用域检查，不能只依赖前端隐藏选项。

### 2.4 账单术语

- 原“计费异常”功能改为“账单核对”，表达其用途是核对账单计算结果。
- 列表中的异常结果使用“待确认”语义展示数量和金额，避免把所有差异直接定性为错误。
- 账单系统不再使用“CT 金额”概念。
- 金额区分“正常账单金额”和“待确认金额”。

### 2.5 用户账单层级

用户账单的业务层级固定为：

`用户 → 令牌 → 日期 → 模型`

用户区间账单抽屉支持两种查看方式：

- 不按令牌：直接展示区间内的“日期 → 模型”日账单列表。
- 按令牌统计：第一层按令牌汇总，展开令牌后展示“日期 → 模型”明细。

每一层需要展示请求数、普通输入 Token、缓存读取 Token、缓存写入 Token、输出 Token、待确认数量、待确认金额和正常账单金额。

## 3. 当前已实现

### 3.1 后端与数据

- 增加账单日状态和当前生效版本相关迁移：
  - `server/migrations/050_billing_day_status.sql`
  - `server/migrations/051_repair_billing_daily_active.sql`
  - `server/migrations/052_fix_billing_request_bill_day.sql`
- 删除服务启动时的自动账单调度，保留手动账单任务处理能力。
- 用户日账单按“日期 + 模型”聚合。
- 用户令牌账单按“令牌 + 日期 + 模型”聚合。
- 待确认数量和金额按相同用户、令牌、日期、模型维度计算，避免跨模型重复统计。
- 增加账单覆盖率计算、缺失日期提示和只读站点服务端校验。
- 历史账单可以依据当前生效任务版本查询和重新生成。

### 3.2 用户账单页面

- 默认日期区间为当月 1 日至昨天。
- 主列表展示用户区间汇总，包括各类 Token 总量、待确认数据和正常账单金额。
- 用户行只保留“查看区间账单”入口。
- 区间账单抽屉顶部提供“不按令牌 / 按令牌统计”切换和“下载当前账单”按钮。
- 不按令牌时展示日期模型列表；每一行均可下载该“日期 + 模型”的请求明细。
- 按令牌时展示令牌汇总；展开令牌后展示该令牌的日期模型明细。

### 3.3 下载格式

- 不按令牌下载：Excel 包含“区间汇总”和“日账单列表”。
- 按令牌下载：Excel 包含“令牌汇总”和“令牌账单明细”。
- 日账单行明细下载严格按用户、当前生效任务、日期和模型过滤。
- 对没有旧版落地日文件的历史账单，可直接从当前生效账单请求明细动态生成 Excel。

### 3.4 渠道账单与账单核对

- 渠道账单已同步日期区间、账单完整性、Token 汇总和待确认语义。
- 导航与路由中的“计费异常”已调整为“账单核对”。
- 账单核对继续承担差异分类、请求级核对和人工确认辅助职责。

## 4. 关键接口

- `GET /api/dashboard/billing/user-days`：用户的日期模型账单。
- `GET /api/dashboard/billing/user-token-days`：指定用户的令牌、日期、模型账单。
- `GET /api/dashboard/billing/range-workbook`：指定用户的区间账单下载。
  - 默认：不按令牌格式。
  - `group_by=token`：按令牌格式。
- `GET /api/dashboard/billing/files`：日账单或日账单模型明细下载。
  - 传入 `model_name` 时，下载该日期与模型对应的请求明细。
- 账单任务相关接口仍是生成和重算账单的唯一写入口。

所有账单接口都必须携带 `instance_id`，用户相关接口还必须验证 `user_id` 是否在当前登录用户作用域内。

## 5. 本地开发与验证

- 本地后端启动脚本：`deploy/start-server-local.ps1`。
- 默认后端监听：`127.0.0.1:18081`。
- 前端开发地址通常为：`127.0.0.1:5173`。
- 本地密钥、数据库密码和管理员密码不得写入本文件或提交到仓库。

提交前至少执行：

```powershell
go test ./server/...
cd webapp
pnpm typecheck
```

涉及 Excel 下载时，还应实际请求接口并检查工作簿名称、工作表名称、文件可打开性以及明细过滤范围。

## 6. 最近验证结果

- `go test ./server/...` 通过。
- `pnpm typecheck` 通过。
- 用户令牌接口已用本地数据验证能返回多个令牌及多个日期模型记录。
- 两种区间下载已验证为不同工作表结构：
  - 不按令牌：`区间汇总, 日账单列表`
  - 按令牌：`令牌汇总, 令牌账单明细`
- 日期模型明细下载已验证返回 HTTP 200，并能动态生成 `.xlsx` 文件。

## 7. 后续工作原则

- 新需求必须保持“站点隔离、手动生成、版本生效、日期闭区间、查询不写数据”的核心约束。
- 用户账单的层级不得改回并列的“按模型 / 按令牌”视图；按令牌是在用户之下增加的一层统计。
- 新增汇总维度时，正常金额与待确认金额必须使用相同维度，防止重复计数。
- 页面统计、区间下载和逐行明细下载必须来自同一当前生效账单版本。
- 修改账单口径、页面层级、任务流程、数据表或关键接口后，必须同步更新本文档的“已确认设计”“当前已实现”和“最近验证结果”。

## 8. 工作区注意事项

- 工作区可能存在用户自己的未提交修改，处理任务时不要重置、覆盖或删除无关变更。
- `.gocache/`、`.tmp/`、`data/`、`release/` 等本地产物不属于账单功能源码，不应随功能提交。
- `deploy/start-server-local.ps1` 可能包含独立的本地环境调整；除非任务明确要求，否则不要与业务功能提交混在一起。
