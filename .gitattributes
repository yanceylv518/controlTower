# Shell scripts and systemd units must stay LF even on Windows checkouts;
# CRLF breaks the shebang line ("env: 'bash\r': No such file or directory").
*.sh text eol=lf
*.service text eol=lf
*.example text eol=lf
*.sql text eol=lf
